PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src> ls


    Répertoire : C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src


Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
d-----        13/12/2025     04:21                api
d-----        13/12/2025     01:37                assets
d-----        12/12/2025     20:18                components
d-----        13/12/2025     21:18                pages
-a----        11/12/2025     23:52             59 App.css
-a----        13/12/2025     21:21           1452 App.js
-a----        11/12/2025     23:38            246 App.test.js
-a----        11/12/2025     23:52             59 index.css
-a----        12/12/2025     18:09            416 index.js
-a----        11/12/2025     23:38           2632 logo.svg
-a----        11/12/2025     23:38            362 reportWebVitals.js
-a----        11/12/2025     23:38            241 setupTests.js


PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src> cd .\components\
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\components> ls


    Répertoire : C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\components


Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
-a----        12/12/2025     20:19           9598 Contact.js
-a----        11/12/2025     23:57            291 Footer.js
-a----        12/12/2025     20:06           5070 Frip2Reve.js
-a----        12/12/2025     20:12           8059 Hero.js
-a----        12/12/2025     20:01           4298 Pourquoi.js
-a----        12/12/2025     19:56           3810 Realisations.js
-a----        12/12/2025     20:08           3562 Temoignages.js
-a----        13/12/2025     21:44          10804 TopNav.js
-a----        12/12/2025     19:54           3300 Valeurs.js


PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\components> cat *.js
import React, { useState } from 'react';
import PropTypes from 'prop-types';

// ContactSection: bloc contact prÃªt Ã  intÃ©grer
// - Fournir props pour personnaliser adresse, email, tÃ©lÃ©phone, horaires, handlers
// - Formulaire simple avec honeypot anti-spam
// - Appel onSubmit pour gÃ©rer l'envoi cÃ´tÃ© serveur

export default function ContactSection({
  address = 'Votre adresse (ex: 12 rue de la SolidaritÃ©, 75010 Paris, France)',
  email = 'contact@revedivoire.org',
  phone = '+33 1 23 45 67 89',
  hours = 'Lun - Ven, 9h - 17h',
  mapsHref = '#',
  onSubmit = null, // function(formData) => Promise
  quickLinks = [
    { key: 'don', label: 'Faire un don', href: '/don' },
    { key: 'benevole', label: 'Devenir bÃ©nÃ©vole', href: '/benevole' },
    { key: 'boutique', label: 'Boutique Frip2RÃªve', href: '/boutique' },
    { key: 'actions', label: 'Actions & projets', href: '/actions' },
    { key: 'a-propos', label: 'Ã€ propos', href: '/a-propos' },
    { key: 'contact', label: 'Contact', href: '/contact' },
  ],
  showSocial = true,
  social = {
    instagram: 'https://www.instagram.com/frip2reve',
    facebook: 'https://www.facebook.com/',
    tiktok: '',
  },
  edisynchroText = 'PropulsÃ© par EDISYNCHRO',
}) {
  const [form, setForm] = useState({ name: '', email: '', subject: 'GÃ©nÃ©ral', message: '', hp: '' });
  const [status, setStatus] = useState({ loading: false, success: null, error: null });

  function handleChange(e) {
    const { name, value } = e.target;
    setForm((s) => ({ ...s, [name]: value }));
  }

  async function handleSubmit(e) {
    e.preventDefault();
    setStatus({ loading: false, success: null, error: null });

    // honeypot check: if filled, treat as spam
    if (form.hp) {
      setStatus({ loading: false, success: null, error: 'Envoi bloquÃ© (spam dÃ©tectÃ©).' });
      return;
    }

    // validation minimale
    if (!form.name.trim() || !form.email.trim() || !form.message.trim()) {
      setStatus({ loading: false, success: null, error: 'Veuillez remplir nom, eâ€‘mail et message.' });
      return;
    }

    const payload = { name: form.name.trim(), email: form.email.trim(), subject: form.subject, message: form.message.trim() };

    try {
      setStatus({ loading: true, success: null, error: null });
      if (typeof onSubmit === 'function') {
        await onSubmit(payload);
      } else {
        // fallback : simple fetch example (commentÃ©)
        // await fetch('/api/contact', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
        await new Promise((r) => setTimeout(r, 600)); // simulate
      }

      setStatus({ loading: false, success: 'Message envoyÃ©, merci !', error: null });
      setForm({ name: '', email: '', subject: 'GÃ©nÃ©ral', message: '', hp: '' });
    } catch (err) {
      setStatus({ loading: false, success: null, error: 'Erreur lors de l\'envoi â€” rÃ©essayez.' });
    }
  }

  return (
    <section aria-labelledby="contact-title" className="py-12 px-6 bg-white">
      <div className="max-w-6xl mx-auto grid grid-cols-1 lg:grid-cols-3 gap-8">
        {/* CoordonnÃ©es */}
        <div className="lg:col-span-1 bg-gray-50 p-6 rounded-lg shadow-sm">
          <h2 id="contact-title" className="text-xl font-bold text-gray-900">Contact</h2>
          <p className="mt-2 text-sm text-gray-600">Trouvez l'info en 3 secondes.</p>

          <div className="mt-4 text-sm space-y-3 text-gray-800">
            <div>
              <div className="font-medium">Adresse</div>
              <address className="not-italic">{address}</address>
              {mapsHref && mapsHref !== '#' && (
                <a href={mapsHref} target="_blank" rel="noreferrer" className="text-sm underline inline-block mt-1">Voir sur Google Maps</a>
              )}
            </div>

            <div>
              <div className="font-medium">Email</div>
              <a href={`mailto:${email}`} className="underline">{email}</a>
            </div>

            <div>
              <div className="font-medium">TÃ©lÃ©phone</div>
              <a href={`tel:${phone}`} className="underline">{phone}</a>
            </div>

            <div>
              <div className="font-medium">Horaires</div>
              <div className="text-sm text-gray-600">{hours}</div>
            </div>
          </div>

          {/* RÃ©seaux sociaux */}
          {showSocial && (
            <div className="mt-6">
              <div className="font-medium text-sm text-gray-900">RÃ©seaux</div>
              <div className="mt-2 flex items-center gap-3">
                {social.instagram && (
                  <a href={social.instagram} target="_blank" rel="noreferrer" aria-label="Instagram" className="text-gray-700 hover:text-gray-900">
                    <svg className="w-5 h-5" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><rect x="3" y="3" width="18" height="18" rx="4" stroke="currentColor" strokeWidth="1.5"/><circle cx="12" cy="12" r="3" stroke="currentColor" strokeWidth="1.5"/><circle cx="18.5" cy="5.5" r="0.5" fill="currentColor"/></svg>
                  </a>
                )}
                {social.facebook && (
                  <a href={social.facebook} target="_blank" rel="noreferrer" aria-label="Facebook" className="text-gray-700 hover:text-gray-900">
                    <svg className="w-5 h-5" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M18 2h-3a4 4 0 0 0-4 4v3H8v4h3v8h4v-8h3l1-4h-4V6a1 1 0 0 1 1-1h3z" stroke="currentColor" strokeWidth="1.2"/></svg>
                  </a>
                )}
                {social.tiktok && (
                  <a href={social.tiktok} target="_blank" rel="noreferrer" aria-label="TikTok" className="text-gray-700 hover:text-gray-900">
                    <svg className="w-5 h-5" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M9 8v8a4 4 0 1 0 4-4V6h3" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/></svg>
                  </a>
                )}
              </div>
            </div>
          )}


        </div>

        {/* Formulaire */}
        <div className="lg:col-span-2 bg-white p-6 rounded-lg shadow-sm">
          <h3 className="text-lg font-semibold text-gray-900">Envoyer un message</h3>
          <p className="mt-2 text-sm text-gray-600">Choisissez l'objet si besoin. Nous rÃ©pondons dÃ¨s que possible.</p>

          <form onSubmit={handleSubmit} className="mt-4 space-y-4">
            <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
              <div>
                <label htmlFor="name" className="block text-sm font-medium text-gray-700">Nom</label>
                <input id="name" name="name" value={form.name} onChange={handleChange} className="mt-1 block w-full rounded-md border-gray-200 shadow-sm px-3 py-2" required />
              </div>

              <div>
                <label htmlFor="email" className="block text-sm font-medium text-gray-700">Email</label>
                <input id="email" name="email" type="email" value={form.email} onChange={handleChange} className="mt-1 block w-full rounded-md border-gray-200 shadow-sm px-3 py-2" required />
              </div>
            </div>

            <div>
              <label htmlFor="subject" className="block text-sm font-medium text-gray-700">Objet</label>
              <select id="subject" name="subject" value={form.subject} onChange={handleChange} className="mt-1 block w-full rounded-md border-gray-200 shadow-sm px-3 py-2">
                <option>GÃ©nÃ©ral</option>
                <option>Don</option>
                <option>BÃ©nÃ©volat</option>
                <option>Boutique / Frip2RÃªve</option>
                <option>Partenariat</option>
              </select>
            </div>

            <div>
              <label htmlFor="message" className="block text-sm font-medium text-gray-700">Message</label>
              <textarea id="message" name="message" rows="5" value={form.message} onChange={handleChange} className="mt-1 block w-full rounded-md border-gray-200 shadow-sm px-3 py-2" required />
            </div>

            {/* honeypot anti-spam (invisible for humans) */}
            <div style={{ position: 'absolute', left: '-9999px', top: 'auto' }} aria-hidden>
              <label htmlFor="hp">Si vous Ãªtes humain, laissez ce champ vide</label>
              <input id="hp" name="hp" value={form.hp} onChange={handleChange} />
            </div>

            <div className="flex items-center gap-3">
              <button type="submit" disabled={status.loading} className="rounded-2xl px-5 py-2 bg-[#D6453A] text-white font-semibold">
                {status.loading ? 'Envoi...' : 'Envoyer'}
              </button>

              {status.success && <div className="text-sm text-green-600">{status.success}</div>}
              {status.error && <div className="text-sm text-red-600">{status.error}</div>}
            </div>
          </form>



        </div>
      </div>
    </section>
  );
}

ContactSection.propTypes = {
  address: PropTypes.string,
  email: PropTypes.string,
  phone: PropTypes.string,
  hours: PropTypes.string,
  mapsHref: PropTypes.string,
  onSubmit: PropTypes.func,
  quickLinks: PropTypes.array,
  showSocial: PropTypes.bool,
  social: PropTypes.object,
  edisynchroText: PropTypes.string,
};
//src/components/Footer.js
export default function Footer() {
  return (
    <footer className="bg-gray-800 text-white py-6 mt-10">
      <div className="container mx-auto text-center">
        &copy; 2025 Reve dâ€™Ivoire. Tous droits rÃ©servÃ©s.
      </div>
    </footer>
  );
}
import React from 'react';
import PropTypes from 'prop-types';
// Remplacez par vos vraies images dans /assets
import imgBanner from '../assets/association.jpg';
import imgProd1 from '../assets/association_4.jpg';
import imgProd2 from '../assets/association_1.jpg';
import imgProd3 from '../assets/association_5.jpg';

export default function EncartFrip2Reve({
  title = 'Frip2RÃªve â€“ Friperie solidaire',
  subtitle = "Chaque vÃªtement donnÃ© ou achetÃ© permet de financer les projets humanitaires de l'association en CÃ´te d'Ivoire.",
  banner = imgBanner,
  boutiqueHref = '/boutique',
  instagramHref = 'https://www.instagram.com/frip2reve',
  showPreview = true,
  previewItems = [
    { key: 'p1', image: imgProd1, alt: 'Robe vintage' , caption: 'Robe, bon Ã©tat' },
    { key: 'p2', image: imgProd2, alt: 'Chemise' , caption: 'Chemise, taille M' },
    { key: 'p3', image: imgProd3, alt: 'Sac' , caption: 'Sac, pratique' },
  ],
  onBoutique = null,
  onInstagram = null,
}) {
  return (
    <section aria-labelledby="frip-title" className="py-10 px-6 bg-gray-50">
      <div className="max-w-6xl mx-auto rounded-lg overflow-hidden shadow-sm bg-white">
        <div className="relative w-full h-48 sm:h-56 md:h-72 lg:h-80">
          <img src={banner} alt="Frip2RÃªve â€” friperie solidaire" className="w-full h-full object-cover object-center" loading="lazy" />
          <div className="absolute inset-0 bg-black/30" aria-hidden />
          <div className="absolute left-6 top-6 text-white z-10">
            <h3 id="frip-title" className="text-xl sm:text-2xl font-bold">{title}</h3>
            <p className="mt-1 text-sm sm:text-base max-w-lg">{subtitle}</p>
          </div>
          <div className="absolute right-6 bottom-6 z-10 flex gap-3">
            <button
              type="button"
              onClick={() => typeof onBoutique === 'function' ? onBoutique() : window.location.assign(boutiqueHref)}
              className="rounded-2xl px-4 py-2 bg-[#D6453A] text-white font-semibold shadow"
              aria-label="AccÃ©der Ã  la boutique"
            >
              AccÃ©der Ã  la boutique
            </button>

            <a
              href={instagramHref}
              target="_blank"
              rel="noreferrer"
              onClick={(e) => { if (typeof onInstagram === 'function') { e.preventDefault(); onInstagram(); } }}
              className="inline-flex items-center rounded-2xl px-4 py-2 bg-white/10 text-white font-medium"
              aria-label="Voir sur Instagram"
            >
              Instagram
            </a>
          </div>
        </div>

        {showPreview && (
          <div className="p-6 border-t border-gray-100">
            <h4 className="text-sm font-semibold text-gray-700">Mini-aperÃ§u boutique</h4>
            <div className="mt-3 grid grid-cols-3 gap-3">
              {previewItems.map((it) => (
                <div key={it.key} className="flex flex-col items-center text-center">
                  <div className="w-full h-28 overflow-hidden rounded-lg">
                    <img src={it.image} alt={it.alt} className="w-full h-full object-cover object-center" loading="lazy" />
                  </div>
                  <div className="mt-2 text-xs text-gray-700">{it.caption}</div>
                </div>
              ))}
            </div>

            <div className="mt-4 text-center">
              <button
                type="button"
                onClick={() => typeof onBoutique === 'function' ? onBoutique() : window.location.assign(boutiqueHref)}
                className="rounded-2xl px-5 py-2 bg-[#D6453A] text-white font-semibold"
                aria-label="DÃ©couvrir la boutique Frip2RÃªve"
              >
                DÃ©couvrir la boutique Frip2RÃªve
              </button>
            </div>
          </div>
        )}
      </div>
    </section>
  );
}

EncartFrip2Reve.propTypes = {
  title: PropTypes.string,
  subtitle: PropTypes.string,
  banner: PropTypes.string,
  boutiqueHref: PropTypes.string,
  instagramHref: PropTypes.string,
  showPreview: PropTypes.bool,
  previewItems: PropTypes.arrayOf(PropTypes.shape({ key: PropTypes.string, image: PropTypes.string, alt: PropTypes.string, caption: PropTypes.string })),
  onBoutique: PropTypes.func,
  onInstagram: PropTypes.func,
};

EncartFrip2Reve.defaultProps = {
  title: 'Frip2RÃªve â€“ Friperie solidaire',
  subtitle: "Chaque vÃªtement donnÃ© ou achetÃ© permet de financer les projets humanitaires de l'association en CÃ´te d'Ivoire.",
  banner: imgBanner,
  boutiqueHref: '/boutique',
  instagramHref: 'https://www.instagram.com/frip2reve',
  showPreview: true,
  previewItems: [
    { key: 'p1', image: imgProd1, alt: 'Robe vintage', caption: 'Robe, bon Ã©tat' },
    { key: 'p2', image: imgProd2, alt: 'Chemise', caption: 'Chemise, taille M' },
    { key: 'p3', image: imgProd3, alt: 'Sac', caption: 'Sac, pratique' },
  ],
  onBoutique: null,
  onInstagram: null,
};
import React, { useEffect, useState, useRef } from 'react';
import PropTypes from 'prop-types';
// Remplacez ces imports par vos vraies images dans /assets
import imgCollecte from '../assets/association_1.jpg';
import imgBoutique from '../assets/association_2.jpg';
import imgEnvoi from '../assets/association_3.jpg';

export default function Hero({
  images = [imgCollecte, imgBoutique, imgEnvoi],
  alt = 'Collecte / Boutique / Envoi â€” Frip2RÃªve',
  interval = 5000,
  kpis = { pagesViews: 0, avgSecondsOnActions: 0 },
  onPrimaryClick = null,
  onSecondaryClick = null,
  showMicroText = true,
}) {
  const [active, setActive] = useState(0);
  const imgs = Array.isArray(images) && images.length >= 1 ? images : [imgCollecte, imgBoutique, imgEnvoi];
  const timerRef = useRef(null);

  useEffect(() => {
    timerRef.current = setInterval(() => setActive((s) => (s + 1) % imgs.length), interval);
    return () => clearInterval(timerRef.current);
  }, [imgs.length, interval]);

  function formatTime(sec) {
    if (!sec || Number.isNaN(sec)) return 'â€”';
    const m = Math.floor(sec / 60);
    const s = Math.round(sec % 60);
    return `${m}m ${s}s`;
  }

  return (
    <section
      className="relative overflow-hidden"
      aria-labelledby="hero-title"
      role="region"
    >
      {/* Background image + overlay */}
      <div className="absolute inset-0 -z-20">
        <img
          src={imgs[active]}
          alt={alt}
          className="w-full h-full object-cover object-center blur-0"
          loading="lazy"
        />
        {/* Overlay lÃ©ger 30% */}
        <div className="absolute inset-0 bg-black/30 -z-10" aria-hidden="true" />
      </div>

      {/* Subtle lion SVG (filigrane) */}
      <div className="absolute top-6 left-6 z-30 pointer-events-none" style={{ opacity: 0.08 }} aria-hidden>
        <svg width="84" height="84" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M12 2c1.1 0 2 .9 2 2v0.5c0 .55-.45 1-1 1h-2c-.55 0-1-.45-1-1V4c0-1.1.9-2 2-2z" fill="#000" />
          <path d="M5 8c1.5 1 2 2 2 2s1-1 3-1 3 1 3 1 0-1 2-2c0 0-1 4 2 6 0 0-2 2-6 2s-6-2-6-2c3-2 2-6 2-6z" fill="#000" />
        </svg>
      </div>

      <div className="relative z-10 max-w-6xl mx-auto px-6 py-20">
        <div className="text-center sm:text-left grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
          {/* Left: Text + CTAs */}
          <div className="max-w-xl mx-auto lg:mx-0">
            {/* Gros titre court et punchy */}
            <h1 id="hero-title" className="text-3xl sm:text-4xl lg:text-5xl font-extrabold text-white leading-tight text-center lg:text-left">
              Redonner espoir aux enfants de CÃ´te dâ€™Ivoire.
            </h1>

            {/* Sous-titre simple (1-2 lignes) */}
            <p className="mt-4 text-base sm:text-lg text-white/90 text-center lg:text-left">
              Collectes, friperie solidaire Frip2RÃªve et envois sur le terrain pour soutenir familles et Ã©coles depuis 2016.
            </p>

            {/* CTA principal + secondaire */}
            <div className="mt-8 flex flex-col sm:flex-row sm:items-center gap-3 justify-center lg:justify-start">
              <button
                type="button"
                onClick={onPrimaryClick}
                className="inline-flex items-center justify-center rounded-2xl px-8 py-4 text-base font-semibold shadow-lg bg-[#D6453A] text-white w-full sm:w-auto"
                aria-label="Faire un don"
              >
                Faire un don
              </button>

              <button
                type="button"
                onClick={onSecondaryClick}
                className="inline-flex items-center justify-center rounded-2xl px-6 py-3 text-sm font-medium bg-white/10 backdrop-blur-sm text-white/95 w-full sm:w-auto"
                aria-label="DÃ©couvrir nos actions"
              >
                DÃ©couvrir nos actions
              </button>
            </div>

            {/* micro-texte rassurant (optionnel) */}
            {showMicroText && (
              <p className="mt-3 text-sm text-white/80 text-center lg:text-left">100% des dons utilisÃ©s pour des actions sur le terrain.</p>
            )}

            {/* Petits liens de confiance */}
            <div className="mt-6 flex flex-wrap items-center gap-3 text-sm text-white/90 justify-center lg:justify-start">
              <a href="/statuts" className="underline" aria-label="Voir statuts">Statuts</a>
              <span aria-hidden>â€¢</span>
              <a href="/rapport" className="underline" aria-label="Rapport d'activitÃ©">Rapport d'activitÃ©</a>
              <span aria-hidden>â€¢</span>
              <a href="/contact" className="underline" aria-label="Nous contacter">Contact</a>
            </div>

            {/* KPIs simples (facultatif) */}
            <div className="mt-6 inline-flex items-center gap-6 bg-white/6 text-white px-4 py-2 rounded-2xl text-sm mx-auto lg:mx-0">
              <div className="flex flex-col items-center">
                <span className="text-xs text-white/80">Pages vues</span>
                <span className="font-semibold text-lg">{kpis.pagesViews ?? 'â€”'}</span>
              </div>
              <div className="flex flex-col items-center">
                <span className="text-xs text-white/80">Temps moyen</span>
                <span className="font-semibold text-lg">{formatTime(kpis.avgSecondsOnActions)}</span>
              </div>
            </div>
          </div>

          {/* Right: Mini galerie + contrÃ´les */}
          <div className="w-full">
            <div className="rounded-2xl overflow-hidden shadow-xl bg-black/20 backdrop-blur-sm">
              <div className="relative w-full h-[360px] sm:h-[420px] lg:h-[480px]">
                <img
                  src={imgs[active]}
                  alt={alt}
                  loading="lazy"
                  className="w-full h-full object-cover object-center transition-opacity duration-700"
                />

                <div className="absolute left-4 bottom-4 bg-black/40 backdrop-blur-sm rounded-md px-3 py-1 text-sm text-white/90">
                  Photo : {active === 0 ? 'Collecte' : active === 1 ? 'Boutique' : 'Envoi'}
                </div>
              </div>

              <div className="mt-3 p-4 flex items-center justify-center gap-3">
                {imgs.map((src, i) => (
                  <button
                    key={i}
                    type="button"
                    onClick={() => setActive(i)}
                    aria-label={`Voir image ${i + 1}`}
                    className={`w-20 h-12 rounded-lg overflow-hidden focus:ring-2 focus:ring-[#D6453A] ${i === active ? 'ring-2 ring-[#D6453A]' : ''}`}
                  >
                    <img src={src} alt={`${alt} â€” vignette ${i + 1}`} className="w-full h-full object-cover object-center" />
                  </button>
                ))}
              </div>
            </div>

            <p className="mt-3 text-sm text-white/80 text-center">Galerie : collecte / boutique / envoi â€” images cyclÃ©es automatiquement.</p>
          </div>
        </div>
      </div>

      {/* Mobile floating primary CTA */}

    </section>
  );
}

Hero.propTypes = {
  images: PropTypes.arrayOf(PropTypes.string),
  alt: PropTypes.string,
  interval: PropTypes.number,
  kpis: PropTypes.shape({ pagesViews: PropTypes.oneOfType([PropTypes.number, PropTypes.string]), avgSecondsOnActions: PropTypes.number }),
  onPrimaryClick: PropTypes.func,
  onSecondaryClick: PropTypes.func,
  showMicroText: PropTypes.bool,
};

Hero.defaultProps = {
  images: [imgCollecte, imgBoutique, imgEnvoi],
  alt: 'Collecte / Boutique / Envoi â€” Frip2RÃªve',
  interval: 5000,
  kpis: { pagesViews: 0, avgSecondsOnActions: 0 },
  onPrimaryClick: null,
  onSecondaryClick: null,
  showMicroText: true,
};
import React from 'react';
import PropTypes from 'prop-types';

export default function PourquoiNousSoutenir({
  title = 'Pourquoi nous soutenir ?',
  subtitle = "Quelques chiffres qui montrent l'impact de nos actions depuis le dÃ©but.",
  stats = [
    {
      key: 'annees',
      number: 'Depuis 2016',
      value: '+6 ans d\'engagement',
      text: "Depuis 2016, l'association agit pour soutenir les familles en difficultÃ© en CÃ´te d'Ivoire.",
      icon: (
        <svg width="32" height="32" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden>
          <path d="M12 2v4" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
          <path d="M5 7h14" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
          <path d="M4 10h16v10H4z" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      ),
    },
    {
      key: 'familles',
      number: '120+',
      value: 'familles accompagnÃ©es',
      text: 'Soutien direct via dons matÃ©riels, vÃªtements, actions sociales et distribution locale.',
      icon: (
        <svg width="32" height="32" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden>
          <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4z" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
          <path d="M4 20c0-3.31 2.69-6 6-6h4c3.31 0 6 2.69 6 6" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      ),
    },
    {
      key: 'vetements',
      number: '1,5 t',
      value: 'de vÃªtements redistribuÃ©s',
      text: "CollectÃ©s en France, triÃ©s grÃ¢ce Ã  Frip2RÃªve, puis envoyÃ©s ou distribuÃ©s sur place.",
      icon: (
        <svg width="32" height="32" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden>
          <path d="M3 7h18" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
          <path d="M7 7v10a1 1 0 0 0 1 1h8a1 1 0 0 0 1-1V7" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      ),
    },
    {
      key: 'dons',
      number: '100%',
      value: 'des dons rÃ©utilisÃ©s',
      text: "Chaque don (vÃªtement ou financier) sert Ã  financer les actions de terrain.",
      icon: (
        <svg width="32" height="32" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden>
          <path d="M12 21s-6-4.5-6-9a6 6 0 1 1 12 0c0 4.5-6 9-6 9z" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
          <path d="M12 11.5v.01" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      ),
    },
  ],
}) {
  return (
    <section aria-labelledby="pourquoi-title" className="py-12 px-6 bg-white">
      <div className="max-w-6xl mx-auto text-center">
        <h2 id="pourquoi-title" className="text-2xl sm:text-3xl font-bold text-gray-900">{title}</h2>
        {subtitle && <p className="mt-2 text-sm text-gray-600">{subtitle}</p>}

        <div className="mt-8 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
          {stats.map((s) => (
            <div key={s.key} className="flex flex-col items-center bg-gray-50 border border-gray-100 rounded-lg p-6">
              <div className="w-12 h-12 flex items-center justify-center text-[#D6453A]">{s.icon}</div>
              <div className="mt-4 text-2xl sm:text-3xl font-extrabold text-gray-900">{s.number}</div>
              <div className="text-sm text-gray-700 mt-1 font-medium">{s.value}</div>
              <p className="mt-3 text-sm text-gray-600">{s.text}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}

PourquoiNousSoutenir.propTypes = {
  title: PropTypes.string,
  subtitle: PropTypes.string,
  stats: PropTypes.arrayOf(
    PropTypes.shape({
      key: PropTypes.string,
      number: PropTypes.string,
      value: PropTypes.string,
      text: PropTypes.string,
      icon: PropTypes.node,
    })
  ),
};
import React from 'react';
import PropTypes from 'prop-types';
// Remplacez par vos vraies images dans /assets
import imgCollecte from '../assets/association_2.jpg';
import imgPreparation from '../assets/association_5.jpg';
import imgSoutien from '../assets/association_6.jpg';

export default function ActionsRecent({
  title = 'Actions rÃ©centes',
  items = [
    {
      key: 'collecte-beauvais',
      image: imgCollecte,
      alt: 'Collecte de vÃªtements â€” Beauvais',
      title: 'Collecte de vÃªtements â€” Beauvais',
      text:
        "Plusieurs bÃ©nÃ©voles se sont mobilisÃ©s pour rÃ©colter des vÃªtements destinÃ©s aux familles en difficultÃ© et aux futures missions en CÃ´te dâ€™Ivoire.",
      href: '/actions/collecte-beauvais',
    },
    {
      key: 'preparation-colis',
      image: imgPreparation,
      alt: 'PrÃ©paration des colis solidaires',
      title: 'PrÃ©paration des colis solidaires',
      text:
        "Les dons sont triÃ©s, vÃ©rifiÃ©s et conditionnÃ©s avant l'envoi. Chaque colis soutient une famille ou un centre local partenaire.",
      href: '/actions/preparation-colis',
    },
    {
      key: 'soutien-cote-ivoire',
      image: imgSoutien,
      alt: "Soutien aux familles en CÃ´te d'Ivoire",
      title: "Soutien aux familles en CÃ´te d'Ivoire",
      text:
        "Une partie des vÃªtements fripâ€™ solidaires a Ã©tÃ© distribuÃ©e sur place, accompagnÃ©e d'actions sociales menÃ©es avec les Ã©quipes locales.",
      href: '/actions/soutien-cote-ivoire',
    },
  ],
  onLearnMore = null,
}) {
  return (
    <section aria-labelledby="actions-title" className="py-12 px-6 bg-gray-50">
      <div className="max-w-6xl mx-auto">
        <div className="text-center mb-6">
          <h2 id="actions-title" className="text-2xl sm:text-3xl font-bold text-gray-900">{title}</h2>
          <p className="mt-2 text-sm text-gray-600">Ce que nous faisons â€” rapide et concret.</p>
        </div>

        <div className="grid grid-cols-1 sm:grid-cols-3 gap-6">
          {items.map((it) => (
            <article key={it.key} className="bg-white rounded-lg shadow-sm overflow-hidden flex flex-col">
              <div className="h-44 sm:h-40 w-full overflow-hidden">
                <img
                  src={it.image}
                  alt={it.alt}
                  loading="lazy"
                  className="w-full h-full object-cover object-center"
                />
              </div>

              <div className="p-4 flex-1 flex flex-col">
                <h3 className="text-lg font-semibold text-gray-900">{it.title}</h3>
                <p className="mt-2 text-sm text-gray-700 flex-1">{it.text}</p>

                <div className="mt-4">
                  <button
                    type="button"
                    onClick={() => typeof onLearnMore === 'function' ? onLearnMore(it) : window.location.assign(it.href)}
                    className="inline-block rounded-2xl px-5 py-2 text-sm font-semibold shadow bg-[#D6453A] text-white"
                    aria-label={`En savoir sur ${it.title}`}
                  >
                    En savoir
                  </button>
                </div>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>
  );
}

ActionsRecent.propTypes = {
  title: PropTypes.string,
  items: PropTypes.arrayOf(
    PropTypes.shape({
      key: PropTypes.string,
      image: PropTypes.string,
      alt: PropTypes.string,
      title: PropTypes.string,
      text: PropTypes.string,
      href: PropTypes.string,
    })
  ),
  onLearnMore: PropTypes.func,
};

ActionsRecent.defaultProps = {
  title: 'Actions rÃ©centes',
  items: undefined,
  onLearnMore: null,
};
import React from 'react';
import PropTypes from 'prop-types';
// Remplacez par vos vraies images d'avatars si besoin
import avatar1 from '../assets/association_5.jpg';
import avatar2 from '../assets/association_4.jpg';
import avatar3 from '../assets/association_1.jpg';

export default function Temoignages({
  title = 'Ils nous ont fait confiance',
  subtitle = "Quelques retours de familles et partenaires que nous accompagnons.",
  testimonials = [
    {
      key: 't1',
      avatar: avatar1,
      alt: "Photo bÃ©nÃ©ficiaire Abidjan",
      quote:
        "GrÃ¢ce aux vÃªtements et fournitures reÃ§us, mes enfants ont pu reprendre l'Ã©cole plus sereinement. Merci pour votre aide et votre cÅ“ur.",
      author: 'Maman bÃ©nÃ©ficiaire â€” Abidjan',
    },
    {
      key: 't2',
      avatar: avatar2,
      alt: "Photo famille Yopougon",
      quote:
        "L'association nous a accompagnÃ©s avec respect et bienveillance. Les dons sont arrivÃ©s directement chez nous, sans aucun intermÃ©diaire.",
      author: 'Famille soutenue â€” Yopougon',
    },
    {
      key: 't3',
      avatar: avatar3,
      alt: "Photo partenaire local",
      quote:
        "RÃªve d'Ivoire agit avec sÃ©rieux et transparence. Leur soutien rÃ©gulier fait une rÃ©elle diffÃ©rence auprÃ¨s des familles que nous suivons.",
      author: 'Association Entraide CI â€” partenaire local',
    },
  ],
  asSlider = false,
}) {
  return (
    <section aria-labelledby="temoignages-title" className="py-12 px-6 bg-gray-50">
      <div className="max-w-6xl mx-auto text-center">
        <h2 id="temoignages-title" className="text-2xl sm:text-3xl font-bold text-gray-900">{title}</h2>
        {subtitle && <p className="mt-2 text-sm text-gray-600">{subtitle}</p>}

        <div className={`mt-8 ${asSlider ? 'swiper-container' : ''}`}>
          <div className={`grid ${asSlider ? '' : 'grid-cols-1 sm:grid-cols-3'} gap-6 items-start`}>
            {testimonials.map((t) => (
              <article key={t.key} className="bg-white rounded-lg p-6 shadow-sm text-left">
                <div className="flex items-start gap-4">
                  <div className="w-12 h-12 rounded-full overflow-hidden flex-shrink-0">
                    <img src={t.avatar} alt={t.alt} className="w-full h-full object-cover" />
                  </div>
                  <div>
                    <p className="text-gray-800">"{t.quote}"</p>
                    <div className="mt-3 text-sm text-gray-600 font-medium">{t.author}</div>
                  </div>
                </div>
              </article>
            ))}
          </div>
        </div>

        {/* Variante slider basique (si asSlider=true, vous pouvez intÃ©grer Swiper ou un slider JS) */}
        {asSlider && (
          <p className="mt-4 text-xs text-gray-500">Slider activÃ© : intÃ©grer une librairie JS type Swiper pour animation et pagination.</p>
        )}
      </div>
    </section>
  );
}

Temoignages.propTypes = {
  title: PropTypes.string,
  subtitle: PropTypes.string,
  testimonials: PropTypes.arrayOf(
    PropTypes.shape({
      key: PropTypes.string,
      avatar: PropTypes.string,
      alt: PropTypes.string,
      quote: PropTypes.string,
      author: PropTypes.string,
    })
  ),
  asSlider: PropTypes.bool,
};

Temoignages.defaultProps = {
  title: 'Ils nous ont fait confiance',
  subtitle: "Quelques retours de familles et partenaires que nous accompagnons.",
  testimonials: undefined,
  asSlider: false,
};

import React, { useState, useEffect } from 'react';
// en haut du fichier
import { NavLink } from 'react-router-dom';
import {
  Menu,
  X,
  ShoppingCart,
  Search,
  Facebook,
  Instagram,
  Mail,
  Heart,
  HandHeart
} from 'lucide-react';

/**
 * TopNav component
 * - Responsive accessible navigation for an association / site-vitrine
 * - Uses TailwindCSS utility classes (assumes Tailwind is configured)
 * - Colors follow the provided palette (arbitrary color classes used)
 *
 * Props (optional):
 * - announcement: string | null (text shown in announcement bar)
 * - showAnnouncement: boolean
 * - cartCount: number
 * - onSearch: (query) => void
 *
 * Notes:
 * - Primary CTA (Faire un don) is always visible on desktop and mobile (floating on mobile).
 * - Mobile menu is full-height panel.
 * - Accessibility: role="navigation", aria-label, skip-link, focus management.
 */

export default function TopNav({
  announcement = 'Prochaine collecte : 14 dÃ©cembre â€” DÃ©posez vos dons Ã  Frip2RÃªve',
  showAnnouncement = true,
  cartCount = 0,
  onSearch = null,
}) {
  const [mobileOpen, setMobileOpen] = useState(false);
  const [searchOpen, setSearchOpen] = useState(false);
  const [query, setQuery] = useState('');

  useEffect(() => {
    function onKey(e) {
      if (e.key === 'Escape') setMobileOpen(false);
    }
    document.addEventListener('keydown', onKey);
    // lock scroll when mobile menu open
    document.body.style.overflow = mobileOpen ? 'hidden' : '';
    return () => {
      document.removeEventListener('keydown', onKey);
      document.body.style.overflow = '';
    };
  }, [mobileOpen]);

  function handleSearchSubmit(e) {
    e.preventDefault();
    if (onSearch) onSearch(query);
    setSearchOpen(false);
  }

  return (
    <header className="w-full sticky top-0 z-40 bg-white shadow-sm">
      <a href="#main" className="sr-only focus:not-sr-only">
        Passer au contenu
      </a>

      {/* Announcement bar (optionnel) */}
      {showAnnouncement && announcement && (
        <div className="w-full bg-[#F5E7D3] text-[#222222] text-sm">
          <div className="max-w-7xl mx-auto px-4 py-2 flex items-center justify-center">
            <p className="truncate">{announcement}</p>
          </div>
        </div>
      )}

      <nav role="navigation" aria-label="Top Navigation" className="bg-white">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex items-center justify-between h-16">
            {/* Left: Logo */}
       <div className="flex items-center gap-4">
  <a
    href="/"
    className="flex items-center gap-3"
    aria-label="Accueil - Reves d'Ivoire"
  >
    {/* Logo rÃ©el */}
    <img
      src={require('../assets/logo.png')}
      alt="Logo RÃªves d'Ivoire"
      className="w-20 h-21 rounded-md object-cover"
    />

  </a>
</div>


            {/* Center: Main menu (hidden on mobile) */}
            <div className="hidden md:flex md:items-center md:space-x-6">
              <a href="/" className="text-sm font-medium text-[#222222] hover:underline">
                Accueil
              </a>
              <NavLink to="/a-propos" className="text-sm font-medium text-[#222222] hover:underline">
  Ã€ propos
</NavLink>
              <a href="/actions" className="text-sm font-medium text-[#222222] hover:underline">
                Actions &amp; Projets
              </a>
              <a href="/frip2reve" className="text-sm font-medium text-[#222222] hover:underline relative">
                Frip2RÃªve
              </a>
              <a href="/actualites" className="text-sm font-medium text-[#222222] hover:underline">
                ActualitÃ©s
              </a>
              <a href="/contact" className="text-sm font-medium text-[#222222] hover:underline">
                Contact
              </a>
            </div>

            {/* Right: Actions */}
            <div className="flex items-center gap-3">
              {/* Search (optional) */}
              <div className="hidden md:flex items-center">
                <button
                  aria-label="Rechercher"
                  onClick={() => setSearchOpen((s) => !s)}
                  className="p-2 rounded-md hover:bg-gray-100"
                >
                  <Search size={18} />
                </button>
              </div>

              {/* Social/contact small icons */}
              <div className="hidden md:flex items-center gap-2">
                <a href="https://www.facebook.com/revedivoire225" aria-label="Facebook" className="p-2 rounded-md hover:bg-gray-100">
                  <Facebook size={16} />
                </a>
                <a href="https://www.instagram.com/reve_divoire" aria-label="Instagram" className="p-2 rounded-md hover:bg-gray-100">
                  <Instagram size={16} />
                </a>
                <a href="mailto:contact@revesivoire.org" aria-label="Email" className="p-2 rounded-md hover:bg-gray-100">
                  <Mail size={16} />
                </a>
              </div>

              {/* Boutique / panier */}
              <a
                href="/boutique"
                className="relative p-2 rounded-md hover:bg-gray-100"
                aria-label="Voir la boutique"
              >
                <ShoppingCart size={18} />
                {cartCount > 0 && (
                  <span className="absolute -top-1 -right-1 text-xs px-1.5 py-0.5 rounded-full bg-[#7B2D2D] text-white">{cartCount}</span>
                )}
              </a>

              {/* Secondary CTA */}
              <a
                href="/benevole"
                className="hidden sm:inline-block text-sm font-medium px-3 py-2 rounded-md border border-gray-200"
                aria-label="Devenir bÃ©nÃ©vole"
              >
                Devenir bÃ©nÃ©vole
              </a>

              {/* Primary CTA */}
              <a
                href="/don"
                className="hidden md:inline-block text-sm font-semibold px-4 py-2 rounded-md shadow-sm text-white bg-[#7B2D2D] hover:brightness-95"
                aria-label="Faire un don"
              >
                Faire un don
              </a>

              {/* Mobile hamburger */}
              <button
                className="md:hidden p-2 rounded-md focus:outline-none"
                onClick={() => setMobileOpen(true)}
                aria-controls="mobile-menu"
                aria-expanded={mobileOpen}
                aria-label="Ouvrir le menu"
              >
                <Menu size={20} />
              </button>
            </div>
          </div>
        </div>

        {/* Search bar dropdown */}
        {searchOpen && (
          <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <form onSubmit={handleSearchSubmit} className="py-2">
              <label htmlFor="site-search" className="sr-only">Rechercher</label>
              <div className="flex">
                <input
                  id="site-search"
                  value={query}
                  onChange={(e) => setQuery(e.target.value)}
                  className="flex-1 border rounded-l-md px-3 py-2"
                  placeholder="Rechercher..."
                />
                <button type="submit" className="px-4 rounded-r-md border bg-gray-100">OK</button>
              </div>
            </form>
          </div>
        )}

        {/* Mobile panel (full-height) */}
        <div
          id="mobile-menu"
          className={`fixed inset-0 z-50 transform bg-white transition-transform duration-300 ${
            mobileOpen ? 'translate-x-0' : 'translate-x-full'
          } md:hidden`}
          role="dialog"
          aria-modal="true"
        >
          <div className="flex items-center justify-between px-4 py-3 border-b">
         <div className="flex items-center gap-4">
  <a
    href="/"
    className="flex items-center gap-3"
    aria-label="Accueil - Reves d'Ivoire"
  >
    {/* Logo rÃ©el */}
    <img
      src={require('../assets/logo.png')}
      alt="Logo RÃªves d'Ivoire"
      className="w-22 h-20 rounded-md object-cover"
    />

  </a>
</div>

            <div className="flex items-center gap-2">
              <a href="/boutique" className="p-2">
                <ShoppingCart size={18} />
              </a>
              <button aria-label="Fermer le menu" className="p-2" onClick={() => setMobileOpen(false)}>
                <X size={20} />
              </button>
            </div>
          </div>

          <div className="p-6 space-y-6">
            <a href="/" className="block text-lg font-medium">Accueil</a>
          <NavLink to="/a-propos" className="text-sm font-medium text-[#222222] hover:underline">
  Ã€ propos
</NavLink>  <a href="/actions" className="block text-lg font-medium">Actions &amp; Projets</a>
            <a href="/frip2reve" className="block text-lg font-medium">Frip2RÃªve</a>
            <a href="/actualites" className="block text-lg font-medium">ActualitÃ©s</a>
            <a href="/contact" className="block text-lg font-medium">Contact</a>

            <div className="pt-4 border-t">
              <a href="/benevole" className="block w-full text-center py-2 rounded-md border">Devenir bÃ©nÃ©vole</a>
              <a href="/don" className="mt-3 block w-full text-center py-2 rounded-md text-white bg-[#7B2D2D]">Faire un don</a>
            </div>

            <div className="pt-4 border-t flex items-center gap-3">
              <a href="#" aria-label="Facebook">
                <Facebook />
              </a>
              <a href="#" aria-label="Instagram">
                <Instagram />
              </a>
              <a href="mailto:contact@revesivoire.org" aria-label="Email">
                <Mail />
              </a>
            </div>
          </div>
        </div>

        {/* Floating mobile donate button (always visible on small screens) */}

<div className="fixed bottom-4 right-4 z-50 flex flex-col md:hidden gap-2">
  <a
    href="/don"
    className="inline-flex items-center gap-2 px-4 py-3 rounded-full text-white font-semibold shadow-lg bg-[#7B2D2D]"
    aria-label="Faire un don"
  >
    <Heart className="w-5 h-5" />
    Faire un don
  </a>

  <a
    href="/benevole"
    className="inline-flex items-center gap-2 px-4 py-3 rounded-full text-black font-semibold shadow-lg bg-white"
    aria-label="Devenir bÃ©nÃ©vole"
  >
    <HandHeart className="w-5 h-5" />
    Devenir bÃ©nÃ©vole
  </a>
</div>


      </nav>
    </header>
  );
}
import React from 'react';
import PropTypes from 'prop-types';

export default function ValeursThreePoints({
  title = 'Nos valeurs en 3 points',
  intro = "Ce qui guide chaque projet de l'association.",
  items = [
    {
      key: 'solidarite',
      title: 'SolidaritÃ©',
      text: 'Une association engagÃ©e pour soutenir les familles les plus vulnÃ©rables.',
      // icÃ´ne : mains qui se serrent
      icon: (
        <svg width="40" height="40" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden>
          <path d="M2 12c0 5 4 9 9 9s9-4 9-9" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
          <path d="M7 12l3-3 4 4 3-3" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      ),
    },
    {
      key: 'friperie',
      title: 'Friperie solidaire (Frip2RÃªve)',
      text: 'Les vÃªtements collectÃ©s financent nos actions sociales et nos missions terrain.',
      // icÃ´ne : t-shirt
      icon: (
        <svg width="40" height="40" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden>
          <path d="M4 7l2-2h3l2 2 2-2h3l2 2v11a1 1 0 0 1-1 1H5a1 1 0 0 1-1-1V7z" stroke="currentColor" strokeWidth="1.2" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      ),
    },
    {
      key: 'actions',
      title: 'Actions en CÃ´te dâ€™Ivoire',
      text: 'Livraison directe, soutien matÃ©riel et accompagnement des enfants et familles.',
      // icÃ´ne : gÃ©olocalisation
      icon: (
        <svg width="40" height="40" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden>
          <path d="M12 21s7-4.5 7-10a7 7 0 1 0-14 0c0 5.5 7 10 7 10z" stroke="currentColor" strokeWidth="1.2" strokeLinecap="round" strokeLinejoin="round" />
          <circle cx="12" cy="11" r="2.5" stroke="currentColor" strokeWidth="1.2" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      ),
    },
  ],
}) {
  return (
    <section aria-labelledby="valeurs-title" className="py-12 px-6 bg-white">
      <div className="max-w-6xl mx-auto">
        <div className="text-center">
          <h2 id="valeurs-title" className="text-2xl sm:text-3xl font-bold text-gray-900">{title}</h2>
          {intro && <p className="mt-2 text-sm text-gray-600">{intro}</p>}
        </div>

        <div className="mt-8 grid grid-cols-1 sm:grid-cols-3 gap-6">
          {items.map((it) => (
            <div key={it.key} className="flex flex-col items-center text-center px-4 py-6 border border-gray-100 rounded-lg shadow-sm bg-gray-50">
              <div className="w-12 h-12 flex items-center justify-center text-[#D6453A]">{it.icon}</div>
              <h3 className="mt-4 text-lg font-semibold text-gray-900">{it.title}</h3>
              <p className="mt-2 text-sm text-gray-700">{it.text}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}

ValeursThreePoints.propTypes = {
  title: PropTypes.string,
  intro: PropTypes.string,
  items: PropTypes.arrayOf(PropTypes.shape({
    key: PropTypes.string,
    title: PropTypes.string,
    text: PropTypes.string,
    icon: PropTypes.node,
  })),
};
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\components> cd..
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src> ls


    Répertoire : C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src


Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
d-----        13/12/2025     04:21                api
d-----        13/12/2025     01:37                assets
d-----        12/12/2025     20:18                components
d-----        13/12/2025     21:18                pages
-a----        11/12/2025     23:52             59 App.css
-a----        13/12/2025     21:21           1452 App.js
-a----        11/12/2025     23:38            246 App.test.js
-a----        11/12/2025     23:52             59 index.css
-a----        12/12/2025     18:09            416 index.js
-a----        11/12/2025     23:38           2632 logo.svg
-a----        11/12/2025     23:38            362 reportWebVitals.js
-a----        11/12/2025     23:38            241 setupTests.js


PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src> cd .\pages\
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages> ls


    Répertoire : C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages


Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
d-----        12/12/2025     18:22                About
d-----        12/12/2025     22:44                Actions
d-----        13/12/2025     03:45                actualites
d-----        13/12/2025     21:18                contact
d-----        13/12/2025     14:57                don
d-----        13/12/2025     00:52                Frip2Reve
-a----        12/12/2025     20:18            552 Home.js


PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages> cat .\Home.js
import Hero from '../components/Hero';
import Valeurs from '../components/Valeurs';
import Realisations from '../components/Realisations';
import Pourquoi from '../components/Pourquoi';
import Frip2Reve from '../components/Frip2Reve';
import Temoignages from '../components/Temoignages';
import Contact from '../components/Contact';

export default function Home() {
  return (
    <>
      <Hero />
      <Valeurs />
      <Realisations/>
      <Pourquoi />
      <Frip2Reve/>
      <Temoignages/>
      <Contact/>
    </>
  );
}
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages> cd .\About\
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages\About> cat *.js
import React from 'react';

import Hero from './sections/Hero';
import APropos from './sections/APropos';
import ActionsProjets from './sections/ActionsProjets';
import Frip2Reve from './sections/Frip2Reve';
import Actualites from './sections/Actualites';
import Don from './sections/Don'
import Contact from './sections/Contact';
import Vision from './sections/Vision';
import Histoire from './sections/Histoire';
import Equipe from './sections/Equipe';
import CeQueNousFaisons from './sections/CeQueNousFaisons';
import Impact from './sections/Impact';
import Transparence from './sections/Transparence';
import Partenaires from './sections/Partenaires';
import CommentVousPouvezAider from './sections/CommentVousPouvezAider';
import FAQ from './sections/FAQ';
export default function AboutPage() {
  return (
    <>
      <main id="main">
        <Hero />
        <Vision/>
        <Histoire/>
        <Equipe/>
        <CeQueNousFaisons/>
        <Impact />
        <Transparence />
        <Partenaires/>
        <CommentVousPouvezAider/>
        <FAQ/>
      </main>
    </>
  );
}
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages\About> cat .\sections\*.js
import React from "react";
import PropTypes from "prop-types";

/**
 * ActionsProjets - Section "Nos actions & projets"
 *
 * Objectif : prÃ©senter les actions concrÃ¨tes depuis 2016, montrer l'impact,
 * rassurer et orienter vers les CTA (don, bÃ©nÃ©volat, boutique / projets).
 *
 * Principes :
 *  - SÃ©mantique accessible (h2, article, figure, dl)
 *  - Grille responsive pour projets, bandeau de chiffres, tÃ©moignage bref
 *  - CTAs visibles et keyboard-focusables
 *  - Props pour personnaliser les projets, stats et tÃ©moignages
 *
 * Usage :
 * <ActionsProjets
 *   projects={[{title, description, image, alt, href}]}
 *   statsText="Depuis 2016 â€¢ 5 tonnes..."
 *   testimonials={[{quote, author, image, alt}]}
 *   onDonate={() => analytics.track('action_cta_don_click')}
 *   onVolunteer={() => analytics.track('action_cta_volunteer_click')}
 *   onProjects={() => analytics.track('action_cta_projects_click')}
 * />
 */

const noop = () => {};

export default function ActionsProjets({
  title = "Nos actions & projets",
  accroche = "Depuis 2016, RÃªve dâ€™Ivoire transforme les dons et le bÃ©nÃ©volat en actions concrÃ¨tes qui changent des vies.",
  projects = null,
  statsText = "Depuis 2016 â€¢ 5 tonnes de vÃªtements collectÃ©s â€¢ 200 familles aidÃ©es â€¢ 50 bÃ©nÃ©voles mobilisÃ©s chaque annÃ©e",
  testimonials = null,
  ctaDonateHref = "/faire-un-don",
  ctaVolunteerHref = "/devenir-benevole",
  ctaProjectsHref = "/projets",
  ctaDonateLabel = "Faire un don",
  ctaVolunteerLabel = "Devenir bÃ©nÃ©vole",
  ctaProjectsLabel = "Voir tous nos projets",
  imageAltDefault = "Volontaires RÃªve dâ€™Ivoire prÃ©parant des vÃªtements pour distribution locale et internationale.",
  rgpdLink = "/rgpd",
  showFiscalReceiptCopy = false,
  onDonate = noop,
  onVolunteer = noop,
  onProjects = noop,
}) {
  const defaultProjects = [
    {
      id: "collectes-locales",
      title: "Collectes locales de vÃªtements",
      description:
        "Organisation de points de collecte dans la rÃ©gion de Beauvais, tri et remise en Ã©tat pour distribution.",
      image: "/images/projects/collectes.jpg",
      alt: "BÃ©nÃ©voles triant des vÃªtements avant distribution.",
      href: "/projets/collectes-locales",
    },
    {
      id: "boutique-solidaire",
      title: "Boutique solidaire Frip2RÃªve",
      description:
        "Vente de vÃªtements et objets remis en Ã©tat ; les recettes financent nos actions humanitaires.",
      image: "/images/projects/boutique.jpg",
      alt: "IntÃ©rieur de la boutique Frip2RÃªve avec vÃªtements exposÃ©s.",
      href: "/boutique",
    },
    {
      id: "envois-humanitaires",
      title: "Envois humanitaires en CÃ´te dâ€™Ivoire",
      description:
        "Kits scolaires et vÃªtements envoyÃ©s chaque annÃ©e pour soutenir familles et Ã©coles locales.",
      image: "/images/projects/envois.jpg",
      alt: "PrÃ©paration de colis humanitaires pour l'envoi en CÃ´te dâ€™Ivoire.",
      href: "/projets/envois-humanitaires",
    },
    {
      id: "soutien-entrepreneurs",
      title: "Soutien aux entrepreneurs",
      description:
        "Accompagnement de projets culturels et artisanaux pour dÃ©velopper lâ€™Ã©conomie locale.",
      image: "/images/projects/entrepreneurs.jpg",
      alt: "Artisan prÃ©sentant ses crÃ©ations soutenu par RÃªve dâ€™Ivoire.",
      href: "/projets/soutien-entrepreneurs",
    },
  ];

  const items = Array.isArray(projects) && projects.length ? projects : defaultProjects;

  // Parse stats into small badges if possible
  const statBadges = React.useMemo(() => {
    if (!statsText) return [];
    return statsText.split("â€¢").map((s) => s.trim());
  }, [statsText]);

  const testimonialItem = testimonials && testimonials.length ? testimonials[0] : {
    quote: "GrÃ¢ce Ã  RÃªve dâ€™Ivoire, ma famille a reÃ§u tout le nÃ©cessaire pour la rentrÃ©e scolaire.",
    author: "â€” Fatou, bÃ©nÃ©ficiaire en CÃ´te dâ€™Ivoire",
    image: "/images/testimonials/fatou.jpg",
    alt: "Portrait de Fatou, bÃ©nÃ©ficiaire en CÃ´te dâ€™Ivoire.",
  };

  const handleCTA = (handler, analyticsName) => (e) => {
    try {
      handler(e);
    } catch (err) {
      // eslint-disable-next-line no-console
      console.warn("ActionsProjets: CTA handler error", err);
    }
  };

  return (
    <section aria-labelledby="actions-projets-heading" className="bg-white text-gray-900">
      <div className="max-w-6xl mx-auto px-6 py-12">
        <header className="mb-6">
          <h2 id="actions-projets-heading" className="text-2xl md:text-3xl font-semibold">
            {title}
          </h2>
          <p className="mt-3 text-sm md:text-base text-gray-700">{accroche}</p>
        </header>

        {/* Projets grid */}
        <div className="grid gap-6 lg:grid-cols-3">
          <div className="lg:col-span-2 grid gap-6 sm:grid-cols-2">
            {items.map((p) => (
              <article key={p.id || p.title} className="flex flex-col bg-gray-50 rounded-lg overflow-hidden border" aria-labelledby={`proj-${p.id}`}>
                <figure className="h-40 sm:h-44 md:h-48 w-full overflow-hidden bg-gray-100">
                  <img
                    src={p.image}
                    alt={p.alt || imageAltDefault}
                    className="w-full h-full object-cover object-center"
                    loading="lazy"
                    onError={(e) => {
                      e.currentTarget.style.display = "none";
                      const el = e.currentTarget.parentNode;
                      if (el) el.style.background = "linear-gradient(180deg,#f7f3ef,#efe7df)";
                    }}
                  />
                </figure>
                <div className="p-4 flex-1 flex flex-col">
                  <h3 id={`proj-${p.id}`} className="text-sm font-semibold text-gray-900">{p.title}</h3>
                  <p className="mt-2 text-sm text-gray-700 flex-1">{p.description}</p>
                  <div className="mt-4">
                    <a
                      href={p.href || ctaProjectsHref}
                      className="inline-flex items-center text-sm font-medium underline text-[#7B2D2D]"
                      aria-label={`En savoir plus sur ${p.title}`}
                      onClick={handleCTA(onProjects)}
                    >
                      En savoir plus
                    </a>
                  </div>
                </div>
              </article>
            ))}
          </div>

          {/* Right column: chiffres clÃ©s + tÃ©moignage + CTAs */}
          <aside className="space-y-6">
            {/* Chiffres clÃ©s bandeau */}
            <div className="rounded-lg overflow-hidden shadow-sm">
              <div className="bg-[#7B2D2D] text-white px-4 py-3 flex flex-wrap gap-2 items-center" role="status" aria-live="polite">
                {statBadges.map((s, i) => (
                  <span key={i} className="text-xs md:text-sm font-medium" title={s}>
                    {s}
                  </span>
                ))}
              </div>
            </div>

            {/* TÃ©moignage */}
            <blockquote className="bg-gray-50 border rounded-lg p-4 text-sm text-gray-800">
              <div className="flex items-start gap-3">
                <img src={testimonialItem.image} alt={testimonialItem.alt} className="w-12 h-12 rounded-full object-cover" loading="lazy" onError={(e)=>{e.currentTarget.style.display='none';}} />
                <div>
                  <p className="italic">â€œ{testimonialItem.quote}â€</p>
                  <cite className="mt-2 block text-xs text-gray-600">{testimonialItem.author}</cite>
                </div>
              </div>
            </blockquote>

            {/* CTAs */}
            <div className="rounded-lg bg-white border p-4">
              <div className="flex flex-col gap-3">
                <a
                  href={ctaDonateHref}
                  onClick={handleCTA(onDonate)}
                  className="inline-flex items-center justify-center px-4 py-2 rounded-md text-white text-sm font-medium focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2"
                  style={{ backgroundColor: "#7B2D2D" }}
                  aria-label={ctaDonateLabel}
                >
                  {ctaDonateLabel}
                </a>

                <a
                  href={ctaVolunteerHref}
                  onClick={handleCTA(onVolunteer)}
                  className="inline-flex items-center justify-center px-3 py-2 rounded-md text-sm font-medium border border-[#7B2D2D] text-[#7B2D2D] bg-white"
                  aria-label={ctaVolunteerLabel}
                >
                  {ctaVolunteerLabel}
                </a>

                <a
                  href={ctaProjectsHref}
                  onClick={handleCTA(onProjects)}
                  className="inline-flex items-center justify-center px-3 py-2 rounded-md text-sm font-medium text-gray-700 underline bg-transparent"
                  aria-label={ctaProjectsLabel}
                >
                  {ctaProjectsLabel}
                </a>

                <div className="mt-2 text-xs text-gray-600">
                  <a href={rgpdLink} className="underline">Politique RGPD</a>
                  <span className="mx-2">Â·</span>
                  {showFiscalReceiptCopy ? (
                    <span>ReÃ§u fiscal disponible sur demande.</span>
                  ) : (
                    <span>ReÃ§u fiscal : Ã  confirmer avec le comptable.</span>
                  )}
                </div>
              </div>
            </div>
          </aside>
        </div>
      </div>
    </section>
  );
}

ActionsProjets.propTypes = {
  title: PropTypes.string,
  accroche: PropTypes.string,
  projects: PropTypes.arrayOf(
    PropTypes.shape({
      id: PropTypes.string,
      title: PropTypes.string.isRequired,
      description: PropTypes.string.isRequired,
      image: PropTypes.string,
      alt: PropTypes.string,
      href: PropTypes.string,
    })
  ),
  statsText: PropTypes.string,
  testimonials: PropTypes.arrayOf(
    PropTypes.shape({ quote: PropTypes.string, author: PropTypes.string, image: PropTypes.string, alt: PropTypes.string })
  ),
  ctaDonateHref: PropTypes.string,
  ctaVolunteerHref: PropTypes.string,
  ctaProjectsHref: PropTypes.string,
  ctaDonateLabel: PropTypes.string,
  ctaVolunteerLabel: PropTypes.string,
  ctaProjectsLabel: PropTypes.string,
  imageAltDefault: PropTypes.string,
  rgpdLink: PropTypes.string,
  showFiscalReceiptCopy: PropTypes.bool,
  onDonate: PropTypes.func,
  onVolunteer: PropTypes.func,
  onProjects: PropTypes.func,
};
import React from "react";
import PropTypes from "prop-types";

/**
 * Actualites - Section "ActualitÃ©s & Ã‰vÃ©nements"
 *
 * Objectif : mettre en avant Ã©vÃ©nements passÃ©s et Ã  venir, galerie photo, et CTAs
 * pour engager (participer, donner, s'inscrire).
 *
 * Principes :
 *  - Titre H2 + accroche
 *  - Liste / mini-calendrier des Ã©vÃ©nements Ã  venir
 *  - Galerie lightbox pour photos d'Ã©vÃ©nements passÃ©s
 *  - CTA visibles et handlers pour tracking
 *  - AccessibilitÃ© : alt text, focus, aria labels
 *
 * Usage :
 * <Actualites
 *   events={[{id,title,date,location,description,image,alt,href}]}
 *   gallery={[{id,image,alt}]}
 *   statsText="X Ã©vÃ©nements â€¢ Y participants"
 *   onDonate={() => analytics.track('event_cta_don_click')}
 *   onParticipate={() => analytics.track('event_cta_participate_click')}
 * />
 */

const noop = () => {};

export default function Actualites({
  title = "ActualitÃ©s & Ã‰vÃ©nements",
  accroche = "Restez informÃ© des actions, collectes et Ã©vÃ©nements organisÃ©s par RÃªve dâ€™Ivoire en France et en CÃ´te dâ€™Ivoire.",
  events = null,
  gallery = null,
  statsText = "",
  ctaAllHref = "/actualites",
  ctaParticipateHref = "/evenements/inscription",
  ctaDonateHref = "/faire-un-don",
  ctaAllLabel = "Voir toutes les actualitÃ©s",
  ctaParticipateLabel = "Participer Ã  un Ã©vÃ©nement",
  ctaDonateLabel = "Faire un don",
  rgpdLink = "/rgpd",
  showFiscalReceiptCopy = false,
  onDonate = noop,
  onParticipate = noop,
  onAll = noop,
}) {
  // Defaults (si l'appelant ne fournit rien)
  const defaultEvents = [
    {
      id: "collecte-rentree-2025",
      title: "Collecte spÃ©ciale rentrÃ©e scolaire",
      date: "2025-09-15",
      dateLabel: "15 septembre 2025",
      location: "35 avenue Hector Berlioz, BruyÃ¨res-sur-Oise",
      description: "Points de collecte, tri et remise en Ã©tat pour les familles en besoin.",
      image: "/images/events/collecte-rentree-2025.jpg",
      alt: "BÃ©nÃ©voles triant des fournitures lors d'une collecte de rentrÃ©e.",
      href: "/actualites/collecte-rentree-2025",
    },
    {
      id: "vente-frip2reve-oct-2025",
      title: "Vente Frip2RÃªve en ligne",
      date: "2025-10-10",
      dateLabel: "10â€“12 octobre 2025",
      location: "Instagram / Boutique en ligne",
      description: "SÃ©lection d'articles solidaires vendus via Instagram et notre boutique en ligne.",
      image: "/images/events/vente-oct-2025.jpg",
      alt: "SÃ©lection d'articles Frip2RÃªve prÃªts pour la vente en ligne.",
      href: "/actualites/vente-frip2reve-oct-2025",
    },
    {
      id: "atelier-artistique-nov-2025",
      title: "Atelier artistique pour enfants",
      date: "2025-11-20",
      dateLabel: "20 novembre 2025",
      location: "Maison de quartier",
      description: "Atelier crÃ©atif pour enfants â€” inscriptions ouvertes.",
      image: "/images/events/atelier-nov-2025.jpg",
      alt: "Enfants participant Ã  un atelier artistique soutenu par RÃªve dâ€™Ivoire.",
      href: "/actualites/atelier-artistique-nov-2025",
    },
  ];

  const defaultGallery = [
    {
      id: "tri-distribution",
      image: "/images/gallery/tri-distribution.jpg",
      alt: "BÃ©nÃ©voles triant des vÃªtements lors dâ€™une collecte Ã  BruyÃ¨res-sur-Oise",
    },
    {
      id: "boutique-frip2reve",
      image: "/images/gallery/boutique-frip2reve.jpg",
      alt: "IntÃ©rieur de la boutique Frip2RÃªve avec clients et bÃ©nÃ©voles",
    },
    {
      id: "atelier-culturel",
      image: "/images/gallery/atelier-culturel.jpg",
      alt: "Atelier artistique pour enfants soutenu par RÃªve dâ€™Ivoire",
    },
  ];

  const items = Array.isArray(events) && events.length ? events : defaultEvents;
  const images = Array.isArray(gallery) && gallery.length ? gallery : defaultGallery;

  // Lightbox state simple
  const [lightboxIndex, setLightboxIndex] = React.useState(null);
  const openLightbox = (index) => setLightboxIndex(index);
  const closeLightbox = () => setLightboxIndex(null);

  // Partition upcoming vs past (simple date compare)
  const now = new Date();
  const withParsedDate = items.map((ev) => ({
    ...ev,
    _dateObj: ev.date ? new Date(ev.date) : null,
  }));

  const upcoming = withParsedDate.filter((e) => e._dateObj && e._dateObj >= now);
  const past = withParsedDate.filter((e) => !e._dateObj || e._dateObj < now);

  const handleCTA = (handler, name) => (e) => {
    try {
      handler(e);
    } catch (err) {
      // eslint-disable-next-line no-console
      console.warn(`Actualites: CTA ${name} handler error`, err);
    }
  };

  return (
    <section aria-labelledby="actualites-heading" className="bg-white text-gray-900">
      <div className="max-w-6xl mx-auto px-6 py-12">
        <header className="mb-6">
          <h2 id="actualites-heading" className="text-2xl md:text-3xl font-semibold">{title}</h2>
          <p className="mt-3 text-sm md:text-base text-gray-700">{accroche}</p>
        </header>

        <div className="grid gap-8 lg:grid-cols-3">
          {/* Left: calendrier / Ã©vÃ©nements */}
          <div className="lg:col-span-2">
            <div className="space-y-6">
              {/* Upcoming events */}
              {upcoming && upcoming.length ? (
                <div>
                  <h3 className="text-lg font-semibold">Ã‰vÃ©nements Ã  venir</h3>
                  <ul className="mt-4 grid gap-4 sm:grid-cols-2">
                    {upcoming.map((ev) => (
                      <li key={ev.id} className="p-4 bg-gray-50 rounded-lg border flex gap-4 items-start">
                        <time className="text-xs font-semibold text-[#7B2D2D] w-28 flex-shrink-0">{ev.dateLabel || (ev._dateObj ? ev._dateObj.toLocaleDateString() : ev.date)}</time>
                        <div>
                          <h4 className="text-sm font-semibold text-gray-900">{ev.title}</h4>
                          <p className="mt-1 text-sm text-gray-700">{ev.description}</p>
                          <div className="mt-3 flex items-center gap-3">
                            <a href={ev.href} className="text-sm underline text-[#7B2D2D]" onClick={handleCTA(onAll, 'event_cta_news_click')}>DÃ©tails</a>
                            <a href={ctaParticipateHref} className="inline-flex items-center px-3 py-1 rounded-md text-sm border border-[#7B2D2D] text-[#7B2D2D] bg-white" onClick={handleCTA(onParticipate, 'event_cta_participate_click')}>S'inscrire</a>
                          </div>
                        </div>
                      </li>
                    ))}
                  </ul>
                </div>
              ) : (
                <div>
                  <h3 className="text-lg font-semibold">Aucun Ã©vÃ©nement Ã  venir pour le moment</h3>
                  <p className="mt-2 text-sm text-gray-700">Suivez-nous sur Instagram ou inscrivez-vous Ã  la newsletter pour Ãªtre informÃ©.</p>
                </div>
              )}

              {/* Past events / gallery teaser */}
              {past && past.length ? (
                <div>
                  <h3 className="text-lg font-semibold">Ã‰vÃ©nements rÃ©cents</h3>
                  <ul className="mt-3 grid gap-3 sm:grid-cols-3">
                    {past.slice(0, 6).map((ev) => (
                      <li key={ev.id} className="bg-gray-50 rounded overflow-hidden border">
                        <figure className="h-28 w-full bg-gray-100 overflow-hidden">
                          <img src={ev.image} alt={ev.alt || ev.title} className="w-full h-full object-cover object-center" loading="lazy" onError={(e)=>{e.currentTarget.style.display='none';}} />
                        </figure>
                        <div className="p-2 text-xs text-gray-700">
                          <div className="font-medium">{ev.title}</div>
                          <div className="text-xs text-gray-600">{ev.dateLabel || (ev._dateObj ? ev._dateObj.toLocaleDateString() : ev.date)} â€¢ {ev.location}</div>
                        </div>
                      </li>
                    ))}
                  </ul>
                </div>
              ) : null}
            </div>
          </div>

          {/* Right: galerie + CTAs + stats */}
          <aside className="space-y-6">
            {/* Stats banner */}
            {statsText ? (
              <div className="rounded-lg overflow-hidden shadow-sm">
                <div className="bg-[#7B2D2D] text-white px-4 py-3 text-sm font-medium">{statsText}</div>
              </div>
            ) : null}

            {/* Gallery thumbnails */}
            <div>
              <h3 className="text-sm font-semibold mb-3">Galerie</h3>
              <div className="grid grid-cols-3 gap-2">
                {images.map((img, idx) => (
                  <button key={img.id} onClick={() => openLightbox(idx)} className="h-20 w-full overflow-hidden rounded bg-gray-100 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2" aria-label={`Ouvrir image ${img.alt}`}>
                    <img src={img.image} alt={img.alt} className="w-full h-full object-cover object-center" loading="lazy" onError={(e)=>{e.currentTarget.style.display='none';}} />
                  </button>
                ))}
              </div>
            </div>

            {/* CTAs */}
            <div className="rounded-lg bg-white border p-4">
              <div className="flex flex-col gap-3">
                <a href={ctaAllHref} onClick={handleCTA(onAll, 'event_cta_news_click')} className="inline-flex items-center justify-center px-4 py-2 rounded-md text-sm font-medium text-white" style={{ backgroundColor: '#7B2D2D' }} aria-label={ctaAllLabel}>{ctaAllLabel}</a>

                <a href={ctaParticipateHref} onClick={handleCTA(onParticipate, 'event_cta_participate_click')} className="inline-flex items-center justify-center px-3 py-2 rounded-md text-sm font-medium border border-[#7B2D2D] text-[#7B2D2D] bg-white" aria-label={ctaParticipateLabel}>{ctaParticipateLabel}</a>

                <a href={ctaDonateHref} onClick={handleCTA(onDonate, 'event_cta_don_click')} className="inline-flex items-center justify-center px-3 py-2 rounded-md text-sm font-medium text-gray-700 underline bg-transparent" aria-label={ctaDonateLabel}>{ctaDonateLabel}</a>

                <div className="mt-2 text-xs text-gray-600">
                  <a href={rgpdLink} className="underline">Politique RGPD</a>
                  <span className="mx-2">Â·</span>
                  {showFiscalReceiptCopy ? (
                    <span>ReÃ§u fiscal disponible sur demande.</span>
                  ) : (
                    <span>ReÃ§u fiscal : Ã  confirmer avec le comptable.</span>
                  )}
                </div>
              </div>
            </div>
          </aside>
        </div>

        {/* Lightbox modal */}
        {lightboxIndex !== null ? (
          <div className="fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-70" role="dialog" aria-modal="true">
            <div className="max-w-3xl w-full p-4">
              <div className="bg-white rounded-lg overflow-hidden">
                <div className="relative">
                  <button onClick={closeLightbox} className="absolute right-2 top-2 p-2 rounded bg-white text-gray-700 focus:outline-none focus-visible:ring-2">Fermer</button>
                  <img src={images[lightboxIndex].image} alt={images[lightboxIndex].alt} className="w-full h-auto object-contain" />
                </div>
                <div className="p-3 text-sm text-gray-700">{images[lightboxIndex].alt}</div>
              </div>
            </div>
          </div>
        ) : null}
      </div>
    </section>
  );
}

Actualites.propTypes = {
  title: PropTypes.string,
  accroche: PropTypes.string,
  events: PropTypes.arrayOf(
    PropTypes.shape({ id: PropTypes.string, title: PropTypes.string, date: PropTypes.string, dateLabel: PropTypes.string, location: PropTypes.string, description: PropTypes.string, image: PropTypes.string, alt: PropTypes.string, href: PropTypes.string })
  ),
  gallery: PropTypes.arrayOf(PropTypes.shape({ id: PropTypes.string, image: PropTypes.string, alt: PropTypes.string })),
  statsText: PropTypes.string,
  ctaAllHref: PropTypes.string,
  ctaParticipateHref: PropTypes.string,
  ctaDonateHref: PropTypes.string,
  ctaAllLabel: PropTypes.string,
  ctaParticipateLabel: PropTypes.string,
  ctaDonateLabel: PropTypes.string,
  rgpdLink: PropTypes.string,
  showFiscalReceiptCopy: PropTypes.bool,
  onDonate: PropTypes.func,
  onParticipate: PropTypes.func,
  onAll: PropTypes.func,
};
import React from "react";
import PropTypes from "prop-types";

/**
 * APropos - Section "Ã€ propos" (contenu informatif, non-HERO)
 *
 * Principes :
 *  - Section sÃ©mantique et accessible (heading, <dl> pour chiffres, figure pour image)
 *  - TonnalitÃ© informative / rassurante (mission, actions, preuve sociale)
 *  - CTAs modestes mais visibles (don / bÃ©nÃ©volat / projets)
 *
 * Usage : mÃªme props que l'ancien composant. N'hÃ©sitez pas Ã  passer `stats` (array)
 * pour afficher plusieurs mÃ©triques individuelles.
 */

export default function APropos({
  title = "Qui sommesâ€‘nous ?",
  accroche = "RÃªve dâ€™Ivoire agit depuis 2016 pour lutter contre la pauvretÃ© en France et en CÃ´te dâ€™Ivoire grÃ¢ce Ã  des collectes, une boutique solidaire et le soutien Ã  des projets locaux.",
  texte = "Nous mettons en relation des dons, des compÃ©tences et des ressources pour accompagner des familles et des micro-projets. Transparence, ancrage local et accompagnement durable sont au cÅ“ur de notre action.",
  image = "/images/apropos-fallback.jpg",
  alt = "BÃ©nÃ©voles RÃªve dâ€™Ivoire en action triant des vÃªtements pour distribution locale et internationale.",
  proofText = "Depuis 2016 â€¢ 5 tonnes collectÃ©es â€¢ 200 familles aidÃ©es",
  stats = null, // tableau optionnel [{label: 'Tonnes collectÃ©es', value: '5 t'}, ...]
  siren = "",
  rgpdLink = "/rgpd",
  ctaDonateHref = "/faire-un-don",
  ctaVolunteerHref = "/devenir-benevole",
  ctaProjectsHref = "/projets",
  ctaDonateLabel = "Faire un don",
  ctaVolunteerLabel = "Devenir bÃ©nÃ©vole",
  ctaProjectsLabel = "Voir nos projets",
  showFiscalReceiptCopy = false,
  onDonate = null,
  onVolunteer = null,
  onProjects = null,
}) {
  const handleClick = (handler) => (e) => {
    try {
      if (handler) handler(e);
    } catch (err) {
      // don't break UX on analytics error
      // eslint-disable-next-line no-console
      console.warn("APropos: handler error", err);
    }
  };

  // Si l'appelant n'a pas fourni de stats dÃ©taillÃ©es, on essaye de parser proofText
  const parsedStats = React.useMemo(() => {
    if (Array.isArray(stats) && stats.length) return stats;
    if (typeof proofText === "string") {
      return proofText.split("â€¢").map((s) => {
        const trimmed = s.trim();
        // heuristique simple : trouve la premiÃ¨re suite de chiffres
        const match = trimmed.match(/^(.+?)\s*:\s*(.+)$/) || [null, trimmed, ""]; // fallback
        return {
          label: match[1] ? match[1].trim() : trimmed,
          value: match[2] ? match[2].trim() : "",
        };
      });
    }
    return [];
  }, [stats, proofText]);

  return (
    <section
      aria-labelledby="apropos-heading"
      className="bg-white text-gray-900"
      role="region"
    >
      <div className="max-w-5xl mx-auto px-6 py-12">
        <div className="grid gap-8 lg:grid-cols-3 lg:items-start">

          {/* Texte principal */}
          <div className="lg:col-span-2">
            <h2 id="apropos-heading" className="text-2xl md:text-3xl font-semibold">
              {title}
            </h2>

            <p className="mt-4 text-base text-gray-700 leading-relaxed">
              <strong className="block text-gray-900">{accroche}</strong>
              <span className="mt-2 block">{texte}</span>
            </p>

            {/* Mission + Valeurs */}
            <div className="mt-6 grid gap-4 sm:grid-cols-2">
              <article className="p-4 bg-gray-50 rounded-lg border">
                <h3 className="text-sm font-semibold">Notre mission</h3>
                <p className="mt-2 text-sm text-gray-700">Accompagner durablement des familles et des initiatives locales grÃ¢ce au don, Ã  la formation et Ã  la mise en rÃ©seau.</p>
              </article>

              <article className="p-4 bg-gray-50 rounded-lg border">
                <h3 className="text-sm font-semibold">Nos valeurs</h3>
                <ul className="mt-2 text-sm text-gray-700 list-disc pl-5 space-y-1">
                  <li>Transparence financiÃ¨re</li>
                  <li>Ancrage local</li>
                  <li>Impact durable</li>
                </ul>
              </article>
            </div>

            {/* CTA row (discrÃ¨te) */}
            <div className="mt-6 flex flex-wrap gap-3">
              <a
                href={ctaDonateHref}
                onClick={handleClick(onDonate)}
                className="inline-flex items-center justify-center px-4 py-2 rounded-md text-white text-sm font-medium focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2"
                style={{ backgroundColor: "#7B2D2D" }}
                aria-label={ctaDonateLabel}
              >
                {ctaDonateLabel}
              </a>

              <a
                href={ctaVolunteerHref}
                onClick={handleClick(onVolunteer)}
                className="inline-flex items-center justify-center px-3 py-2 rounded-md text-sm font-medium border border-[#7B2D2D] text-[#7B2D2D] bg-white"
                aria-label={ctaVolunteerLabel}
              >
                {ctaVolunteerLabel}
              </a>

              <a
                href={ctaProjectsHref}
                onClick={handleClick(onProjects)}
                className="inline-flex items-center justify-center px-3 py-2 rounded-md text-sm font-medium text-gray-700 underline bg-transparent"
                aria-label={ctaProjectsLabel}
              >
                {ctaProjectsLabel}
              </a>
            </div>

            {/* Micro-copy lÃ©gal & preuve sociale */}
            <div className="mt-6 text-sm text-gray-600">
              <div className="flex flex-wrap items-center gap-3">
                {parsedStats && parsedStats.length ? (
                  <dl className="flex flex-wrap gap-2 items-center" aria-hidden>
                    {parsedStats.map((s, i) => (
                      <div key={i} className="inline-flex items-baseline gap-2 bg-gray-100 px-3 py-1 rounded-full text-sm">
                        <dt className="font-medium text-gray-800">{s.value || s.label}</dt>
                        {s.value ? <dd className="text-gray-600 ml-1">{s.label}</dd> : null}
                      </div>
                    ))}
                  </dl>
                ) : (
                  <span className="inline-block px-3 py-1 bg-gray-100 rounded-full text-gray-800">{proofText}</span>
                )}

                {siren ? (
                  <span className="text-sm">Â· SIREN {siren}</span>
                ) : null}
              </div>

              <div className="mt-3">
                <a href={rgpdLink} className="underline mr-2">Politique RGPD</a>
                <span className="mr-2">Â·</span>
                <span>Paiement sÃ©curisÃ© (Stripe / HTTPS)</span>
                {showFiscalReceiptCopy ? (
                  <span className="block mt-1">ReÃ§u fiscal disponible sur demande.</span>
                ) : (
                  <span className="block mt-1 text-amber-700">ReÃ§u fiscal : Ã  confirmer avec le comptable.</span>
                )}
              </div>
            </div>
          </div>

          {/* Image (figure) */}
          <figure className="w-full rounded-lg overflow-hidden border bg-gray-50">
            <img
              src={image}
              alt={alt}
              className="w-full h-56 object-cover object-center"
              loading="lazy"
              onError={(e) => {
                e.currentTarget.style.display = "none";
                const el = e.currentTarget.parentNode;
                if (el) el.style.background = "linear-gradient(180deg,#f7f3ef,#efe7df)";
              }}
            />
            <figcaption className="p-3 text-xs text-gray-600">{alt}</figcaption>
          </figure>
        </div>
      </div>
    </section>
  );
}

APropos.propTypes = {
  title: PropTypes.string,
  accroche: PropTypes.string,
  texte: PropTypes.string,
  image: PropTypes.string,
  alt: PropTypes.string,
  proofText: PropTypes.string,
  stats: PropTypes.arrayOf(
    PropTypes.shape({ label: PropTypes.string, value: PropTypes.string })
  ),
  siren: PropTypes.string,
  rgpdLink: PropTypes.string,
  ctaDonateHref: PropTypes.string,
  ctaVolunteerHref: PropTypes.string,
  ctaProjectsHref: PropTypes.string,
  ctaDonateLabel: PropTypes.string,
  ctaVolunteerLabel: PropTypes.string,
  ctaProjectsLabel: PropTypes.string,
  showFiscalReceiptCopy: PropTypes.bool,
  onDonate: PropTypes.func,
  onVolunteer: PropTypes.func,
  onProjects: PropTypes.func,
};
import React from 'react';
import PropTypes from 'prop-types';
import { Archive, Repeat, Tag, Truck, Heart } from 'lucide-react';

// CeQueNousFaisons.js
// Section "Ce que nous faisons â€” Actions concrÃ¨tes" â€” courte, lisible, pensÃ©e UI/UX
export default function CeQueNousFaisons({
  title = "Ce que nous faisons",
  subtitle = "Actions concrÃ¨tes au quotidien",
  actions = [
    {
      key: 'collectes',
      title: 'Collectes rÃ©guliÃ¨res',
      text: "Nous organisons des points de collecte en rÃ©gion pour rÃ©cupÃ©rer vÃªtements, chaussures et articles utiles auprÃ¨s de particuliers et partenaires locaux.",
      micro: "Donnez prÃ¨s de chez vous, on sâ€™occupe du reste.",
      Icon: Archive,
    },
    {
      key: 'tri',
      title: 'Tri & revalorisation',
      text: "Chaque article est triÃ©, nettoyÃ© et vÃ©rifiÃ©. Les piÃ¨ces rÃ©cupÃ©rables sont rÃ©parÃ©es ou revalorisÃ©es pour une seconde vie.",
      micro: "Rien ne se perd, tout est triÃ© avec soin.",
      Icon: Repeat,
    },
    {
      key: 'ventes',
      title: 'Ventes solidaires â€” Frip2RÃªve',
      text: "Les meilleures piÃ¨ces sont mises en vente Ã  petits prix afin de financer les actions sociales et les envois humanitaires.",
      micro: "Acheter solidaire, câ€™est aider directement.",
      Icon: Tag,
    },
    {
      key: 'envois',
      title: "Envois & distributions en CÃ´te d'Ivoire",
      text: "Les dons triÃ©s sont acheminÃ©s vers nos relais locaux, puis distribuÃ©s aux familles et communautÃ©s qui en ont le plus besoin.",
      micro: "Une aide qui arrive rÃ©ellement sur le terrain.",
      Icon: Truck,
    },
    {
      key: 'accompagnement',
      title: 'Accompagnement social ponctuel',
      text: "Nous intervenons auprÃ¨s de familles en difficultÃ©, en apportant soutien matÃ©riel ou mise en relation avec des partenaires sociaux.",
      micro: "Une aide humaine, pas seulement matÃ©rielle.",
      Icon: Heart,
    },
  ],
}) {
  return (
    <section aria-labelledby="actions-title" className="py-12 px-4 bg-white">
      <div className="max-w-6xl mx-auto">
        <header className="mb-6">
          <h2 id="actions-title" className="text-xl sm:text-2xl font-extrabold text-gray-900">{title}</h2>
          <p className="mt-2 text-sm text-gray-600">{subtitle}</p>
        </header>

        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
          {actions.map((a) => {
            const Icon = a.Icon || (() => null);
            return (
              <article key={a.key} className="flex flex-col p-4 bg-gray-50 rounded-xl border border-gray-100 shadow-sm hover:shadow-md transition-shadow" aria-labelledby={`action-${a.key}-title`}>
                <div className="flex items-start gap-3">
                  <div className="flex-shrink-0">
                    <div className="w-10 h-10 rounded-md bg-white flex items-center justify-center shadow">
                      <Icon aria-hidden="true" size={18} className="text-[#D6453A]" />
                    </div>
                  </div>

                  <div>
                    <h3 id={`action-${a.key}-title`} className="text-sm font-semibold text-gray-900">{a.title}</h3>
                    <p className="mt-1 text-xs text-gray-600">{a.text}</p>
                  </div>
                </div>

                <div className="mt-3 text-xs text-gray-500 italic">{a.micro}</div>
              </article>
            );
          })}
        </div>

       </div>
    </section>
  );
}

CeQueNousFaisons.propTypes = {
  title: PropTypes.string,
  subtitle: PropTypes.string,
  actions: PropTypes.arrayOf(
    PropTypes.shape({
      key: PropTypes.string.isRequired,
      title: PropTypes.string.isRequired,
      text: PropTypes.string.isRequired,
      micro: PropTypes.string,
      Icon: PropTypes.any,
    })
  ),
};
import React from 'react';
import PropTypes from 'prop-types';
import { Heart, Users, Megaphone } from 'lucide-react';

// CommentVousPouvezAider.js
// Section "Comment vous pouvez aider" â€” UI/UX claire, lisible en 5 secondes.

export default function CommentVousPouvezAider({
  title = "Comment vous pouvez aider",
  lead = "Chaque geste compte. Le vÃ´tre aussi.",
  ctaHref = '/don-contact',
  cards = [
    {
      key: 'donner',
      Icon: Heart,
      heading: 'Donnez ce que vous pouvez',
      desc: "VÃªtements propres, en bon Ã©tat, ou soutien financier.",
      micro: 'Votre don aide directement une famille.',
      cta: 'Je donne',
    },
    {
      key: 'benevole',
      Icon: Users,
      heading: 'Rejoignez nos bÃ©nÃ©voles',
      desc: 'Aide au tri, aux collectes, aux ventes solidaires.',
      micro: 'Quelques heures peuvent changer beaucoup.',
      cta: "Je rejoins",
    },
    {
      key: 'partager',
      Icon: Megaphone,
      heading: "Parlez-en autour de vous",
      desc: "Suivez-nous et partagez nos actions sur Instagram & Facebook.",
      micro: "Une publication = plus de familles soutenues.",
      cta: 'Je partage',
    },
  ],
  className = '',
}) {
  return (
    <section aria-labelledby="aide-title" className={`py-12 px-4 bg-white ${className}`}>
      <div className="max-w-6xl mx-auto">
        <header className="mb-6 text-center">
          <h2 id="aide-title" className="text-xl sm:text-2xl font-extrabold text-gray-900">{title}</h2>
          <p className="mt-1 text-sm text-gray-600">{lead}</p>
        </header>

        <div className="grid grid-cols-1 sm:grid-cols-3 gap-6">
          {cards.map((card) => {
            const Icon = card.Icon || (() => null);
            return (
              <a
                key={card.key}
                href={ctaHref}
                className="group block rounded-2xl border border-gray-100 bg-gray-50 p-6 shadow-sm hover:shadow-md transition-shadow focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[#D6453A]"
                aria-label={card.heading}
              >
                <div className="flex flex-col h-full">
                  <div className="flex items-center gap-4">
                    <div className="flex-shrink-0 w-12 h-12 rounded-lg bg-white flex items-center justify-center shadow">
                      <Icon size={20} aria-hidden="true" className="text-[#16A34A]" />
                    </div>
                    <div>
                      <div className="text-lg font-semibold text-gray-900">{card.heading}</div>
                      <div className="text-xs text-gray-600 mt-1">{card.desc}</div>
                    </div>
                  </div>

                  <div className="mt-auto flex items-center justify-between pt-4">
                    <div className="text-xs text-gray-500 italic">{card.micro}</div>
                    <div>
                      <span className="inline-flex items-center gap-2 text-sm font-semibold text-white bg-[#F97316] px-3 py-2 rounded-lg group-hover:bg-[#FB923C]">
                        {card.cta}
                        <svg xmlns="http://www.w3.org/2000/svg" className="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="2">
                          <path strokeLinecap="round" strokeLinejoin="round" d="M5 12h14M12 5l7 7-7 7" />
                        </svg>
                      </span>
                    </div>
                  </div>
                </div>
              </a>
            );
          })}
        </div>

      </div>
    </section>
  );
}

CommentVousPouvezAider.propTypes = {
  title: PropTypes.string,
  lead: PropTypes.string,
  ctaHref: PropTypes.string,
  cards: PropTypes.arrayOf(
    PropTypes.shape({
      key: PropTypes.string.isRequired,
      Icon: PropTypes.elementType,
      heading: PropTypes.string.isRequired,
      desc: PropTypes.string,
      micro: PropTypes.string,
      cta: PropTypes.string,
    })
  ),
  className: PropTypes.string,
};
import React from "react";
import PropTypes from "prop-types";

/**
 * ContactAcces - Section "Contact & AccÃ¨s"
 *
 * Objectifs : fournir les coordonnÃ©es, formulaire de contact, carte et liens
 * rÃ©seaux sociaux. Le composant expose des callbacks pour gÃ©rer l'envoi cÃ´tÃ©
 * serveur / CMS / email (onContactSubmit) et pour tracker les interactions.
 *
 * IMPORTANT : Ce composant n'envoie pas d'email seul â€” implÃ©mentez l'envoi
 * cÃ´tÃ© serveur via la callback onContactSubmit.
 *
 * Usage :
 * <ContactAcces
 *   address="35 avenue Hector Berlioz, BruyÃ¨res-sur-Oise"
 *   phone="0600000000"
 *   email="contact@frip2reve.com"
 *   mapsHref="https://www.google.com/maps/place/..."
 *   socials={{ instagram: 'https://instagram.com/..', facebook: 'https://facebook.com/..' }}
 *   onContactSubmit={(payload) => api.sendContact(payload)}
 *   onDonateClick={() => analytics.track('cta_don_click')}
 *   onVolunteerClick={() => analytics.track('cta_volunteer_click')}
 *   onSocialClick={(network) => analytics.track('social_click', {network})}
 * />
 */

export default function ContactAcces({
  title = "Contact & AccÃ¨s",
  accroche = "Nâ€™hÃ©sitez pas Ã  nous Ã©crire, nous rendre visite ou nous suivre sur nos rÃ©seaux sociaux pour rester connectÃ©s.",
  address = "35 avenue Hector Berlioz, BruyÃ¨res-sur-Oise",
  phone = "",
  email = "contact@frip2reve.com",
  mapsHref = "#",
  mapEmbedSrc = null, // iframe src (Google Maps embed). Si null, on montre un fallback image.
  mapAlt = "Localisation de RÃªve dâ€™Ivoire, BruyÃ¨res-sur-Oise",
  socials = { facebook: "", instagram: "", tiktok: "", youtube: "" },
  ctaDonateHref = "/faire-un-don",
  ctaVolunteerHref = "/devenir-benevole",
  ctaDonateLabel = "Faire un don",
  ctaVolunteerLabel = "Devenir bÃ©nÃ©vole",
  rgpdLink = "/rgpd",
  onContactSubmit = null, // async (payload) => Promise
  onDonateClick = null,
  onVolunteerClick = null,
  onSocialClick = null,
}) {
  const [name, setName] = React.useState("");
  const [emailField, setEmailField] = React.useState("");
  const [message, setMessage] = React.useState("");
  const [newsletter, setNewsletter] = React.useState(false);
  const [consent, setConsent] = React.useState(false); // RGPD for message processing
  const [submitting, setSubmitting] = React.useState(false);
  const [error, setError] = React.useState("");
  const [success, setSuccess] = React.useState("");

  const validate = () => {
    if (!name.trim()) return "Merci d'indiquer votre nom complet.";
    if (!emailField.trim() || !emailField.includes("@")) return "Merci d'indiquer un email valide.";
    if (!message.trim()) return "Merci d'indiquer un message.";
    if (!consent) return "Veuillez accepter la politique de confidentialitÃ© (RGPD).";
    return null;
  };

  const handleSubmit = async (e) => {
    e.preventDefault();
    setError("");
    setSuccess("");
    const v = validate();
    if (v) {
      setError(v);
      return;
    }

    const payload = {
      name: name.trim(),
      email: emailField.trim(),
      message: message.trim(),
      newsletter,
      date: new Date().toISOString(),
    };

    try {
      setSubmitting(true);
      if (!onContactSubmit) throw new Error("Aucun handler configurÃ© pour l'envoi du formulaire. ImplÃ©mentez onContactSubmit.");
      await onContactSubmit(payload);
      setSuccess("Merci â€” votre message a Ã©tÃ© envoyÃ©. Nous vous rÃ©pondrons sous 48h ouvrÃ©es.");
      // reset
      setName("");
      setEmailField("");
      setMessage("");
      setNewsletter(false);
      setConsent(false);
    } catch (err) {
      // eslint-disable-next-line no-console
      console.error("ContactAcces: submit error", err);
      setError(err?.message || "Erreur lors de l'envoi. RÃ©essayez plus tard.");
    } finally {
      setSubmitting(false);
    }
  };

  const handleSocialClick = (network) => (e) => {
    try {
      if (onSocialClick) onSocialClick(network);
    } catch (err) {
      // ignore
    }
  };

  const handleDonateClick = (e) => {
    try {
      if (onDonateClick) onDonateClick(e);
    } catch (err) {
      // ignore
    }
  };

  const handleVolunteerClick = (e) => {
    try {
      if (onVolunteerClick) onVolunteerClick(e);
    } catch (err) {
      // ignore
    }
  };

  return (
    <section aria-labelledby="contact-acces-heading" className="bg-white text-gray-900">
      <div className="max-w-6xl mx-auto px-6 py-12">
        <header className="mb-6">
          <h2 id="contact-acces-heading" className="text-2xl md:text-3xl font-semibold">{title}</h2>
          <p className="mt-3 text-sm md:text-base text-gray-700">{accroche}</p>
        </header>

        <div className="grid gap-8 lg:grid-cols-2">
          {/* Formulaire */}
          <div>
            <form onSubmit={handleSubmit} className="bg-gray-50 p-6 rounded-lg border" aria-describedby="contact-desc">
              <p id="contact-desc" className="sr-only">Formulaire de contact â€” nom, email, message et option newsletter.</p>

              <label className="block text-sm font-medium text-gray-700">Nom complet</label>
              <input type="text" value={name} onChange={(e) => setName(e.target.value)} className="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-2 focus:ring-offset-1" required />

              <label className="block text-sm font-medium text-gray-700 mt-4">Email</label>
              <input type="email" value={emailField} onChange={(e) => setEmailField(e.target.value)} className="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-2 focus:ring-offset-1" required />

              <label className="block text-sm font-medium text-gray-700 mt-4">Message</label>
              <textarea value={message} onChange={(e) => setMessage(e.target.value)} rows={5} className="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-2 focus:ring-offset-1" required />

              <div className="mt-4 flex items-center gap-3">
                <input id="newsletter" type="checkbox" checked={newsletter} onChange={(e) => setNewsletter(e.target.checked)} />
                <label htmlFor="newsletter" className="text-sm text-gray-700">Sâ€™inscrire Ã  la newsletter (optionnel)</label>
              </div>

              <div className="mt-3 flex items-start gap-2">
                <input id="consent" type="checkbox" checked={consent} onChange={(e) => setConsent(e.target.checked)} className="mt-1" />
                <label htmlFor="consent" className="text-sm text-gray-700">Jâ€™accepte la <a href={rgpdLink} className="underline">politique de confidentialitÃ©</a></label>
              </div>

              {error ? <div className="mt-3 text-sm text-red-600">{error}</div> : null}
              {success ? <div className="mt-3 text-sm text-green-700">{success}</div> : null}

              <div className="mt-6 flex gap-3">
                <button type="submit" disabled={submitting} className="inline-flex items-center px-4 py-2 rounded-md text-white text-sm font-medium focus:outline-none focus-visible:ring-2" style={{ backgroundColor: '#7B2D2D' }}>
                  {submitting ? 'Envoiâ€¦' : 'Envoyer le message'}
                </button>

                <a href={ctaDonateHref} onClick={handleDonateClick} className="inline-flex items-center px-3 py-2 rounded-md text-sm font-medium border border-[#7B2D2D] text-[#7B2D2D] bg-white">{ctaDonateLabel}</a>

                <a href={ctaVolunteerHref} onClick={handleVolunteerClick} className="inline-flex items-center px-3 py-2 rounded-md text-sm font-medium border border-[#7B2D2D] text-[#7B2D2D] bg-white">{ctaVolunteerLabel}</a>
              </div>

              <div className="mt-4 text-xs text-gray-600">
                <p>Nous traitons vos donnÃ©es conformÃ©ment Ã  notre <a href={rgpdLink} className="underline">politique de confidentialitÃ©</a>. Les informations transmises via ce formulaire sont utilisÃ©es uniquement pour rÃ©pondre Ã  votre demande.</p>
              </div>
            </form>

            {/* CoordonnÃ©es */}
            <div className="mt-6 bg-white border rounded-lg p-4">
              <h3 className="text-sm font-semibold">CoordonnÃ©es</h3>
              <address className="not-italic mt-2 text-sm text-gray-700">
                <div>{address}</div>
                {phone ? <div>TÃ©lÃ©phone : <a href={`tel:${phone}`} className="underline">{phone}</a></div> : <div>TÃ©lÃ©phone : Ã  renseigner</div>}
                <div>Email : <a href={`mailto:${email}`} className="underline">{email}</a></div>
              </address>
            </div>
          </div>

          {/* Carte et rÃ©seaux */}
          <aside>
            <div className="bg-gray-50 rounded-lg border overflow-hidden">
              {mapEmbedSrc ? (
                <iframe src={mapEmbedSrc} title="Carte â€” RÃªve dâ€™Ivoire" width="100%" height="320" loading="lazy" className="border-0" />
              ) : (
                <div className="w-full h-72 bg-gray-200 flex items-center justify-center">
                  <img src="/images/map-fallback.png" alt={mapAlt} className="max-w-full max-h-full object-contain" />
                </div>
              )}
            </div>

            <div className="mt-4 bg-white border rounded-lg p-4">
              <h3 className="text-sm font-semibold mb-3">Suivez-nous</h3>
              <div className="flex items-center gap-3">
                {socials.facebook ? (
                  <a href={socials.facebook} aria-label="Facebook" onClick={handleSocialClick('facebook')} className="inline-flex items-center justify-center w-10 h-10 rounded-full border">
                    <svg className="w-5 h-5" viewBox="0 0 24 24" fill="currentColor" aria-hidden>
                      <path d="M22 12a10 10 0 10-11.5 9.9v-7h-2.3V12h2.3V9.7c0-2.3 1.4-3.6 3.4-3.6.99 0 2.03.18 2.03.18v2.23h-1.14c-1.12 0-1.47.7-1.47 1.42V12h2.5l-.4 2.9h-2.1v7A10 10 0 0022 12z" />
                    </svg>
                  </a>
                ) : null}

                {socials.instagram ? (
                  <a href={socials.instagram} aria-label="Instagram" onClick={handleSocialClick('instagram')} className="inline-flex items-center justify-center w-10 h-10 rounded-full border">
                    <svg className="w-5 h-5" viewBox="0 0 24 24" fill="currentColor" aria-hidden>
                      <path d="M7 2h10a5 5 0 015 5v10a5 5 0 01-5 5H7a5 5 0 01-5-5V7a5 5 0 015-5zm5 6.5A4 4 0 1016 12a4 4 0 00-4-3.5zM18.5 6a1 1 0 11-2 0 1 1 0 012 0z" />
                    </svg>
                  </a>
                ) : null}

                {socials.tiktok ? (
                  <a href={socials.tiktok} aria-label="TikTok" onClick={handleSocialClick('tiktok')} className="inline-flex items-center justify-center w-10 h-10 rounded-full border">
                    <svg className="w-5 h-5" viewBox="0 0 24 24" fill="currentColor" aria-hidden>
                      <path d="M12 2v6a4 4 0 004 4h2v2a6 6 0 11-6-6V2h0z" />
                    </svg>
                  </a>
                ) : null}

                {socials.youtube ? (
                  <a href={socials.youtube} aria-label="YouTube" onClick={handleSocialClick('youtube')} className="inline-flex items-center justify-center w-10 h-10 rounded-full border">
                    <svg className="w-5 h-5" viewBox="0 0 24 24" fill="currentColor" aria-hidden>
                      <path d="M10 15l5-3-5-3v6zm11-6.5s-.1-1-.4-1.5c-.4-.9-1.5-1.6-3-1.6C14.1 5 12.1 5 12.1 5H12 12c0 0-2 .1-5.5.4C4.5 5.8 3.4 6.5 3 7.4 2.6 8 2.5 9 2.5 9S2 10.9 2 12v0c0 1.1.1 3 .5 3.6.4.9 1.5 1.6 3 1.6C7.9 17 10 17 10 17h0c0 0 2-.1 5.5-.4 2.5-.3 3.6-1 4-1.9.3-.7.4-1.8.4-1.8v-3.4z" />
                    </svg>
                  </a>
                ) : null}
              </div>
            </div>

            {/* Small CTA / microcopy */}
            <div className="mt-4 text-sm text-gray-600">
              <p>Vous pouvez aussi soutenir nos actions : <a href={ctaDonateHref} onClick={handleDonateClick} className="underline">faire un don</a> ou <a href={ctaVolunteerHref} onClick={handleVolunteerClick} className="underline">devenir bÃ©nÃ©vole</a>.</p>
            </div>
          </aside>
        </div>
      </div>
    </section>
  );
}

ContactAcces.propTypes = {
  title: PropTypes.string,
  accroche: PropTypes.string,
  address: PropTypes.string,
  phone: PropTypes.string,
  email: PropTypes.string,
  mapsHref: PropTypes.string,
  mapEmbedSrc: PropTypes.string,
  mapAlt: PropTypes.string,
  socials: PropTypes.object,
  ctaDonateHref: PropTypes.string,
  ctaVolunteerHref: PropTypes.string,
  ctaDonateLabel: PropTypes.string,
  ctaVolunteerLabel: PropTypes.string,
  rgpdLink: PropTypes.string,
  onContactSubmit: PropTypes.func,
  onDonateClick: PropTypes.func,
  onVolunteerClick: PropTypes.func,
  onSocialClick: PropTypes.func,
};
import React from "react";
import PropTypes from "prop-types";

/**
 * Don - Section "Soutenez RÃªve dâ€™Ivoire"
 *
 * Objectif : convertir l'intÃ©rÃªt en don ou engagement bÃ©nÃ©vole. Formulaire
 * simplifiÃ© cÃ´tÃ© client. IntÃ©grer votre backend / Stripe dans onSubmit.
 *
 * Usage :
 * <Don
 *   onDonateClick={() => analytics.track('don_cta_click')}
 *   onVolunteerClick={() => analytics.track('volunteer_cta_click')}
 *   onDonateSubmit={(formData) => api.donate(formData)}
 * />
 *
 * IMPORTANT : Ce composant ne rÃ©alise pas de paiement. ImplÃ©mentez l'intÃ©gration
 * cÃ´tÃ© serveur / Stripe dans la callback `onDonateSubmit`.
 */

export default function Don({
  title = "Soutenez RÃªve dâ€™Ivoire",
  accroche = "Chaque don ou engagement bÃ©nÃ©vole aide directement les familles et soutient nos projets en France et en CÃ´te dâ€™Ivoire.",
  image = "/images/don-benevoles.jpg",
  alt = "BÃ©nÃ©voles RÃªve dâ€™Ivoire aidant des familles locales et internationales.",
  ctaVolunteerHref = "/devenir-benevole",
  ctaVolunteerLabel = "Devenir bÃ©nÃ©vole",
  ctaDonateLabel = "Faire un don",
  rgpdLink = "/rgpd",
  showFiscalReceiptCopy = false,
  onDonateClick = null,
  onVolunteerClick = null,
  onDonateSubmit = null, // (formData) => Promise
}) {
  const [name, setName] = React.useState("");
  const [email, setEmail] = React.useState("");
  const [amount, setAmount] = React.useState(25);
  const [customAmount, setCustomAmount] = React.useState("");
  const [method, setMethod] = React.useState("card");
  const [rgpd, setRgpd] = React.useState(false);
  const [submitting, setSubmitting] = React.useState(false);
  const [error, setError] = React.useState("");
  const [success, setSuccess] = React.useState("");

  const predefined = [10, 25, 50, 100];

  const handleDonateClick = (e) => {
    try {
      if (onDonateClick) onDonateClick(e);
    } catch (err) {
      // don't block
      // eslint-disable-next-line no-console
      console.warn("Don: onDonateClick error", err);
    }
  };

  const handleVolunteerClick = (e) => {
    try {
      if (onVolunteerClick) onVolunteerClick(e);
    } catch (err) {
      // eslint-disable-next-line no-console
      console.warn("Don: onVolunteerClick error", err);
    }
  };

  const validate = () => {
    setError("");
    if (!name.trim()) return "Merci d'indiquer votre nom complet.";
    if (!email.trim() || !email.includes("@")) return "Merci d'indiquer un email valide.";
    const amt = customAmount ? Number(customAmount) : Number(amount);
    if (!amt || Number.isNaN(amt) || amt <= 0) return "Merci d'indiquer un montant valide.";
    if (!rgpd) return "Veuillez accepter la politique de confidentialitÃ© (RGPD).";
    return null;
  };

  const handleSubmit = async (e) => {
    e.preventDefault();
    setError("");
    setSuccess("");
    const v = validate();
    if (v) {
      setError(v);
      return;
    }

    const payload = {
      name: name.trim(),
      email: email.trim(),
      amount: customAmount ? Number(customAmount) : Number(amount),
      method,
      date: new Date().toISOString(),
    };

    // tracker
    try {
      if (onDonateClick) onDonateClick();
    } catch (err) {
      // ignore
    }

    if (!onDonateSubmit) {
      setError("Aucune action de paiement configurÃ©e. ImplÃ©mentez onDonateSubmit.");
      return;
    }

    try {
      setSubmitting(true);
      await onDonateSubmit(payload);
      setSuccess("Merci â€” votre don a bien Ã©tÃ© enregistrÃ©. Nous vous avons envoyÃ© un email de confirmation si vous avez fourni une adresse.");
      setName("");
      setEmail("");
      setAmount(25);
      setCustomAmount("");
      setRgpd(false);
    } catch (err) {
      // eslint-disable-next-line no-console
      console.error("Don: submit error", err);
      setError(err?.message || "Erreur lors du traitement du don. RÃ©essayez plus tard.");
    } finally {
      setSubmitting(false);
    }
  };

  return (
    <section aria-labelledby="don-heading" className="bg-white text-gray-900">
      <div className="max-w-6xl mx-auto px-6 py-12">
        <div className="grid gap-8 lg:grid-cols-2 lg:items-center">
          <div>
            <h2 id="don-heading" className="text-2xl md:text-3xl font-semibold">{title}</h2>
            <p className="mt-4 text-base text-gray-700">{accroche}</p>

            <div className="mt-6">
              <form onSubmit={handleSubmit} className="bg-gray-50 p-6 rounded-lg border" aria-describedby="don-form-desc">
                <p id="don-form-desc" className="sr-only">Formulaire simplifiÃ© pour faire un don sÃ©curisÃ©.</p>

                <label className="block text-sm font-medium text-gray-700">Nom complet</label>
                <input type="text" value={name} onChange={(e)=>setName(e.target.value)} className="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-2 focus:ring-offset-1" required />

                <label className="block text-sm font-medium text-gray-700 mt-4">Email</label>
                <input type="email" value={email} onChange={(e)=>setEmail(e.target.value)} className="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-2 focus:ring-offset-1" required />

                <div className="mt-4">
                  <div className="text-sm font-medium text-gray-700">Montant du don</div>
                  <div className="mt-2 flex gap-2 flex-wrap">
                    {predefined.map((p) => (
                      <button key={p} type="button" onClick={()=>{ setAmount(p); setCustomAmount(""); }} className={`px-3 py-1 rounded-md border ${amount === p && !customAmount ? 'bg-[#7B2D2D] text-white' : 'bg-white'}`}>{p}â‚¬</button>
                    ))}
                    <input type="number" min="1" placeholder="Montant personnalisÃ©" value={customAmount} onChange={(e)=>{ setCustomAmount(e.target.value); }} className="ml-2 px-2 py-1 rounded-md border w-36" aria-label="Montant personnalisÃ© en euros" />
                  </div>
                </div>

                <fieldset className="mt-4">
                  <legend className="text-sm font-medium text-gray-700">Moyen de paiement</legend>
                  <div className="mt-2 flex items-center gap-4">
                    <label className="inline-flex items-center">
                      <input type="radio" name="method" value="card" checked={method === 'card'} onChange={()=>setMethod('card')} className="mr-2" /> Carte
                    </label>
                    <label className="inline-flex items-center">
                      <input type="radio" name="method" value="virement" checked={method === 'virement'} onChange={()=>setMethod('virement')} className="mr-2" /> Virement
                    </label>
                  </div>
                </fieldset>

                <div className="mt-4 flex items-start gap-2">
                  <input id="rgpd" type="checkbox" checked={rgpd} onChange={(e)=>setRgpd(e.target.checked)} className="mt-1" />
                  <label htmlFor="rgpd" className="text-sm text-gray-700">Jâ€™accepte la <a href={rgpdLink} className="underline">politique de confidentialitÃ©</a></label>
                </div>

                {error ? <div className="mt-3 text-sm text-red-600">{error}</div> : null}
                {success ? <div className="mt-3 text-sm text-green-700">{success}</div> : null}

                <div className="mt-6 flex gap-3">
                  <button type="submit" onClick={handleDonateClick} disabled={submitting} className="inline-flex items-center px-4 py-2 rounded-md text-white text-sm font-medium focus:outline-none focus-visible:ring-2" style={{ backgroundColor: '#7B2D2D' }}>
                    {submitting ? 'Traitementâ€¦' : ctaDonateLabel}
                  </button>

                  <a href={ctaVolunteerHref} onClick={handleVolunteerClick} className="inline-flex items-center px-3 py-2 rounded-md text-sm font-medium border border-[#7B2D2D] text-[#7B2D2D] bg-white">{ctaVolunteerLabel}</a>
                </div>

                <div className="mt-4 text-xs text-gray-600">
                  <p>Les dons collectÃ©s financent la collecte et redistribution de vÃªtements, les envois humanitaires, ainsi que les projets culturels et entrepreneuriaux. Les paiements sont sÃ©curisÃ©s via Stripe ou virement bancaire.</p>
                  {showFiscalReceiptCopy ? (
                    <p>ReÃ§u fiscal disponible sur demande.</p>
                  ) : (
                    <p>ReÃ§u fiscal : Ã  confirmer avec le comptable.</p>
                  )}
                </div>
              </form>
            </div>
          </div>

          <figure className="w-full rounded-lg overflow-hidden border bg-gray-50">
            <img src={image} alt={alt} className="w-full h-72 object-cover object-center" loading="lazy" onError={(e)=>{e.currentTarget.style.display='none'; const el = e.currentTarget.parentNode; if (el) el.style.background = 'linear-gradient(180deg,#f7f3ef,#efe7df)';}} />
            <figcaption className="p-3 text-xs text-gray-600">{alt}</figcaption>
          </figure>
        </div>
      </div>
    </section>
  );
}

Don.propTypes = {
  title: PropTypes.string,
  accroche: PropTypes.string,
  image: PropTypes.string,
  alt: PropTypes.string,
  ctaVolunteerHref: PropTypes.string,
  ctaVolunteerLabel: PropTypes.string,
  ctaDonateLabel: PropTypes.string,
  rgpdLink: PropTypes.string,
  showFiscalReceiptCopy: PropTypes.bool,
  onDonateClick: PropTypes.func,
  onVolunteerClick: PropTypes.func,
  onDonateSubmit: PropTypes.func,
};
import React from 'react';
import PropTypes from 'prop-types';
import { Users, Truck, HeartHandshake, Zap } from 'lucide-react';

// Equipe.js
// Section "L'Ã©quipe" pour la page Ã€ propos â€” claire, accessible et prÃªte Ã  l'emploi.

export default function Equipe({
  title = "L'Ã©quipe",
  members = [
    {
      key: 'natacha',
      name: "Natacha Lokpo â€” Fondatrice",
      role: "Porteuse du projet et coordinatrice locale",
      description:
        "Responsable de la stratÃ©gie associative, du lien avec les bÃ©nÃ©ficiaires et des prises de dÃ©cision sur le terrain.",
      micro: "Natacha organise, coordonne et veille au respect des bÃ©nÃ©ficiaires.",
      imgAlt: "Natacha Lokpo â€” fondatrice, RÃªve d'Ivoire",
      imgSrc: null,
    },

    {
      key: 'bÃ©nÃ©voles',
      name: "Volontaires & partenaires locaux",
      role: "Equipe de tri, distribution et accompagnement sur place",
      description:
        "BÃ©nÃ©voles, associations partenaires et relais locaux â€” leur travail permet la continuitÃ© des collectes et la qualitÃ© des distributions.",
      micro: "Une Ã©quipe de terrain discrÃ¨te et essentielle.",
      imgAlt: "Volontaires RÃªve d'Ivoire â€” tri et distribution",
      imgSrc: null,
    },
  ],
  techPartner = {
    name: 'EDISYNCHRO',
    micro: "PropulsÃ© par EDISYNCHRO â€” site et communication.",
    url: 'https://edisynchro.com',
    imgAlt: "Logo EDISYNCHRO â€” partenaire technique",
    imgSrc: null,
  },
}) {
  return (
    <section aria-labelledby="equipe-title" className="py-12 px-4 bg-white">
      <div className="max-w-6xl mx-auto">
        <header className="mb-6">
          <h2 id="equipe-title" className="text-xl sm:text-2xl font-extrabold text-gray-900">{title}</h2>
        </header>

        <div className="grid grid-cols-1 sm:grid-cols-3 gap-6">
          {members.map((m) => (
            <article key={m.key} className="flex flex-col rounded-xl border border-gray-100 bg-gray-50 p-4 shadow-sm">
              <div className="flex items-center gap-3">
                <div className="w-14 h-14 rounded-lg bg-white flex items-center justify-center shadow">
                  {/* Placeholder icon if image not provided */}
                  {m.imgSrc ? (
                    <img src={m.imgSrc} alt={m.imgAlt} className="w-full h-full object-cover rounded-lg" />
                  ) : (
                    <Users size={18} aria-hidden="true" className="text-[#D6453A]" />
                  )}
                </div>

                <div>
                  <div className="text-sm font-semibold text-gray-900">{m.name}</div>
                  <div className="text-xs text-gray-600">{m.role}</div>
                </div>
              </div>

              <p className="mt-3 text-xs text-gray-700">{m.description}</p>
              <p className="mt-2 text-xs text-gray-500 italic">{m.micro}</p>

              {m.researchNote && (
                <p className="mt-3 text-[11px] text-gray-400">{m.researchNote}</p>
              )}
            </article>
          ))}
        </div>

        {/* Partenaire technique */}
        <div className="mt-6 flex items-center justify-between rounded-lg border border-gray-100 bg-white p-3 shadow-sm">
          <div className="flex items-center gap-3">
            <div className="w-10 h-10 rounded-lg bg-[#EFE4E2] flex items-center justify-center text-[#D6453A]">
              <Zap size={18} aria-hidden="true" />
            </div>
            <div>
              <div className="text-sm font-semibold text-gray-900">{techPartner.name}</div>
              <div className="text-xs text-gray-600">{techPartner.micro}</div>
            </div>
          </div>

          <div>
            <a href={techPartner.url} className="text-xs font-semibold text-[#D6453A] hover:underline" aria-label={`Voir ${techPartner.name}`}>
              Voir le partenaire
            </a>
          </div>
        </div>

        <p className="mt-4 text-xs text-gray-600">Alt image suggÃ©rÃ©s fournis pour chaque personne/Ã©lÃ©ment. Si vous avez des photos (ou logos) je peux les intÃ©grer et ajuster le layout (taille des vignettes, grille 2/3 colonnes, ou accordÃ©ons).</p>
      </div>
    </section>
  );
}

Equipe.propTypes = {
  title: PropTypes.string,
  members: PropTypes.arrayOf(
    PropTypes.shape({
      key: PropTypes.string.isRequired,
      name: PropTypes.string.isRequired,
      role: PropTypes.string,
      description: PropTypes.string,
      micro: PropTypes.string,
      imgAlt: PropTypes.string,
      imgSrc: PropTypes.string,
      researchNote: PropTypes.string,
    })
  ),
  techPartner: PropTypes.shape({
    name: PropTypes.string,
    micro: PropTypes.string,
    url: PropTypes.string,
    imgAlt: PropTypes.string,
    imgSrc: PropTypes.string,
  }),
};
import React, { useState } from 'react';
import PropTypes from 'prop-types';
import { ChevronDown } from 'lucide-react';

// FAQ.js
// Section "Questions frÃ©quentes" â€” UI/UX : accordion simple, 3 items, lisible en 20 secondes.

export default function FAQ({
  title = "Questions frÃ©quentes",
  lead = "Les rÃ©ponses aux questions qu'on nous pose le plus souvent.",
  items = [
    {
      key: 'deposer',
      q: 'OÃ¹ dÃ©poser un don ?',
      a: "Les points de collecte sont indiquÃ©s sur la page Contact / Boutique. Vous pouvez choisir le lieu le plus proche de vous.",
      micro: "Pensez Ã  nous prÃ©venir avant un dÃ©pÃ´t.",
    },
    {
      key: 'financier',
      q: 'Recevez-vous des dons financiers ?',
      a: "Oui. Les dons financiers peuvent Ãªtre faits par virement ou via un lien sÃ©curisÃ©. Un reÃ§u peut Ãªtre fourni lorsque c'est applicable.",
      micro: "Chaque euro est utilisÃ© pour nos actions terrain.",
    },
    {
      key: 'suivi',
      q: "Comment suivez-vous la destination des dons ?",
      a: "Chaque envoi est documentÃ© : photos, liste du contenu, confirmation de rÃ©ception. Toutes les preuves sont archivÃ©es pour la transparence.",
      micro: "Vous savez oÃ¹ part votre aide.",
    },
  ],
  className = '',
}) {
  const [open, setOpen] = useState(null);

  function toggle(key) {
    setOpen((prev) => (prev === key ? null : key));
  }

  return (
    <section aria-labelledby="faq-title" className={`py-12 px-4 bg-white ${className}`}>
      <div className="max-w-4xl mx-auto">
        <header className="mb-6 text-center">
          <h2 id="faq-title" className="text-xl sm:text-2xl font-extrabold text-gray-900">{title}</h2>
          <p className="mt-1 text-sm text-gray-600">{lead}</p>
        </header>

        <div className="space-y-3">
          {items.map((it) => (
            <div key={it.key} className="border border-gray-100 rounded-xl bg-gray-50 overflow-hidden">
              <button
                type="button"
                onClick={() => toggle(it.key)}
                className="w-full flex items-center justify-between p-4 text-left"
                aria-expanded={open === it.key}
                aria-controls={`faq-panel-${it.key}`}
              >
                <div>
                  <div className="text-sm font-semibold text-gray-900">{it.q}</div>
                </div>
                <ChevronDown className={`transform transition-transform ${open === it.key ? 'rotate-180' : ''}`} />
              </button>

              <div
                id={`faq-panel-${it.key}`}
                className={`px-4 pt-0 pb-4 text-sm text-gray-700 ${open === it.key ? 'block' : 'hidden'}`}
              >
                <div className="mt-2">{it.a}</div>
                <div className="mt-2 text-xs text-gray-500 italic">{it.micro}</div>
              </div>
            </div>
          ))}
        </div>

        <div className="mt-6 text-center">
          <a href="/contact" className="text-sm font-semibold text-[#D6453A] hover:underline">Contactez-nous</a>
        </div>
      </div>
    </section>
  );
}

FAQ.propTypes = {
  title: PropTypes.string,
  lead: PropTypes.string,
  items: PropTypes.arrayOf(
    PropTypes.shape({
      key: PropTypes.string.isRequired,
      q: PropTypes.string.isRequired,
      a: PropTypes.string.isRequired,
      micro: PropTypes.string,
    })
  ),
  className: PropTypes.string,
};
import React from "react";
import PropTypes from "prop-types";

/**
 * Frip2Reve - Section "Frip2RÃªve â€” Boutique solidaire"
 *
 * Objectif : mettre en avant la boutique solidaire (produits, horaires, lieu),
 * relier ventes â†’ financement des projets et orienter vers CTAs (shop / don / bÃ©nÃ©vole / Instagram).
 *
 * Principes :
 *  - SÃ©mantique accessible (h2, figure, list, address)
 *  - Grid responsive / carrousel lÃ©ger (si tu veux un vrai carrousel, intÃ©grer un composant dÃ©diÃ©)
 *  - Badges d'impact, micro-copy lÃ©gale (RGPD, reÃ§u fiscal conditionnel)
 *  - Handlers pour tracker Ã©vÃ¨nements : shop_cta_click, instagram_cta_click, don_cta_click
 *
 * Usage :
 * <Frip2Reve
 *   products={[{id, title, price, image, alt, href}]}
 *   hours="Du mardi au samedi, 10hâ€“18h"
 *   address="35 avenue Hector Berlioz, BruyÃ¨res-sur-Oise"
 *   mapsHref="https://maps.google.com/..."
 *   statsText="Chaque vente permet de financer X familles"
 *   onShop={() => analytics.track('shop_cta_click')}
 *   onInstagram={() => analytics.track('instagram_cta_click')}
 *   onDonate={() => analytics.track('don_cta_click')}
 * />
 */

const noop = () => {};

export default function Frip2Reve({
  title = "Frip2RÃªve â€” Boutique solidaire",
  accroche = "Achetez solidaire et soutenez les actions de RÃªve dâ€™Ivoire en France et en CÃ´te dâ€™Ivoire.",
  description = "Frip2RÃªve est notre boutique solidaire oÃ¹ chaque vÃªtement ou objet remis en Ã©tat finance directement nos projets humanitaires et culturels. Les ventes soutiennent les familles, financent les envois humanitaires et dÃ©veloppent des initiatives locales.",
  hours = "Ouvert du mardi au samedi, 10hâ€“18h",
  address = "35 avenue Hector Berlioz, BruyÃ¨res-sur-Oise",
  mapsHref = "#",
  products = null,
  productsTitle = "Produits Ã  la une",
  statsText = "Chaque vente permet de financer des projets locaux",
  ctaShopHref = "/boutique",
  ctaInstagramHref = "https://instagram.com/frip2reve",
  ctaDonateHref = "/faire-un-don",
  ctaVolunteerHref = "/devenir-benevole",
  ctaShopLabel = "Voir la boutique",
  ctaInstagramLabel = "Instagram Frip2RÃªve",
  ctaDonateLabel = "Faire un don",
  ctaVolunteerLabel = "Devenir bÃ©nÃ©vole",
  imageAltDefault = "IntÃ©rieur Frip2RÃªve, boutique solidaire de RÃªve dâ€™Ivoire.",
  rgpdLink = "/rgpd",
  showFiscalReceiptCopy = false,
  onShop = noop,
  onInstagram = noop,
  onDonate = noop,
  onVolunteer = noop,
}) {
  const defaultProducts = [
    {
      id: "veste-ethique",
      title: "Veste en denim recyclÃ©e",
      price: "25â‚¬",
      image: "/images/frip2reve/veste-denim.jpg",
      alt: "Veste en denim recyclÃ©e exposÃ©e dans la boutique Frip2RÃªve.",
      href: "/boutique/veste-denim",
    },
    {
      id: "robe-femme",
      title: "Robe vintage",
      price: "18â‚¬",
      image: "/images/frip2reve/robe-vintage.jpg",
      alt: "Robe vintage sur mannequin, Frip2RÃªve.",
      href: "/boutique/robe-vintage",
    },
    {
      id: "sac-artisanal",
      title: "Sac artisanal",
      price: "12â‚¬",
      image: "/images/frip2reve/sac-artisanal.jpg",
      alt: "Sac artisanal fabriquÃ© par un crÃ©ateur soutenu.",
      href: "/boutique/sac-artisanal",
    },
  ];

  const items = Array.isArray(products) && products.length ? products : defaultProducts;

  const handleClick = (handler, analyticsName) => (e) => {
    try {
      if (handler) handler(e);
    } catch (err) {
      // eslint-disable-next-line no-console
      console.warn(`Frip2Reve: handler ${analyticsName} error`, err);
    }
  };

  return (
    <section aria-labelledby="frip2reve-heading" className="bg-white text-gray-900">
      <div className="max-w-6xl mx-auto px-6 py-12">
        <header className="md:flex md:items-start md:justify-between md:gap-6">
          <div>
            <h2 id="frip2reve-heading" className="text-2xl md:text-3xl font-semibold">{title}</h2>
            <p className="mt-3 text-sm md:text-base text-gray-700">{accroche}</p>
          </div>

          <div className="mt-4 md:mt-0 flex gap-3">
            <a
              href={ctaShopHref}
              onClick={handleClick(onShop, 'shop_cta_click')}
              className="inline-flex items-center justify-center px-4 py-2 rounded-md text-white text-sm font-medium focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2"
              style={{ backgroundColor: '#7B2D2D' }}
              aria-label={ctaShopLabel}
            >
              {ctaShopLabel}
            </a>

            <a
              href={ctaInstagramHref}
              onClick={handleClick(onInstagram, 'instagram_cta_click')}
              className="inline-flex items-center justify-center px-3 py-2 rounded-md text-sm font-medium border border-[#7B2D2D] text-[#7B2D2D] bg-white"
              aria-label={ctaInstagramLabel}
            >
              {ctaInstagramLabel}
            </a>
          </div>
        </header>

        <div className="mt-8 grid gap-8 lg:grid-cols-3 lg:items-start">
          {/* Left: description + horaires + lieu + badge impact */}
          <div className="lg:col-span-1">
            <p className="text-gray-700">{description}</p>

            <div className="mt-6 space-y-4">
              <div className="flex items-start gap-3">
                <svg className="w-5 h-5 text-gray-600" viewBox="0 0 24 24" fill="none" aria-hidden>
                  <path d="M12 2v6" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"></path>
                  <path d="M21 12c0 4.97-4.03 9-9 9s-9-4.03-9-9 4.03-9 9-9v0" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"></path>
                </svg>
                <div>
                  <div className="text-sm font-semibold">Horaires</div>
                  <div className="text-sm text-gray-700">{hours}</div>
                </div>
              </div>

              <div className="flex items-start gap-3">
                <svg className="w-5 h-5 text-gray-600" viewBox="0 0 24 24" fill="none" aria-hidden>
                  <path d="M21 10c0 6-9 12-9 12s-9-6-9-12a9 9 0 1118 0z" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"></path>
                  <path d="M12 14a2 2 0 100-4 2 2 0 000 4z" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"></path>
                </svg>
                <div>
                  <div className="text-sm font-semibold">Lieu</div>
                  <address className="not-italic text-sm text-gray-700">
                    <a href={mapsHref} className="underline" aria-label={`Voir ${address} sur Google Maps`}>
                      {address}
                    </a>
                  </address>
                </div>
              </div>

              <div className="mt-4 inline-block bg-[#7B2D2D] text-white px-3 py-2 rounded-md text-sm font-medium">
                {statsText}
              </div>
            </div>

            {/* Legal micro-copy */}
            <div className="mt-6 text-sm text-gray-600">
              <a href={rgpdLink} className="underline">Politique RGPD</a>
              <span className="mx-2">Â·</span>
              {showFiscalReceiptCopy ? (
                <span>ReÃ§u fiscal disponible sur demande.</span>
              ) : (
                <span>ReÃ§u fiscal : Ã  confirmer avec le comptable.</span>
              )}
            </div>

            {/* Secondary CTAs */}
            <div className="mt-6 flex flex-col gap-3">
              <a
                href={ctaDonateHref}
                onClick={handleClick(onDonate, 'don_cta_click')}
                className="inline-flex items-center justify-center px-4 py-2 rounded-md text-white text-sm font-medium focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2"
                style={{ backgroundColor: '#7B2D2D' }}
                aria-label={ctaDonateLabel}
              >
                {ctaDonateLabel}
              </a>

              <a
                href={ctaVolunteerHref}
                onClick={handleClick(onVolunteer, 'volunteer_cta_click')}
                className="inline-flex items-center justify-center px-3 py-2 rounded-md text-sm font-medium border border-[#7B2D2D] text-[#7B2D2D] bg-white"
                aria-label={ctaVolunteerLabel}
              >
                {ctaVolunteerLabel}
              </a>
            </div>
          </div>

          {/* Right: produits mise en avant (mini-grid) */}
          <div className="lg:col-span-2">
            <h3 className="text-base font-semibold mb-4">{productsTitle}</h3>
            <ul className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3 list-none p-0 m-0">
              {items.map((prod) => (
                <li key={prod.id} className="bg-gray-50 rounded-lg border overflow-hidden flex flex-col">
                  <figure className="h-40 w-full bg-gray-100 overflow-hidden">
                    <img
                      src={prod.image}
                      alt={prod.alt || imageAltDefault}
                      className="w-full h-full object-cover object-center"
                      loading="lazy"
                      onError={(e) => { e.currentTarget.style.display = 'none'; const el = e.currentTarget.parentNode; if (el) el.style.background = 'linear-gradient(180deg,#f7f3ef,#efe7df)'; }}
                    />
                  </figure>

                  <div className="p-3 flex-1 flex flex-col">
                    <div className="flex items-baseline justify-between">
                      <h4 className="text-sm font-medium text-gray-900">{prod.title}</h4>
                      {prod.price ? <span className="text-sm text-gray-700">{prod.price}</span> : null}
                    </div>
                    <div className="mt-3 flex items-center justify-between">
                      <a href={prod.href || ctaShopHref} className="text-sm underline text-[#7B2D2D]" aria-label={`Voir ${prod.title}`} onClick={handleClick(onShop, 'shop_cta_click')}>
                        Voir
                      </a>
                      <a href={ctaShopHref} className="inline-flex items-center px-2 py-1 rounded text-sm bg-white border border-gray-200" aria-label={`Acheter ${prod.title}`} onClick={handleClick(onShop, 'shop_cta_click')}>
                        Acheter
                      </a>
                    </div>
                  </div>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </section>
  );
}

Frip2Reve.propTypes = {
  title: PropTypes.string,
  accroche: PropTypes.string,
  description: PropTypes.string,
  hours: PropTypes.string,
  address: PropTypes.string,
  mapsHref: PropTypes.string,
  products: PropTypes.arrayOf(
    PropTypes.shape({ id: PropTypes.string, title: PropTypes.string, price: PropTypes.string, image: PropTypes.string, alt: PropTypes.string, href: PropTypes.string })
  ),
  productsTitle: PropTypes.string,
  statsText: PropTypes.string,
  ctaShopHref: PropTypes.string,
  ctaInstagramHref: PropTypes.string,
  ctaDonateHref: PropTypes.string,
  ctaVolunteerHref: PropTypes.string,
  ctaShopLabel: PropTypes.string,
  ctaInstagramLabel: PropTypes.string,
  ctaDonateLabel: PropTypes.string,
  ctaVolunteerLabel: PropTypes.string,
  imageAltDefault: PropTypes.string,
  rgpdLink: PropTypes.string,
  showFiscalReceiptCopy: PropTypes.bool,
  onShop: PropTypes.func,
  onInstagram: PropTypes.func,
  onDonate: PropTypes.func,
  onVolunteer: PropTypes.func,
};
import React from 'react';
import PropTypes from 'prop-types';
import { Handshake, Shirt, MapPin, CheckCircle } from 'lucide-react';

export default function AProposSection1({
  surtitre = "Notre raison d'agir",
  title = 'Soutenir les familles et transformer chaque don en impact rÃ©el',
  intro = "RÃªve d'Ivoire mÃ¨ne des actions solidaires en France et en CÃ´te d'Ivoire : collecte, tri, redistribution de vÃªtements et matÃ©riel essentiel. Chaque geste compte, et chaque contribution aide une famille, un enfant, ou un projet local.",
  points = [
    {
      key: 'collecter',
      title: 'Collecter avec respect',
      text: "Nous rÃ©cupÃ©rons le matÃ©riel localement et veillons Ã  sa qualitÃ© : tout est triÃ© pour partir utile.",
      icon: Handshake,
    },
    {
      key: 'redistribuer',
      title: 'Redistribuer utile',
      text: "On fournit ce dont on a vraiment besoin : vÃªtements, fournitures scolaires et matÃ©riel adaptÃ©.",
      icon: Shirt,
    },
    {
      key: 'soutenir',
      title: 'Soutenir sur place',
      text: "Livraisons directes et actions avec nos partenaires locaux pour un accompagnement durable.",
      icon: MapPin,
    },
  ],
  reassurance = {
    title: 'Transparence totale',
    text: "100% traÃ§able : rapports simples, photos de terrain et suivi des actions pour que vous sachiez exactement l'impact.",
    icon: CheckCircle,
  },
  ctaLabel = 'Voir nos actions concrÃ¨tes â†’',
  ctaHref = '/actions',
  onCta = null,
}) {
  return (
    <section aria-labelledby="a-propos-1-title" className="py-16 px-6 bg-white">
      <div className="max-w-6xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-10 items-center">

        {/* Left column: texte principal */}
        <div className="space-y-6">
          <div className="text-sm font-semibold text-[#D6453A]">{surtitre}</div>
          <h1 id="a-propos-1-title" className="text-3xl sm:text-4xl font-extrabold text-gray-900 leading-tight">{title}</h1>
          <p className="text-gray-700 text-base">{intro}</p>

          <div className="mt-4">
            <a
              href={ctaHref}
              onClick={(e) => { if (typeof onCta === 'function') { e.preventDefault(); onCta(); } }}
              className="inline-flex items-center gap-2 text-sm font-semibold text-[#D6453A] hover:underline"
              aria-label="Voir nos actions"
            >
              {ctaLabel}
              <svg xmlns="http://www.w3.org/2000/svg" className="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.5">
                <path strokeLinecap="round" strokeLinejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6" />
              </svg>
            </a>
          </div>
        </div>

        {/* Right column: cartes points clÃ©s */}
        <div className="grid grid-cols-1 gap-4">
          <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
            {points.slice(0, 2).map((p) => (
              <div key={p.key} className="flex gap-4 p-4 bg-gray-50 rounded-xl border border-gray-100 shadow-sm hover:shadow-md transition-shadow">
                <div className="flex-shrink-0">
                  <div className="w-12 h-12 rounded-lg bg-white flex items-center justify-center shadow">
                    <p className="text-[#D6453A]"><p /></p>
                    <p className="sr-only">icÃ´ne</p>
                    <p className="sr-only" />
                    <p className="sr-only" />
                    <p className="sr-only" />
                    <p className="sr-only" />
                    <p className="sr-only" />
                    {/* lucide icon */}
                    <p className="sr-only" />
                  </div>
                </div>
                <div>
                  <h3 className="text-lg font-semibold text-gray-900">{p.title}</h3>
                  <p className="text-sm text-gray-600 mt-1">{p.text}</p>
                </div>
              </div>
            ))}
          </div>

          <div className="grid grid-cols-1 gap-4">
            {points.slice(2).map((p) => (
              <div key={p.key} className="flex gap-4 p-4 bg-gray-50 rounded-xl border border-gray-100 shadow-sm hover:shadow-md transition-shadow">
                <div className="flex-shrink-0">
                  <div className="w-12 h-12 rounded-lg bg-white flex items-center justify-center shadow">
                    {/* lucide icon */}
                  </div>
                </div>
                <div>
                  <h3 className="text-lg font-semibold text-gray-900">{p.title}</h3>
                  <p className="text-sm text-gray-600 mt-1">{p.text}</p>
                </div>
              </div>
            ))}

            {/* encart rÃ©assurance */}
            <div className="p-4 bg-white rounded-xl border border-gray-100 shadow-sm flex items-start gap-4">
              <div className="flex-shrink-0">
                <div className="w-12 h-12 rounded-full bg-[#EFE4E2] flex items-center justify-center text-[#D6453A]">
                  <CheckCircle size={22} />
                </div>
              </div>
              <div>
                <div className="font-semibold text-gray-900">{reassurance.title}</div>
                <div className="text-sm text-gray-600 mt-1">{reassurance.text}</div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}

AProposSection1.propTypes = {
  surtitre: PropTypes.string,
  title: PropTypes.string,
  intro: PropTypes.string,
  points: PropTypes.arrayOf(PropTypes.shape({ key: PropTypes.string, title: PropTypes.string, text: PropTypes.string })),
  reassurance: PropTypes.shape({ title: PropTypes.string, text: PropTypes.string }),
  ctaLabel: PropTypes.string,
  ctaHref: PropTypes.string,
  onCta: PropTypes.func,
};
import React from 'react';
import PropTypes from 'prop-types';

// Histoire.js
// Section "Histoire de l'association RÃªve d'Ivoire"
// Simple, claire et prÃªte Ã  intÃ©grer la page Ã€ propos

export default function Histoire({
  title = "Histoire de lâ€™association RÃªve dâ€™Ivoire",
  lead = "CrÃ©Ã©e en 2016, RÃªve dâ€™Ivoire accompagne les familles et mobilise la communautÃ© autour dâ€™actions solidaires liÃ©es Ã  la CÃ´te dâ€™Ivoire et Ã  la diaspora.",
  timeline = [
    {
      year: '2016',
      title: 'La crÃ©ation',
      text: "CrÃ©Ã©e en 2016, l'association RÃªve d'Ivoire voit le jour pour soutenir les familles en difficultÃ© et mener des actions solidaires tournÃ©es vers la CÃ´te d'Ivoire et la diaspora. DÃ¨s ses dÃ©buts, l'Ã©quipe s'engage dans la collecte de vÃªtements, le soutien social de proximitÃ© et l'organisation d'Ã©vÃ©nements communautaires.",
    },
    {
      year: '2017',
      title: 'DÃ©veloppement local',
      text: "Ã€ partir de 2017, l'association dÃ©veloppe ses premiÃ¨res actions locales : aide aux familles, Ã©vÃ©nements culturels afro-caribÃ©ens, et premiÃ¨res opÃ©rations de collecte et redistribution. Les rÃ©seaux sociaux deviennent rapidement un relais important pour mobiliser la communautÃ©.",
    },
    {
      year: '2017 - 2023',
      title: 'Consolidation',
      text: "Entre 2017 et 2023, RÃªve d'Ivoire continue d'avancer pas Ã  pas : collectes ponctuelles, accompagnement de personnes en difficultÃ© et soutien Ã  des projets Ã©ducatifs et humanitaires en CÃ´te d'Ivoire. La visibilitÃ© augmente grÃ¢ce Ã  des publications, partenariats locaux et initiatives communautaires.",
    },
    {
      year: '2024 - 2025',
      title: 'Nouvelle dynamique',
      text: "En 2024 et 2025, l'association accÃ©lÃ¨re la structuration : communication clarifiÃ©e, officialisation des actions et relance de projets pour toucher davantage de bÃ©nÃ©ficiaires. C'est aussi la pÃ©riode oÃ¹ l'idÃ©e de Frip2RÃªve â€” une friperie solidaire â€” prend forme pour financer les actions futures.",
    },
    {
      year: '2025',
      title: 'Relance et site vitrine',
      text: "En 2025, RÃªve d'Ivoire repart sur des bases claires avec la crÃ©ation d'un nouveau site vitrine destinÃ© Ã  mieux prÃ©senter missions, valeurs et projets. L'objectif : renforcer la transparence, faciliter les dons et offrir un espace stable pour suivre l'Ã©volution des actions.",
    },
  ],
}) {
  return (
    <section aria-labelledby="histoire-title" className="py-16 px-6 bg-white">
      <div className="max-w-5xl mx-auto">
        <header className="mb-8">
          <h2 id="histoire-title" className="text-2xl sm:text-3xl font-extrabold text-gray-900">{title}</h2>
          <p className="mt-3 text-gray-700 text-base max-w-3xl">{lead}</p>
        </header>

        <ol className="relative border-l border-gray-200 ml-2">
          {timeline.map((item, idx) => (
            <li key={item.year + idx} className="mb-10 ml-6">
              <span className="absolute -left-3 flex items-center justify-center w-6 h-6 rounded-full bg-[#D6453A] text-white text-xs font-semibold">{item.year}</span>
              <div className="flex flex-col sm:flex-row sm:items-start gap-2">
                <div className="sm:w-1/3">
                  <h3 className="text-sm font-semibold text-gray-900">{item.title}</h3>
                </div>
                <div className="sm:flex-1 text-sm text-gray-700">{item.text}</div>
              </div>
            </li>
          ))}
        </ol>

        <p className="mt-6 text-sm text-gray-600">Texte basÃ© sur les informations partagÃ©es et nos Ã©changes. Si vous souhaitez ajouter des photos, tÃ©moignages ou documents d'archive, je peux intÃ©grer un carrousel ou des liens de tÃ©lÃ©chargement.</p>
      </div>
    </section>
  );
}

Histoire.propTypes = {
  title: PropTypes.string,
  lead: PropTypes.string,
  timeline: PropTypes.arrayOf(
    PropTypes.shape({
      year: PropTypes.string.isRequired,
      title: PropTypes.string.isRequired,
      text: PropTypes.string.isRequired,
    })
  ),
};
import React from 'react';
import PropTypes from 'prop-types';
import { Clock, Users, Package } from 'lucide-react';

// ImpactShort.js
// Section "Impact â€” version courte" â€” conÃ§ue comme un brief UI/UX pour le designer.
// Objectif : preuve rapide et crÃ©dible (statistiques clÃ©s lisibles en 4â€“6 secondes).

export default function ImpactShort({
  items = [
    {
      key: 'since',
      number: '2016',
      label: 'AnnÃ©e de crÃ©ation',
      sub: '8 ans d\'actions solidaires',
      micro: 'Une aide qui dure dans le temps.',
      Icon: Clock,
    },
    {
      key: 'families',
      number: '120+',
      label: 'Familles accompagnÃ©es',
      sub: 'En France et en CÃ´te d\'Ivoire',
      micro: 'Un impact humain rÃ©el.',
      Icon: Users,
    },
    {
      key: 'weight',
      number: '1,5t',
      label: 'VÃªtements redistribuÃ©s',
      sub: 'CollectÃ©s, triÃ©s, transmis',
      micro: 'Rien ne reste dans un carton.',
      Icon: Package,
    },
  ],
  className = '',
}) {
  return (
    <section aria-labelledby="impact-short-title" className={`py-12 px-4 bg-white ${className}`}>
      <div className="max-w-6xl mx-auto">
        <header className="mb-6">
          <h2 id="impact-short-title" className="text-lg font-extrabold text-gray-900">Impact</h2>
          <p className="mt-1 text-sm text-gray-600">Chiffres clÃ©s â€” lisibles en 4 secondes.</p>
        </header>

        {/* 3 cartes alignÃ©es */}
        <div className="grid grid-cols-1 sm:grid-cols-3 gap-6">
          {items.map((it) => {
            const Icon = it.Icon || (() => null);
            return (
              <article
                key={it.key}
                className="flex flex-col items-start gap-3 p-6 bg-gray-50 rounded-2xl border border-gray-100 shadow-sm"
                aria-labelledby={`impact-${it.key}-title`}
              >
                <div className="flex items-center gap-3 w-full">
                  <div className="flex-shrink-0 w-12 h-12 rounded-lg bg-white flex items-center justify-center shadow">
                    <Icon size={20} aria-hidden="true" className="text-[#D6453A]" />
                  </div>

                  <div className="ml-2">
                    <div className="text-3xl sm:text-4xl font-extrabold leading-none text-gray-900">{it.number}</div>
                    <div id={`impact-${it.key}-title`} className="text-xs font-semibold text-gray-600 mt-1">{it.label}</div>
                  </div>
                </div>

                <div className="mt-2 text-sm text-gray-700">{it.sub}</div>
                <div className="mt-auto text-xs text-gray-500 italic">{it.micro}</div>
              </article>
            );
          })}
        </div>

        </div>
    </section>
  );
}

ImpactShort.propTypes = {
  items: PropTypes.arrayOf(
    PropTypes.shape({
      key: PropTypes.string.isRequired,
      number: PropTypes.string.isRequired,
      label: PropTypes.string,
      sub: PropTypes.string,
      micro: PropTypes.string,
      Icon: PropTypes.elementType,
    })
  ),
  className: PropTypes.string,
};
import React from 'react';
import PropTypes from 'prop-types';
import { Users, Globe, Zap } from 'lucide-react';

// Partenaires.js
// Section "Partenaires & remerciements" â€” UI/UX courte, chaleureuse et crÃ©dible.

export default function Partenaires({
  title = "Partenaires & remerciements",
  lead = "Rien ne se fait seul. Merci Ã  ceux qui rendent ce projet vivant.",
  items = [
    {
      key: 'benevoles',
      title: 'BÃ©nÃ©voles locaux',
      text: "Merci aux bÃ©nÃ©voles qui collectent, trient, prÃ©parent et soutiennent l'association au quotidien.",
      micro: 'Votre temps transforme des vies.',
      Icon: Users,
    },
    {
      key: 'partenaires_ci',
      title: 'Partenaires associatifs en CÃ´te d\'Ivoire',
      text: "Merci aux relais locaux qui assurent les distributions sur le terrain et l'accompagnement des familles.",
      micro: 'Une chaÃ®ne solidaire, des deux cÃ´tÃ©s de la mer.',
      Icon: Globe,
    },
    {
      key: 'edisynchro',
      title: 'Partenaire technique â€” EDISYNCHRO',
      text: "Merci Ã  EDISYNCHRO pour l'accompagnement digital et le support technique du projet.",
      micro: 'Un appui pro pour aller plus loin.',
      Icon: Zap,
      logo: null, // optional url to logo image
      url: 'https://edisynchro.com',
    },
  ],
  className = '',
}) {
  return (
    <section aria-labelledby="partenaires-title" className={`py-12 px-4 bg-white ${className}`}>
      <div className="max-w-6xl mx-auto">
        <header className="mb-6">
          <h2 id="partenaires-title" className="text-xl sm:text-2xl font-extrabold text-gray-900">{title}</h2>
          <p className="mt-1 text-sm text-gray-600">{lead}</p>
        </header>

        <div className="grid grid-cols-1 sm:grid-cols-3 gap-4">
          {items.slice(0,3).map((it) => {
            const Icon = it.Icon || (() => null);
            return (
              <article key={it.key} className="flex gap-3 items-start p-4 bg-gray-50 rounded-xl border border-gray-100 shadow-sm">
                <div className="flex-shrink-0 w-12 h-12 rounded-lg bg-white flex items-center justify-center shadow">
                  {it.logo ? (
                    <img src={it.logo} alt={`${it.title} logo`} className="w-10 h-10 object-contain" />
                  ) : (
                    <Icon size={18} aria-hidden="true" className="text-[#D6453A]" />
                  )}
                </div>

                <div className="flex-1">
                  <div className="text-sm font-semibold text-gray-900">{it.title}</div>
                  <div className="mt-1 text-xs text-gray-600">{it.text}</div>
                  <div className="mt-2 text-xs text-gray-500 italic">{it.micro}</div>
                </div>

                {it.url && (
                  <div className="flex-shrink-0">
                    <a href={it.url} className="text-xs font-semibold text-[#D6453A] hover:underline" aria-label={`Voir ${it.title}`}>
                      Voir
                    </a>
                  </div>
                )}
              </article>
            );
          })}
        </div>

      </div>
    </section>
  );
}

Partenaires.propTypes = {
  title: PropTypes.string,
  lead: PropTypes.string,
  items: PropTypes.arrayOf(
    PropTypes.shape({
      key: PropTypes.string.isRequired,
      title: PropTypes.string.isRequired,
      text: PropTypes.string,
      micro: PropTypes.string,
      Icon: PropTypes.elementType,
      logo: PropTypes.string,
      url: PropTypes.string,
    })
  ),
  className: PropTypes.string,
};
import React from 'react';
import PropTypes from 'prop-types';
import { CheckCircle, FileText, Award } from 'lucide-react';

// Transparence.js
// Section "Transparence & preuves" â€” UI/UX : bloc clair, rassurant et facile Ã  scanner.

export default function Transparence({
  year = '2016',
  title = 'Transparence & preuves',
  lead = "Rien n'est cachÃ©, tout est accessible.",
  adminContact = 'contact@revedivoire.fr',
  docsNote = "Statuts, rÃ©cÃ©pissÃ©s prÃ©fectoraux, bilans simplifiÃ©s et rapports d'actions.",
  className = '',
}) {
  return (
    <section aria-labelledby="transparence-title" className={`py-12 px-4 bg-white ${className}`}>
      <div className="max-w-6xl mx-auto">
        <div className="p-6 rounded-2xl border border-gray-100 bg-gray-50 shadow-sm flex flex-col sm:flex-row items-stretch gap-4">

          {/* Preuve visuelle â€” grande annÃ©e */}
          <div className="flex-shrink-0 flex items-center justify-center w-full sm:w-40">
            <div className="text-center">
              <div className="text-4xl sm:text-5xl font-extrabold text-gray-900">{year}</div>
              <div className="text-xs text-gray-500 mt-1">AnnÃ©e de crÃ©ation</div>
            </div>
          </div>

          {/* Contenu principal â€” institutionnel doux */}
          <div className="flex-1 flex flex-col justify-between gap-3">
            <div>
              <div className="flex items-center gap-3">
                <div className="w-10 h-10 rounded-md bg-white flex items-center justify-center shadow">
                  <CheckCircle size={18} aria-hidden="true" className="text-[#D6453A]" />
                </div>
                <div>
                  <h3 id="transparence-title" className="text-lg font-semibold text-gray-900">{title}</h3>
                  <p className="text-sm text-gray-600">{lead}</p>
                </div>
              </div>

              <div className="mt-4 grid grid-cols-1 sm:grid-cols-2 gap-3">
                <div className="flex items-start gap-3">
                  <div className="flex-shrink-0 w-8 h-8 rounded-md bg-white flex items-center justify-center shadow">
                    <Award size={16} aria-hidden="true" className="text-[#D6453A]" />
                  </div>
                  <div>
                    <div className="text-sm font-semibold text-gray-900">Association dÃ©clarÃ©e</div>
                    <div className="text-xs text-gray-600">Dossiers administratifs disponibles sur simple demande.</div>
                  </div>
                </div>

                <div className="flex items-start gap-3">
                  <div className="flex-shrink-0 w-8 h-8 rounded-md bg-white flex items-center justify-center shadow">
                    <FileText size={16} aria-hidden="true" className="text-[#D6453A]" />
                  </div>
                  <div>
                    <div className="text-sm font-semibold text-gray-900">Documents conservÃ©s et organisÃ©s</div>
                    <div className="text-xs text-gray-600">{docsNote}</div>
                  </div>
                </div>
              </div>
            </div>

            <div className="mt-3">
              <div className="text-sm text-gray-700">Contact dÃ©diÃ© pour toute demande administrative</div>
              <a href={`mailto:${adminContact}`} className="mt-1 inline-block text-sm font-semibold text-[#D6453A] hover:underline">{adminContact}</a>
              <div className="mt-2 text-xs text-gray-500">Nous vous rÃ©pondons rapidement et clairement.</div>
            </div>
          </div>
        </div>

           </div>
    </section>
  );
}

Transparence.propTypes = {
  year: PropTypes.string,
  title: PropTypes.string,
  lead: PropTypes.string,
  adminContact: PropTypes.string,
  docsNote: PropTypes.string,
  className: PropTypes.string,
};
import React from 'react';
import PropTypes from 'prop-types';
import { Handshake, Eye, Repeat, MapPin } from 'lucide-react';

// Section: Notre vision & nos valeurs
// Objectif: prÃ©senter identitÃ©, valeurs et motivation â€” micro-copy UX claire et concise
export default function Vision({
  surtitre = "Ce qui nous motive au quotidien",
  title = "Une vision simple : aider avec humanitÃ©, clartÃ© et impact",
  intro = "Notre action repose sur des valeurs fortes qui orientent chaque collecte, chaque redistribution et chaque projet menÃ© pour les familles et les enfants. Ces valeurs sont le socle de notre engagement depuis le premier jour.",
  values = [
    {
      key: 'solidarite',
      title: 'SolidaritÃ©',
      text: "Agir avec respect, dignitÃ© et bienveillance, pour que chaque aide soit rÃ©ellement utile.",
      Icon: Handshake,
      note: "Aide adaptÃ©e â€” toujours penser Ã  la dignitÃ© des bÃ©nÃ©ficiaires.",
    },
    {
      key: 'transparence',
      title: 'Transparence',
      text: "Chaque don est tracÃ©, expliquÃ© et montrÃ©. Vous savez comment il est utilisÃ© et qui il aide.",
      Icon: Eye,
      note: "Rapports clairs, photos de terrain et suivi projet par projet.",
    },
    {
      key: 'durabilite',
      title: 'DurabilitÃ©',
      text: "Donner une seconde vie aux vÃªtements, Ã©viter le gaspillage et soutenir une Ã©conomie solidaire.",
      Icon: Repeat,
      note: "RÃ©parer, redistribuer, rÃ©employer : rÃ©duire le gaspillage durablement.",
    },
    {
      key: 'proximite',
      title: 'ProximitÃ©',
      text: "ÃŠtre prÃ©sent sur le terrain : actions locales en France et interventions directes en CÃ´te d'Ivoire.",
      Icon: MapPin,
      note: "Partenariats locaux pour des rÃ©ponses adaptÃ©es et concrÃ¨tes.",
    },
  ],
  finalNote = "Nos valeurs ne sont pas des mots : ce sont nos critÃ¨res pour chaque dÃ©cision",
}) {
  return (
    <section aria-labelledby="vision-title" className="py-16 px-6 bg-white">
      <div className="max-w-6xl mx-auto">
        {/* Header */}
        <header className="mb-8">
          <div className="text-sm font-semibold text-[#D6453A]">{surtitre}</div>
          <h2 id="vision-title" className="mt-2 text-3xl sm:text-4xl font-extrabold text-gray-900 leading-tight">{title}</h2>
          <p className="mt-4 text-gray-700 text-base max-w-3xl">{intro}</p>
        </header>

        {/* Valeurs â€” grille Ã©purÃ©e */}
        <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
          {values.map((v) => {
            const Icon = v.Icon || (() => null);
            return (
              <article
                key={v.key}
                className="group flex flex-col gap-3 p-5 bg-gray-50 rounded-2xl border border-gray-100 shadow-sm hover:shadow-md transition-shadow"
                aria-labelledby={`value-${v.key}-title`}
              >
                <div className="flex items-start gap-4">
                  <div className="flex-shrink-0">
                    <div className="w-12 h-12 rounded-lg bg-white flex items-center justify-center shadow">
                      <Icon aria-hidden="true" size={20} className="text-[#D6453A]" />
                    </div>
                  </div>

                  <div>
                    <h3 id={`value-${v.key}-title`} className="text-lg font-semibold text-gray-900">{v.title}</h3>
                    <p className="text-sm text-gray-600 mt-1">{v.text}</p>
                  </div>
                </div>

                {/* Micro-copy UX â€” pourquoi Ã§a compte */}
                <div className="mt-auto text-xs text-gray-500">{v.note || generateNote(v.key)}</div>
              </article>
            );
          })}
        </div>

        {/* Final note / confiance */}
        <div className="mt-8">
          <p className="text-sm text-gray-600">{finalNote}</p>
        </div>
      </div>
    </section>
  );
}

function generateNote(key) {
  switch (key) {
    case 'solidarite':
      return "Nous favorisons des rÃ©ponses adaptÃ©es et respectueuses envers chaque personne aidÃ©e.";
    case 'transparence':
      return "Suivi visible : oÃ¹ va le don, comment il est utilisÃ©, qui en bÃ©nÃ©ficie.";
    case 'durabilite':
      return "RÃ©duire le gaspillage en donnant une seconde vie et en soutenant l'Ã©conomie locale.";
    case 'proximite':
      return "PrÃ©sence locale pour des actions rapides, pertinentes et durables.";
    default:
      return "Valeur essentielle guidant nos actions sur le terrain.";
  }
}

Vision.propTypes = {
  surtitre: PropTypes.string,
  title: PropTypes.string,
  intro: PropTypes.string,
  values: PropTypes.arrayOf(
    PropTypes.shape({
      key: PropTypes.string.isRequired,
      title: PropTypes.string.isRequired,
      text: PropTypes.string.isRequired,
      Icon: PropTypes.elementType,
      note: PropTypes.string,
    })
  ),
  finalNote: PropTypes.string,
};
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages\About> cd..
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages> ls


    Répertoire : C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages


Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
d-----        12/12/2025     18:22                About
d-----        12/12/2025     22:44                Actions
d-----        13/12/2025     03:45                actualites
d-----        13/12/2025     21:18                contact
d-----        13/12/2025     14:57                don
d-----        13/12/2025     00:52                Frip2Reve
-a----        12/12/2025     20:18            552 Home.js


PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages> cd .\Actions\
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages\Actions> cat .\ActionsPage.js
import React from 'react';

import Hero from './sections/Hero';
import Resume from './sections/Resume';
import Actions from './sections/Actions';
import Projets from './sections/Projets';
import Developpement from './sections/Developpement';
import Proof from './sections/Proof';
import Participer from './sections/Participer';
import Metrics from './sections/Metrics';
import FAQ from './sections/FAQ';

export default function AboutPage() {
  return (
    <>
      <main id="main">
        <Hero />
        <Resume/>
        <Actions/>
        <Projets/>
        <Developpement/>
        <Proof/>
        <Participer/>
        <Metrics/>
        <FAQ/>
      </main>
    </>
  );
}
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages\Actions> cat .\sections\*.js
import React from 'react';
import PropTypes from 'prop-types';

// MiniCard component
function ActionCard({ imageSrc, imageAlt, title, summary, ctaLabel, onClick }) {
  return (
    <div className="bg-white rounded-lg shadow-sm overflow-hidden flex flex-col">
      <div className="w-full h-48 bg-gray-100 flex items-center justify-center">
        {imageSrc ? (
          <img
            src={imageSrc}
            alt={imageAlt}
            className="w-full h-full object-cover"
            loading="lazy"
          />
        ) : (
          <span className="text-gray-400">Image action (Ã  remplacer)</span>
        )}
      </div>
      <div className="p-4 flex-1 flex flex-col justify-between">
        <div>
          <h3 className="text-lg font-semibold mb-1">{title}</h3>
          <p className="text-gray-600 text-sm">{summary}</p>
        </div>
        <button
          onClick={onClick}
          className="mt-3 inline-block px-4 py-2 text-sm font-semibold text-[#D6453A] border border-[#D6453A] rounded hover:bg-[#D6453A] hover:text-white transition-colors"
        >
          {ctaLabel}
        </button>
      </div>
    </div>
  );
}

ActionCard.propTypes = {
  imageSrc: PropTypes.string,
  imageAlt: PropTypes.string,
  title: PropTypes.string.isRequired,
  summary: PropTypes.string.isRequired,
  ctaLabel: PropTypes.string,
  onClick: PropTypes.func,
};

// Section ActionsRÃ©centes
export default function SectionActionsRecent() {
  const actions = [
    {
      title: 'Collecte quartier X',
      imageSrc: null, // remplacer par URL rÃ©elle
      imageAlt: 'Collecte locale',
      summary: 'JournÃ©e de collecte locale â€” 150 piÃ¨ces triÃ©es et revalorisÃ©es.',
      ctaLabel: 'En savoir',
      onClick: () => alert('Ouvre modal ou redirection page dÃ©tail'),
    },
    {
      title: 'Lancement Frip2RÃªve (pop-up)',
      imageSrc: null, // remplacer par URL rÃ©elle
      imageAlt: 'Boutique solidaire',
      summary:
        'PremiÃ¨re vente solidaire : recettes partiellement reversÃ©es pour lâ€™envoi en CÃ´te dâ€™Ivoire.',
      ctaLabel: 'En savoir',
      onClick: () => alert('Ouvre modal ou redirection page dÃ©tail'),
    },
    {
      title: 'Envoi & distribution pilote',
      imageSrc: null, // remplacer par URL rÃ©elle
      imageAlt: 'Distribution terrain',
      summary: '1,2 tonne envoyÃ©e vers nos relais ; distributions documentÃ©es.',
      ctaLabel: 'En savoir',
      onClick: () => alert('Ouvre modal ou redirection page dÃ©tail'),
    },
  ];

  return (
    <section className="py-12 bg-gray-50">
      <div className="max-w-6xl mx-auto px-6">
        <h2 className="text-2xl font-bold text-center mb-8">Actions rÃ©centes</h2>
        <div className="grid grid-cols-1 sm:grid-cols-3 gap-6">
          {actions.map((action, idx) => (
            <ActionCard key={idx} {...action} />
          ))}
        </div>
        <p className="mt-4 text-xs text-gray-400 text-center">
          DonnÃ©es indicatives â€” mises Ã  jour aprÃ¨s validation.
        </p>
      </div>
    </section>
  );
}
import React from 'react';
import PropTypes from 'prop-types';

// Carte roadmap rÃ©utilisable
function RoadmapCard({ title, detail, microCopy, imageSrc, imageAlt, ctaLabel, onClick }) {
  return (
    <div className="bg-white rounded-lg shadow-sm border-l-4 border-[#D6A43A] overflow-hidden flex flex-col">
      <div className="w-full h-40 bg-gray-100 flex items-center justify-center">
        {imageSrc ? (
          <img
            src={imageSrc}
            alt={imageAlt}
            className="w-full h-full object-cover"
            loading="lazy"
          />
        ) : (
          <span className="text-gray-400">Image projet en dÃ©veloppement</span>
        )}
      </div>
      <div className="p-4 flex-1 flex flex-col justify-between">
        <div>
          <div className="text-xs font-semibold text-[#D6A43A] mb-1 flex items-center gap-1">
            ðŸš§ En dÃ©veloppement
          </div>
          <h3 className="text-lg font-bold mb-2">{title}</h3>
          {detail && <p className="text-gray-700 text-sm mb-1">{detail}</p>}
          {microCopy && <p className="text-gray-500 text-xs">{microCopy}</p>}
        </div>
        {ctaLabel && (
          <button
            onClick={onClick}
            className="mt-3 px-4 py-2 text-sm font-semibold text-white bg-[#D6A43A] rounded hover:opacity-90 transition"
          >
            {ctaLabel}
          </button>
        )}
      </div>
    </div>
  );
}

RoadmapCard.propTypes = {
  title: PropTypes.string.isRequired,
  detail: PropTypes.string,
  microCopy: PropTypes.string,
  imageSrc: PropTypes.string,
  imageAlt: PropTypes.string,
  ctaLabel: PropTypes.string,
  onClick: PropTypes.func,
};

// Section Roadmap
export default function SectionRoadmap() {
  const roadmapItems = [
    {
      title: 'Structuration des relais en CÃ´te dâ€™Ivoire',
      detail: 'Mise en place de contrats et charte pour chaque relais local.',
      microCopy: 'En chantier â€” on avance avec vous.',
      imageSrc: null, // remplacer par schÃ©ma ou photo Ã©quipe
      imageAlt: 'RÃ©union relais CÃ´te dâ€™Ivoire',
      ctaLabel: 'Voir dÃ©tails',
      onClick: () => alert('Ouvre modal ou page dÃ©tail'),
    },
    {
      title: 'Plateforme de suivi',
      detail: 'Tableau de bord simple : envoi â†’ rÃ©ceptions â†’ bÃ©nÃ©ficiaires.',
      microCopy: 'En chantier â€” on avance avec vous.',
      imageSrc: null, // remplacer par mockup dashboard
      imageAlt: 'Mockup tableau de suivi',
      ctaLabel: 'Voir dÃ©tails',
      onClick: () => alert('Ouvre modal ou page dÃ©tail'),
    },
    {
      title: 'Calendrier Frip2RÃªve',
      detail: 'Ventes rÃ©guliÃ¨res + live-ventes mensuelles.',
      microCopy: 'En chantier â€” on avance avec vous.',
      imageSrc: null, // remplacer par photo stand / calendrier
      imageAlt: 'Calendrier Ã©vÃ©nement Frip2RÃªve',
      ctaLabel: 'Voir dÃ©tails',
      onClick: () => alert('Ouvre modal ou page dÃ©tail'),
    },
  ];

  return (
    <section className="py-12 bg-gray-50">
      <div className="max-w-6xl mx-auto px-6">
        <h2 className="text-2xl font-bold text-center mb-8">Projets en dÃ©veloppement</h2>
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
          {roadmapItems.map((item, idx) => (
            <RoadmapCard key={idx} {...item} />
          ))}
        </div>
      </div>
    </section>
  );
}
import React, { useState } from 'react';
import PropTypes from 'prop-types';
import { HelpCircle, ChevronDown } from 'lucide-react';

/**
 * SectionFAQActionsProjets
 * Page : Actions & Projets
 * Objectif UX : rÃ©pondre rapidement aux questions frÃ©quentes
 * - Section statique intÃ©grÃ©e dans le flux (pas de sticky / pas de floating)
 * - AccordÃ©on lÃ©ger (mobile-friendly)
 * - Lecture rapide : Q en gras, rÃ©ponses courtes
 */

function FAQItem({ question, answer, defaultOpen = false, showIcon = true }) {
  const [open, setOpen] = useState(defaultOpen);

  return (
    <div className="border border-gray-100 rounded-xl bg-white shadow-sm">
      <button
        type="button"
        onClick={() => setOpen(!open)}
        className="w-full flex items-start gap-3 p-4 text-left focus:outline-none focus-visible:ring-2 focus-visible:ring-[#7B2D2D] rounded-xl"
        aria-expanded={open}
      >
        {showIcon && (
          <HelpCircle
            className="w-5 h-5 mt-0.5 text-gray-400 flex-shrink-0"
            aria-hidden="true"
          />
        )}

        <span className="flex-1 font-semibold text-gray-800">{question}</span>

        <ChevronDown
          className={`w-5 h-5 text-gray-400 transition-transform duration-200 ${open ? 'rotate-180' : ''}`}
          aria-hidden="true"
        />
      </button>

      {open && (
        <div className="px-4 pb-4 pl-12 text-sm text-gray-600">
          {answer}
        </div>
      )}
    </div>
  );
}

FAQItem.propTypes = {
  question: PropTypes.string.isRequired,
  answer: PropTypes.node.isRequired,
  defaultOpen: PropTypes.bool,
  showIcon: PropTypes.bool,
};

export default function SectionFAQActionsProjets({ className = '', id = 'faq-actions', showIcons = true }) {
  const faqs = [
    {
      key: 'coordination',
      question: 'Qui coordonne les envois ?',
      answer: 'Natacha + relais locaux, documentation fournie.',
    },
    {
      key: 'preuve',
      question: 'Puis-je recevoir une preuve de destination ?',
      answer: 'Oui : photos & liste envoyÃ©es sur demande.',
    },
    {
      key: 'partenariat',
      question: 'Comment proposer un partenariat ou un point de dÃ©pÃ´t ?',
      answer: (
        <span>
          Formulaire de contact ou email :{' '}
          <a
            href="mailto:contact@revedivoire.fr"
            className="font-medium text-[#7B2D2D] underline hover:no-underline"
          >
            contact@revedivoire.fr
          </a>
        </span>
      ),
    },
  ];

  return (
    <section id={id} aria-labelledby={`${id}-title`} className={`w-full ${className}`}>
      <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
        <header className="mb-6">
          <h2 id={`${id}-title`} className="text-xl sm:text-2xl font-bold text-gray-800">
            FAQ â€” Actions & Projets
          </h2>
          <p className="mt-1 text-sm text-gray-500 max-w-prose">
            RÃ©ponses rapides aux questions les plus frÃ©quentes concernant nos actions.
          </p>
        </header>

        <div className="space-y-4">
          {faqs.map((item, index) => (
            <FAQItem
              key={item.key}
              question={item.question}
              answer={item.answer}
              defaultOpen={index === 0}
              showIcon={showIcons}
            />
          ))}
        </div>
      </div>
    </section>
  );
}

SectionFAQActionsProjets.propTypes = {
  className: PropTypes.string,
  id: PropTypes.string,
  showIcons: PropTypes.bool,
};
import React from 'react';
import PropTypes from 'prop-types';
import { Heart, Users } from 'lucide-react';

// HeroActions.js â€” page Â« Actions & Projets Â» mise Ã  jour UI/UX
export default function HeroActions({
  imageSrc = null,
  imageAlt = 'BÃ©nÃ©voles en action',
  title = "Agir prÃ¨s de chez vous, aider loin sur le terrain",
  subtitle = "Collectes, revalorisation et envois : chaque vÃªtement donnÃ© devient une aide concrÃ¨te pour une famille.",
  primaryCta = { label: 'Je donne', href: '/don' },
  secondaryCta = { label: "Je deviens bÃ©nÃ©vole", href: '/contact' },
  onPrimary = null,
  onSecondary = null,
  className = '',
}) {
  return (
    <header className={`relative w-full ${className}`} aria-labelledby="hero-actions-title">
      <div className="h-[420px] sm:h-[500px] w-full bg-gray-100 rounded-2xl overflow-hidden shadow-sm">
        {/* Image principale */}
        {imageSrc ? (
          <img src={imageSrc} alt={imageAlt} className="w-full h-full object-cover" />
        ) : (
          <div className="w-full h-full flex items-center justify-center bg-gray-100">
            <div className="text-center text-gray-400">Image d'action (Ã  remplacer)</div>
          </div>
        )}

        {/* Overlay lÃ©ger pour lisibilitÃ© */}
        <div className="absolute inset-0 bg-gradient-to-b from-transparent via-transparent to-white/80 pointer-events-none" />

        <div className="absolute left-0 right-0 bottom-0 p-6 sm:p-12">
          <div className="max-w-4xl mx-auto bg-white/95 rounded-2xl p-6 sm:p-8 shadow-md pointer-events-auto">
            <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
              <div className="sm:flex-1">
                <div className="text-sm font-semibold text-[#D6453A]">Actions & projets</div>
                <h1 id="hero-actions-title" className="mt-2 text-2xl sm:text-3xl font-extrabold text-gray-900 leading-tight">{title}</h1>
                <p className="mt-2 text-sm text-gray-600 max-w-2xl">{subtitle}</p>

                <div className="mt-4 flex flex-wrap items-center gap-3">
                  <a
                    href={primaryCta.href}
                    onClick={(e) => { if (typeof onPrimary === 'function') { e.preventDefault(); onPrimary(); } }}
                    className="inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-[#D6453A] text-white font-semibold shadow-md hover:opacity-95"
                  >
                    <Heart size={16} aria-hidden="true" />
                    {primaryCta.label}
                  </a>

                  {secondaryCta && (
                    <a
                      href={secondaryCta.href}
                      onClick={(e) => { if (typeof onSecondary === 'function') { e.preventDefault(); onSecondary(); } }}
                      className="inline-flex items-center gap-2 px-4 py-2 rounded-lg border border-gray-200 bg-white text-gray-800 font-semibold shadow-sm hover:underline"
                    >
                      <Users size={16} aria-hidden="true" />
                      {secondaryCta.label}
                    </a>
                  )}
                </div>

                <div className="mt-3 text-xs text-gray-500">Simple â†’ concret â†’ vÃ©rifiable</div>
              </div>

              {/* Optionnel : petit visuel d'Ã©quilibre */}
              <div className="hidden sm:flex sm:shrink-0 sm:items-center sm:justify-center">
                <div className="w-24 h-24 rounded-lg bg-gray-50 flex items-center justify-center border border-gray-100">
                  <svg xmlns="http://www.w3.org/2000/svg" className="w-10 h-10 text-gray-300" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="1.5" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                  </svg>
                </div>
              </div>

            </div>
          </div>
        </div>
      </div>
    </header>
  );
}

HeroActions.propTypes = {
  imageSrc: PropTypes.string,
  imageAlt: PropTypes.string,
  title: PropTypes.string,
  subtitle: PropTypes.string,
  primaryCta: PropTypes.shape({ label: PropTypes.string, href: PropTypes.string }),
  secondaryCta: PropTypes.shape({ label: PropTypes.string, href: PropTypes.string }),
  onPrimary: PropTypes.func,
  onSecondary: PropTypes.func,
  className: PropTypes.string,
};
import React, { useEffect, useRef, useState } from 'react';
import PropTypes from 'prop-types';
import { Clock, Users, Package, AlertCircle } from 'lucide-react';

/**
 * SectionMetricsImpact
 * - Version "pro" : design Ã©purÃ©, hiÃ©rarchie visuelle forte, responsive
 * - ConÃ§u pour s'intÃ©grer dans le flux de la page (pas de sticky/floating)
 * - Utilise TailwindCSS (v3+) et lucide-react pour les icÃ´nes
 *
 * Props principaux:
 *  - metrics: tableau d'objets { key, label, value, micro, icon, confirmed }
 *  - showTag: afficher ou non le badge "chiffrÃ©s Ã  confirmer"
 *  - animate: si true, tentera d'animer les nombres (tolÃ©rant aux textes non numÃ©riques)
 *  - className: conteneur optionnel
 */

function parseNumberFromText(text) {
  // Extrait un nombre (entier ou float) depuis un texte du type "~1,5 t" ou "120"
  if (!text || typeof text !== 'string') return null;
  // remplacer virgule par point, retirer espaces et unitÃ©
  const cleaned = text.replace(/,/g, '.').replace(/\s+/g, '').replace(/[a-zA-ZÂ°%â‚¬Â£â‚½kgmt]+/g, '');
  const match = cleaned.match(/-?\d+(?:\.\d+)?/);
  return match ? parseFloat(match[0]) : null;
}

function useAnimatedValue(targetText, enabled) {
  // Si enabled et targetText contient un number, anime la valeur
  const [display, setDisplay] = useState(targetText);
  const rafRef = useRef(null);

  useEffect(() => {
    if (!enabled) {
      setDisplay(targetText);
      return;
    }

    const targetNum = parseNumberFromText(targetText);
    if (targetNum === null) {
      setDisplay(targetText);
      return;
    }

    const duration = 900; // ms
    const start = performance.now();
    const startVal = 0;

    function step(now) {
      const t = Math.min(1, (now - start) / duration);
      const eased = 1 - Math.pow(1 - t, 3); // easeOutCubic
      const current = startVal + (targetNum - startVal) * eased;

      // Format: garder 1 dÃ©cimale si targetNum non entier
      const formatted = Number.isInteger(targetNum) ? Math.round(current).toString() : (Math.round(current * 10) / 10).toString();
      setDisplay(formatted + targetText.replace(/^[\-\d.,\s]+/, ''));

      if (t < 1) rafRef.current = requestAnimationFrame(step);
    }

    rafRef.current = requestAnimationFrame(step);

    return () => {
      if (rafRef.current) cancelAnimationFrame(rafRef.current);
    };
  }, [targetText, enabled]);

  return display;
}

function MetricCard({ Icon, label, value, micro, confirmed, showTag, animate }) {
  const animated = useAnimatedValue(value, animate);

  return (
    <article
      role="group"
      aria-label={`${label}: ${value}`}
      className="relative bg-white border border-gray-100 rounded-2xl p-5 shadow-sm hover:shadow-md transition-shadow duration-200"
    >
      {/* Accent stripe */}
      <div className="absolute left-0 top-4 bottom-4 w-1 rounded-l-2xl bg-gradient-to-b from-[#7B2D2D] to-[#9B4B4B]" aria-hidden="true" />

      <div className="flex items-start gap-4 ml-3">
        <div className="flex-none">
          <div className="w-14 h-14 rounded-lg flex items-center justify-center bg-gradient-to-br from-gray-50 to-white border border-gray-100">
            <Icon className="w-7 h-7 text-[#7B2D2D]" aria-hidden="true" />
          </div>
        </div>

        <div className="flex-1 min-w-0">
          <div className="flex items-center gap-3">
            <h3 className="text-sm font-semibold text-gray-700 truncate">{label}</h3>


          </div>

          <div className="mt-3">
            <div className="text-3xl sm:text-4xl font-extrabold leading-tight text-gray-900">{animated}</div>
            {micro && <p className="mt-1 text-sm text-gray-500">{micro}</p>}
          </div>
        </div>
      </div>
    </article>
  );
}

MetricCard.propTypes = {
  Icon: PropTypes.elementType.isRequired,
  label: PropTypes.string.isRequired,
  value: PropTypes.string.isRequired,
  micro: PropTypes.string,
  confirmed: PropTypes.bool,
  showTag: PropTypes.bool,
  animate: PropTypes.bool,
};

export default function SectionMetricsImpact({
  metrics,
  className = '',
  showTag = true,
  animate = false,
  id = 'metrics-impact',
}) {
  const defaults = [
    {
      key: 'created',
      label: 'AnnÃ©e de crÃ©ation',
      value: 'Depuis 2016 â€” actif',
      micro: "Association en activitÃ© depuis sa crÃ©ation",
      icon: Clock,
      confirmed: true,
    },
    {
      key: 'families',
      label: 'Familles aidÃ©es',
      value: '~120 familles aidÃ©es',
      micro: 'Estimation Ã  confirmer',
      icon: Users,
      confirmed: false,
    },
    {
      key: 'clothes',
      label: 'VÃªtements redistribuÃ©s',
      value: '~1,5 t vÃªtements redistribuÃ©s',
      micro: 'Estimation Ã  confirmer',
      icon: Package,
      confirmed: false,
    },
  ];

  const list = Array.isArray(metrics) && metrics.length > 0 ? metrics : defaults;

  return (
    <section id={id} aria-labelledby={`${id}-title`} className={`w-full ${className}`}>
      <div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
        <header className="flex items-start justify-between mb-6 gap-4">
          <div>
            <h2 id={`${id}-title`} className="text-xl sm:text-2xl font-bold text-gray-800">
              MÃ©triques & impact
            </h2>

          </div>

          <div className="hidden sm:flex items-center gap-2 text-gray-400">
            <AlertCircle className="w-5 h-5" aria-hidden="true" />
            <span className="text-sm">DonnÃ©es : Ã©tat actuel</span>
          </div>
        </header>

        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
          {list.map((m) => (
            <MetricCard
              key={m.key}
              Icon={m.icon}
              label={m.label}
              value={m.value}
              micro={m.micro}
              confirmed={!!m.confirmed}
              showTag={showTag}
              animate={animate}
            />
          ))}
        </div>
  </div>
    </section>
  );
}

SectionMetricsImpact.propTypes = {
  metrics: PropTypes.arrayOf(
    PropTypes.shape({
      key: PropTypes.string.isRequired,
      label: PropTypes.string.isRequired,
      value: PropTypes.string.isRequired,
      micro: PropTypes.string,
      icon: PropTypes.elementType,
      confirmed: PropTypes.bool,
    })
  ),
  className: PropTypes.string,
  showTag: PropTypes.bool,
  animate: PropTypes.bool,
  id: PropTypes.string,
};
import React from 'react';
import { Heart, Users, CreditCard } from 'lucide-react';

function ActionCard({ icon: Icon, title, description, ctaLabel, microCopy, onClick }) {
  return (
    <div className="bg-white rounded-lg shadow-md p-6 flex flex-col items-center text-center">
      <Icon className="w-10 h-10 text-[#D6453A] mb-3" aria-hidden="true" />
      <h3 className="text-lg font-bold mb-2">{title}</h3>
      <p className="text-gray-600 mb-4">{description}</p>
      <button
        onClick={onClick}
        className="px-5 py-2 bg-[#D6453A] text-white font-semibold rounded hover:opacity-90 transition mb-2"
      >
        {ctaLabel}
      </button>
      {microCopy && <p className="text-gray-500 text-xs">{microCopy}</p>}
    </div>
  );
}

export default function SectionParticipate() {
  return (
    <section className="py-12 bg-gray-50">
      <div className="max-w-6xl mx-auto px-6">
        <h2 className="text-2xl font-bold text-center mb-8">Comment participer</h2>
        <div className="grid grid-cols-1 sm:grid-cols-3 gap-6">
          <ActionCard
            icon={Users}
            title="Donner des vÃªtements"
            description="DÃ©poser vÃªtements propres dans un point proche."
            ctaLabel="Je donne"
            microCopy="MÃªme 1 vÃªtement compte."
            onClick={() => window.location.href = '/don'}
          />
          <ActionCard
            icon={Heart}
            title="Devenir bÃ©nÃ©vole"
            description="Aider au tri, aux ventes, Ã  la logistique."
            ctaLabel="Je rejoins"
            microCopy="MÃªme 1 heure compte."
            onClick={() => window.location.href = '/contact'}
          />
          <ActionCard
            icon={CreditCard}
            title="Soutenir financiÃ¨rement"
            description="Virement ou lien paiement."
            ctaLabel="Faire un don"
            microCopy="Chaque contribution fait la diffÃ©rence."
            onClick={() => window.location.href = '/don'}
          />
        </div>
      </div>
    </section>
  );
}
import React from 'react';
import PropTypes from 'prop-types';

// Carte projet rÃ©utilisable
function ProjectCard({ title, objective, process, impact, imageSrc, imageAlt, ctaLabel, onClick }) {
  return (
    <div className="bg-white rounded-lg shadow-sm overflow-hidden flex flex-col">
      <div className="w-full h-48 bg-gray-100 flex items-center justify-center">
        {imageSrc ? (
          <img
            src={imageSrc}
            alt={imageAlt}
            className="w-full h-full object-cover"
            loading="lazy"
          />
        ) : (
          <span className="text-gray-400">Image projet (Ã  remplacer)</span>
        )}
      </div>
      <div className="p-4 flex-1 flex flex-col justify-between">
        <div>
          <div className="text-xs font-semibold text-[#D6453A] mb-1">Projet permanent</div>
          <h3 className="text-lg font-bold mb-2">{title}</h3>
          {objective && <p className="text-gray-700 text-sm mb-1"><strong>Objectif:</strong> {objective}</p>}
          {process && <p className="text-gray-600 text-sm mb-1"><strong>Processus:</strong> {process}</p>}
          {impact && <p className="text-gray-600 text-sm mb-1"><strong>Impact / Garantie:</strong> {impact}</p>}
        </div>
        <button
          onClick={onClick}
          className="mt-3 px-4 py-2 text-sm font-semibold text-white bg-[#D6453A] rounded hover:opacity-90 transition"
        >
          {ctaLabel}
        </button>
      </div>
    </div>
  );
}

ProjectCard.propTypes = {
  title: PropTypes.string.isRequired,
  objective: PropTypes.string,
  process: PropTypes.string,
  impact: PropTypes.string,
  imageSrc: PropTypes.string,
  imageAlt: PropTypes.string,
  ctaLabel: PropTypes.string.isRequired,
  onClick: PropTypes.func,
};

// Section Projets Permanents
export default function SectionProjectsPermanent() {
  const projects = [
    {
      title: 'Points de collecte & tri',
      objective: 'RÃ©colter vÃªtements en bon Ã©tat et identifier piÃ¨ces Ã  revaloriser.',
      process: 'DÃ©pÃ´t â†’ Tri â†’ RÃ©paration lÃ©gÃ¨re â†’ Stockage pour vente/envoi',
      impact: 'RÃ©duction du gaspillage + approvisionnement pour Frip2RÃªve',
      imageSrc: null, // remplacer par photo tri en atelier
      imageAlt: 'Tri des vÃªtements en atelier',
      ctaLabel: 'Signalez un point de collecte',
      onClick: () => alert('Ouvre modal ou redirection page dÃ©tail'),
    },
    {
      title: 'Frip2RÃªve â€” boutique solidaire',
      objective: 'GÃ©nÃ©rer des fonds durables pour financer les actions',
      process: 'Ventes en ligne + pop-up stores mensuels. PiÃ¨ces triÃ©es et rÃ©parÃ©es.',
      impact: '% du chiffre reversÃ© indiquÃ© + preuve dâ€™affectation',
      imageSrc: null, // remplacer par photo boutique
      imageAlt: 'VÃªtements exposÃ©s en boutique',
      ctaLabel: 'Voir la boutique / Nous proposer des piÃ¨ces',
      onClick: () => alert('Ouvre modal ou redirection page boutique'),
    },
    {
      title: 'Envois & distribution en CÃ´te dâ€™Ivoire',
      objective: 'Assurer que les dons arrivent bien aux familles identifiÃ©es',
      process: 'Constitution liste â†’ ExpÃ©dition â†’ Confirmation relais â†’ Photos distribution',
      impact: 'Chaque envoi est archivÃ© pour transparence',
      imageSrc: null, // remplacer par photo distribution
      imageAlt: 'Distribution sur le terrain',
      ctaLabel: 'Voir rapport dâ€™envoi',
      onClick: () => alert('Ouvre modal rapport'),
    },
    {
      title: 'Accompagnement social ponctuel',
      objective: 'Aller au-delÃ  du matÃ©riel : orientation, aide ponctuelle',
      process: 'Tri besoins â†’ Orientation vers partenaires â†’ Soutien matÃ©riel immÃ©diat',
      impact: '',
      imageSrc: null, // remplacer par photo action sociale
      imageAlt: 'Accompagnement social',
      ctaLabel: 'Besoin dâ€™aide ? Contactez-nous',
      onClick: () => alert('Redirection page contact'),
    },
  ];

  return (
    <section className="py-12 bg-gray-50">
      <div className="max-w-6xl mx-auto px-6">
        <h2 className="text-2xl font-bold text-center mb-8">Projets permanents</h2>
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
          {projects.map((project, idx) => (
            <ProjectCard key={idx} {...project} />
          ))}
        </div>
      </div>
    </section>
  );
}
import React from 'react';
import PropTypes from 'prop-types';
import { FileText, DollarSign, Mail } from 'lucide-react';

function ProofCard({ icon: Icon, title, description, microCopy, ctaLabel, onClick }) {
  return (
    <div className="bg-white rounded-lg shadow-sm p-6 flex flex-col justify-between">
      <div className="flex items-center gap-3 mb-4">
        <Icon className="w-6 h-6 text-[#4CAF50]" aria-hidden="true" />
        <h3 className="text-lg font-bold">{title}</h3>
      </div>
      {description && <p className="text-gray-700 text-sm mb-2">{description}</p>}
      {microCopy && <p className="text-gray-500 text-xs mb-4">{microCopy}</p>}
      {ctaLabel && (
        <button
          onClick={onClick}
          className="mt-auto px-4 py-2 bg-[#4CAF50] text-white font-semibold rounded hover:opacity-90 transition text-sm"
        >
          {ctaLabel}
        </button>
      )}
    </div>
  );
}

ProofCard.propTypes = {
  icon: PropTypes.elementType.isRequired,
  title: PropTypes.string.isRequired,
  description: PropTypes.string,
  microCopy: PropTypes.string,
  ctaLabel: PropTypes.string,
  onClick: PropTypes.func,
};

export default function SectionTransparency() {
  return (
    <section className="py-12 bg-gray-50">
      <div className="max-w-6xl mx-auto px-6">
        <h2 className="text-2xl font-bold text-center mb-8">Preuves & Transparence</h2>
        <div className="grid grid-cols-1 sm:grid-cols-3 gap-6">
          <ProofCard
            icon={FileText}
            title="Rapports dâ€™envoi"
            description="TÃ©lÃ©versement PDF et photos des distributions."
            microCopy="Tout est tracÃ© : photos, listes et rÃ©cÃ©pissÃ©s."
            ctaLabel="Voir preuves"
            onClick={() => alert('Ouvre modal ou lien PDF')}
          />
          <ProofCard
            icon={DollarSign}
            title="RÃ©sumÃ© financier simplifiÃ©"
            description="Tableaux par opÃ©ration Frip2RÃªve : recettes, % reversÃ©, affectation."
            microCopy="Vous voulez vÃ©rifier ? On vous envoie tout."
            ctaLabel="Voir dÃ©tails"
            onClick={() => alert('Ouvre modal ou lien PDF')}
          />
          <ProofCard
            icon={Mail}
            title="Contact administratif dÃ©diÃ©"
            description="Email : contact@revedivoire.fr"
            microCopy="Questions sur les rapports ou finances ? Contactez-nous directement."
            ctaLabel="Envoyer un email"
            onClick={() => window.location.href = 'mailto:contact@revedivoire.fr'}
          />
        </div>
      </div>
    </section>
  );
}
import React from 'react';
import { Package, ShoppingBag, MapPin } from 'lucide-react';

// SectionRÃ©sumÃ©.js â€” rÃ©sumÃ© rapide des actions & projets
export default function SectionResume() {
  const bullets = [
    {
      icon: <MapPin size={32} aria-hidden="true" />,
      title: 'Collectes locales rÃ©guliÃ¨res',
      text: 'Collectes organisÃ©es en rÃ©gion pour rÃ©cupÃ©rer des vÃªtements en bon Ã©tat.',
    },
    {
      icon: <ShoppingBag size={32} aria-hidden="true" />,
      title: 'Frip2RÃªve â€” boutique solidaire',
      text: 'Ventes solidaires pour financer directement les actions terrain.',
    },
    {
      icon: <Package size={32} aria-hidden="true" />,
      title: 'Envois & distributions',
      text: 'Acheminement et remise des dons en CÃ´te dâ€™Ivoire via des relais locaux.',
    },
  ];

  return (
    <section className="py-12 bg-gray-50">
      <div className="max-w-5xl mx-auto px-6 text-center">
        <div className="grid grid-cols-1 sm:grid-cols-3 gap-8">
          {bullets.map((item, index) => (
            <div key={index} className="flex flex-col items-center text-center">
              <div className="text-[#D6453A] mb-3">{item.icon}</div>
              <h3 className="font-semibold text-lg mb-1">{item.title}</h3>
              <p className="text-gray-600 text-sm">{item.text}</p>
            </div>
          ))}
        </div>
        <div className="mt-6 text-xs text-gray-500">
          Tout est traÃ§able : photos, listes et rÃ©cÃ©pissÃ©s.
        </div>
      </div>
    </section>
  );
}
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages\Actions> cd..
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages> ls


    Répertoire : C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages


Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
d-----        12/12/2025     18:22                About
d-----        12/12/2025     22:44                Actions
d-----        13/12/2025     03:45                actualites
d-----        13/12/2025     21:18                contact
d-----        13/12/2025     14:57                don
d-----        13/12/2025     00:52                Frip2Reve
-a----        12/12/2025     20:18            552 Home.js


PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages> cd .\actualites\
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages\actualites> cat .\ActualitesPage.js
import React from 'react';

import Hero from './sections/Hero';
import Article from './sections/Article';
import SocialFeed from './sections/SocialFeed';

import Newsletter from './sections/Newsletter';

export default function AboutPage() {
  return (
    <>
      <main id="main">
        <Hero />
        <Article/>
        <SocialFeed/>
        <Newsletter/>
      </main>
    </>
  );
}
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages\actualites> cat .\sections\*.js
import React, { useEffect, useRef, useState } from 'react';
import PropTypes from 'prop-types';

/**
 * Article.js â€” Section Article (template)
 * Version mise Ã  jour : FULL-WIDTH desktop (hero full-bleed + layout 2-col Ã©talÃ©), mobile-first behavior
 * - Desktop : hero full-bleed, contenu en grille deux colonnes (contenu principal large + sidebar fixe/sticky)
 * - Mobile  : images en haut, CTA sticky bas, galerie swipe
 * - Accessible, SEO-friendly, tailwind utility classes
 *
 * Props:
 *  - article: objet (voir defaults)
 *  - onParticipate, onReminder: callbacks facultatifs
 *  - whatsappNumber
 */

export default function Article({ article: articleProp, onParticipate, onReminder, whatsappNumber = '33123456789' }) {
  const defaultArticle = {
    id: 'live-vente-beauvais',
    title: 'Live-vente solidaire Frip2RÃªve Ã  Beauvais',
    date: '2025-12-21T20:00:00',
    tag: 'evenement',
    tagLabel: 'Ã‰vÃ©nement',
    location: 'Beauvais / Instagram Live (@frip2reve)',
    intro: 'Nous organisons une nouvelle live-vente solidaire pour prÃ©senter des piÃ¨ces africaines uniques, remises Ã  neuf avec soin. Chaque achat soutient directement nos actions solidaires.',
    content: [
      { type: 'p', text: 'PrÃ©sentation rapide de lâ€™Ã©vÃ©nement â€” format, dÃ©roulÃ©, intervenants.' },
      { type: 'p', text: "Pourquoi câ€™est important : soutien local et international, rÃ©emploi textile." },
      { type: 'p', text: "Ã€ qui Ã§a sâ€™adresse : toute personne intÃ©ressÃ©e par la mode responsable et solidaire." },
    ],
    infos: {
      when: 'Samedi 21 dÃ©cembre Ã  20h00',
      where: 'Instagram Live â€” @frip2reve',
      what: 'Vente de piÃ¨ces uniques, quantitÃ©s limitÃ©es',
    },
    status: 'upcoming',
    images: [
      { src: 'https://picsum.photos/seed/art1/1600/900', alt: 'Live-vente Frip2RÃªve â€” vÃªtements africains solidaires', caption: 'AperÃ§u des piÃ¨ces proposÃ©es' },
      { src: 'https://picsum.photos/seed/art2/1200/900', alt: 'DÃ©tail tissu wax', caption: 'DÃ©tail du tissu' },
      { src: 'https://picsum.photos/seed/art3/1200/900', alt: 'PrÃ©paration colis solidaires', caption: 'PrÃ©paration des colis' },
    ],
  };

  const [article] = useState(articleProp || defaultArticle);
  const [index, setIndex] = useState(0);
  const [lightbox, setLightbox] = useState(false);
  const [touchStartX, setTouchStartX] = useState(null);
  const galleryRef = useRef(null);

  useEffect(() => setIndex(0), [article.images]);

  const next = () => setIndex(i => (i + 1) % article.images.length);
  const prev = () => setIndex(i => (i - 1 + article.images.length) % article.images.length);

  const onTouchStart = (e) => setTouchStartX(e.touches[0].clientX);
  const onTouchEnd = (e) => {
    if (touchStartX == null) return;
    const dx = e.changedTouches[0].clientX - touchStartX;
    const threshold = 40;
    if (dx > threshold) prev();
    else if (dx < -threshold) next();
    setTouchStartX(null);
  };

  useEffect(() => {
    const onKey = (e) => {
      if (lightbox && e.key === 'Escape') setLightbox(false);
      if (e.key === 'ArrowRight') next();
      if (e.key === 'ArrowLeft') prev();
    };
    window.addEventListener('keydown', onKey);
    return () => window.removeEventListener('keydown', onKey);
  }, [lightbox]);

  const isPast = article.status === 'past' || new Date(article.date) < new Date();
  const whatsappHref = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(`Bonjour Frip2RÃªve â€” Question sur "${article.title}"`)}`;

  const handleParticipate = (e) => { e?.preventDefault?.(); if (typeof onParticipate === 'function') onParticipate(); else alert('Participer â€” implÃ©mente onParticipate'); };
  const handleReminder = (e) => { e?.preventDefault?.(); if (typeof onReminder === 'function') onReminder(); else alert('Rappel â€” implÃ©mente onReminder'); };

  return (
    <article className="w-full">
      {/* HERO FULL-WIDTH */}
      <header className="relative w-full">
        <div className="w-screen left-1/2 right-1/2 -ml-[50vw] -mr-[50vw] overflow-hidden" style={{ position: 'relative' }}>
      <div className="relative h-40 sm:h-56 lg:h-64">
      <img
              src={article.images[0]?.src}
              alt={article.images[0]?.alt || article.title}
              className="absolute inset-0 w-full h-full object-cover"
              loading="eager"
            />
            <div className="absolute inset-0 bg-gradient-to-b from-black/40 via-black/10 to-black/50" />
            <div className="absolute inset-0 flex items-end">
              <div className="max-w-7xl mx-auto w-full px-4 py-6 lg:py-8">
                <div className="max-w-3xl text-white">
                  <div className="inline-flex items-center gap-2 bg-white/10 px-3 py-1 rounded-full text-sm">
                    <span>ðŸ·ï¸ {article.tagLabel}</span>
                    <time dateTime={new Date(article.date).toISOString()} className="ml-2 text-xs opacity-80">ðŸ“… {new Date(article.date).toLocaleDateString('fr-FR', { day: 'numeric', month: 'long', year: 'numeric' })}</time>
                  </div>

                  <h1 id="article-title" className="mt-4 text-2xl sm:text-3xl lg:text-4xl font-extrabold leading-tight">{article.title}</h1>
                  <div className="mt-3 text-sm sm:text-base opacity-90">ðŸ“ {article.location}</div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </header>

      {/* MAIN GRID : content + sidebar */}
      <main className="max-w-7xl mx-auto px-4 -mt-4 lg:-mt-8">
        <div className="grid grid-cols-1 lg:grid-cols-[minmax(0,1fr)_360px] gap-8">

          {/* LEFT: MAIN ARTICLE */}
          <div>
            <div className="bg-white rounded-md shadow-sm p-5 mt-5">
              {/* Intro */}
              <p className="text-gray-700 leading-relaxed mb-4">{article.intro}</p>

              {/* Content blocks */}
              <div className="prose prose-base max-w-none text-gray-800">
                {article.content.map((block, idx) => {
                  if (block.type === 'p') return <p key={idx}>{block.text}</p>;
                  if (block.type === 'h2') return <h2 key={idx}>{block.text}</h2>;
                  if (block.type === 'ul') return (
                    <ul key={idx} className="list-disc list-inside">
                      {block.items.map((it, i) => <li key={i}>{it}</li>)}
                    </ul>
                  );
                  return null;
                })}

                {/* Inline CTA in content (desktop visible) */}
                {!isPast && (
                  <div className="mt-6 hidden lg:block">
                    <div className="p-4 bg-[#F9FAFB] border rounded-md">
                      <h3 className="font-semibold">Participe au live</h3>
                      <p className="text-sm text-gray-600">Inscris-toi pour recevoir le lien et un rappel.</p>
                      <div className="mt-3 flex gap-3">
                        <button onClick={handleParticipate} aria-label={`Participer au live ${article.title}`} className="px-4 py-2 bg-[#7B2D2D] text-white rounded-md">Participer au live</button>
                        <button onClick={handleReminder} aria-label={`Recevoir un rappel ${article.title}`} className="px-4 py-2 border rounded-md">Recevoir un rappel</button>
                      </div>
                    </div>
                  </div>
                )}

              </div>

              {/* Gallery (mobile carousel then grid) */}
              <div className="mt-8">
                <h3 className="text-lg font-semibold mb-3">Galerie</h3>

                {/* Mobile carousel */}
                <div
                  ref={galleryRef}
                  className="sm:hidden relative overflow-hidden rounded-md"
                  onTouchStart={onTouchStart}
                  onTouchEnd={onTouchEnd}
                  aria-roledescription="carousel"
                  aria-label="Galerie photos"
                >
                  <div className="w-full h-56 bg-gray-100 flex items-center justify-center">
                    <img src={article.images[index].src} alt={article.images[index].alt} loading="lazy" className="w-full h-full object-cover" onClick={() => setLightbox(true)} />
                  </div>
                  {article.images.length > 1 && (
                    <>
                      <button aria-label="Image prÃ©cÃ©dente" className="absolute left-2 top-1/2 -translate-y-1/2 bg-white/90 p-1 rounded-full" onClick={prev}>â€¹</button>
                      <button aria-label="Image suivante" className="absolute right-2 top-1/2 -translate-y-1/2 bg-white/90 p-1 rounded-full" onClick={next}>â€º</button>
                    </>
                  )}
                </div>

                {/* Desktop grid */}
                <div className="hidden sm:grid sm:grid-cols-3 sm:gap-3">
                  {article.images.map((img, i) => (
                    <button key={i} className="overflow-hidden rounded-md" onClick={() => { setIndex(i); setLightbox(true); }} aria-label={`Ouvrir image ${i + 1}`}>
                      <img src={img.src} alt={img.alt} loading="lazy" className="w-full h-40 object-cover" />
                      {img.caption && <div className="text-xs text-gray-500 mt-1">{img.caption}</div>}
                    </button>
                  ))}
                </div>

              </div>

            </div>
          </div>

          {/* RIGHT: SIDEBAR (sticky on desktop) */}
          <aside className="lg:relative">
            <div className="lg:sticky top-24">
              <div className="bg-white rounded-md shadow-sm p-5">
                <h4 className="font-semibold">Infos pratiques</h4>
                <dl className="mt-3 text-sm text-gray-700">
                  <div className="flex justify-between py-1"><dt className="font-medium">Quand ?</dt><dd>{article.infos.when}</dd></div>
                  <div className="flex justify-between py-1"><dt className="font-medium">OÃ¹ ?</dt><dd>{article.infos.where}</dd></div>
                  <div className="flex justify-between py-1"><dt className="font-medium">Quoi ?</dt><dd>{article.infos.what}</dd></div>
                </dl>

                <div className="mt-4">
                  {!isPast ? (
                    <>
                      <button onClick={handleParticipate} aria-label={`Participer au live ${article.title}`} className="w-full px-4 py-3 bg-[#7B2D2D] text-white rounded-md">Participer au live</button>
                      <button onClick={handleReminder} aria-label={`Recevoir un rappel ${article.title}`} className="w-full mt-3 px-4 py-3 border rounded-md">Recevoir un rappel</button>
                      <div className="text-xs text-gray-500 mt-3">Gratuit â€¢ Sans engagement â€¢ DÃ©sinscription facile</div>
                    </>
                  ) : (
                    <>
                      <div className="font-semibold">Cet Ã©vÃ©nement est terminÃ©.</div>
                      <a href="/actualites" className="text-indigo-600 underline mt-3 inline-block">Voir les prochains Ã©vÃ©nements</a>
                      <a href={whatsappHref} className="block mt-2 text-sm">Contacter via WhatsApp</a>
                    </>
                  )}
                </div>

                {/* Reassurance */}
                <div className="mt-6 text-xs text-gray-600">
                  <div>âœ… Live accessible</div>
                  <div>âœ… PiÃ¨ces limitÃ©es</div>
                  <div>âœ… Soutien solidaire</div>
                </div>
              </div>

              {/* Optional small module for sharing */}
              <div className="mt-4 hidden lg:block">
                <div className="bg-white rounded-md p-4 text-sm text-gray-700 shadow-sm">
                  <div className="font-medium mb-2">Partager</div>
                  <div className="flex gap-2">
                    <a href={`https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(window?.location?.href || '/')}`} className="underline">Facebook</a>
                    <a href={`https://wa.me/?text=${encodeURIComponent(article.title + ' ' + (window?.location?.href || '/'))}`} className="underline">WhatsApp</a>
                  </div>
                </div>
              </div>

            </div>
          </aside>

        </div>
      </main>

      {/* STICKY MOBILE CTA */}
      {!isPast && (
        <div className="sm:hidden fixed left-0 right-0 bottom-0 bg-white border-t p-3 z-50">
          <div className="max-w-7xl mx-auto px-4 flex gap-3">
            <button onClick={handleParticipate} aria-label={`Participer au live ${article.title}`} className="flex-1 px-4 py-3 bg-[#7B2D2D] text-white rounded-md">Participer</button>
            <button onClick={handleReminder} aria-label={`Recevoir un rappel ${article.title}`} className="px-3 py-3 border rounded-md">Rappel</button>
          </div>
        </div>
      )}

      {/* LIGHTBOX */}
      {lightbox && (
        <div role="dialog" aria-modal="true" className="fixed inset-0 z-50 flex items-center justify-center bg-black/80" onClick={() => setLightbox(false)}>
          <div className="max-w-5xl w-full p-4" onClick={e => e.stopPropagation()}>
            <img src={article.images[index].src} alt={article.images[index].alt} className="w-full h-auto object-contain rounded-md" />
            {article.images[index].caption && <div className="mt-2 text-sm text-white text-center">{article.images[index].caption}</div>}
            <button aria-label="Fermer" onClick={() => setLightbox(false)} className="absolute top-4 right-4 text-white bg-black/40 rounded-full px-2 py-1">âœ•</button>
          </div>
        </div>
      )}

    </article>
  );
}

Article.propTypes = {
  article: PropTypes.object,
  onParticipate: PropTypes.func,
  onReminder: PropTypes.func,
  whatsappNumber: PropTypes.string,
};
import React from 'react';
import PropTypes from 'prop-types';
import { CalendarDays, Newspaper } from 'lucide-react';
import heroImage from '../../../assets/association_1.jpg'; // image large, humaine, action terrain

/**
 * Hero â€” Page ActualitÃ©s (RÃªve dâ€™Ivoire)
 * Objectif : crÃ©dibilitÃ©, projet vivant, entrÃ©e claire vers le feed
 */

export default function HeroActualites({ title, subtitle }) {
  return (
    <section
      className="relative overflow-hidden"
      aria-labelledby="hero-actualites-title"
    >
      {/* Image de fond */}
      <div className="absolute inset-0">
        <img
          src={heroImage}
          alt="Actions solidaires de lâ€™association RÃªve dâ€™Ivoire sur le terrain"
          className="w-full h-full object-cover"
          loading="eager"
        />
        {/* Overlay sombre pour lisibilitÃ© */}
        <div className="absolute inset-0 bg-black/55" />
      </div>

      {/* Contenu */}
      <div className="relative max-w-6xl mx-auto px-4 py-20 sm:py-28">
        <div className="max-w-2xl">
          <span className="inline-flex items-center gap-2 mb-4 px-3 py-1 rounded-full bg-white/90 text-sm font-medium">
            <Newspaper size={16} />
            ActualitÃ©s & Ã©vÃ©nements
          </span>

          <h1
            id="hero-actualites-title"
            className="text-3xl sm:text-5xl font-extrabold text-white leading-tight"
          >
            {title || "Suivez la vie de RÃªve dâ€™Ivoire"}
          </h1>

          <p className="mt-4 text-base sm:text-lg text-white/90">
            {subtitle ||
              "Actions solidaires, projets terrain et Ã©vÃ©nements qui font avancer lâ€™association."}
          </p>

          {/* Indicateur de fraÃ®cheur */}
          <div className="mt-6 flex items-center gap-2 text-sm text-white/80">
            <CalendarDays size={16} />
            <span>Mises Ã  jour rÃ©guliÃ¨res â€¢ DerniÃ¨res infos en premier</span>
          </div>
        </div>
      </div>
    </section>
  );
}

HeroActualites.propTypes = {
  title: PropTypes.string,
  subtitle: PropTypes.string,
};
import React, { useState } from 'react';
import { Mail, Bell, HeartHandshake, CheckCircle2, AlertCircle } from 'lucide-react';

export default function SectionNewsletter() {
  const [email, setEmail] = useState('');
  const [prenom, setPrenom] = useState('');
  const [alerts, setAlerts] = useState({ ventes: true, evenements: true, actus: false });
  const [status, setStatus] = useState(null); // success | error | null

  const handleSubmit = e => {
    e.preventDefault();
    if (!email || !email.includes('@')) {
      setStatus('error');
      return;
    }
    // TODO: brancher API newsletter / Brevo / Mailchimp
    setStatus('success');
    setEmail('');
    setPrenom('');
  };

  return (
    <section className="w-full bg-gray-50 py-16 px-4 sm:px-6 lg:px-12">
      <div className="max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-10 items-center">
        {/* Texte & visuel */}
        <div className="space-y-4">
          <h2 className="text-3xl sm:text-4xl font-extrabold text-gray-900">
            Restez informÃ© de nos actions solidaires
          </h2>
          <p className="text-gray-700 max-w-xl">
            Recevez les prochaines dates de ventes, collectes et actualitÃ©s de RÃªve dâ€™Ivoire.
          </p>
          <div className="flex items-center gap-3 text-sm text-gray-600">
            <HeartHandshake className="w-5 h-5" />
            Une communautÃ© engagÃ©e, des actions concrÃ¨tes.
          </div>
        </div>

        {/* Formulaire */}
        <div className="bg-white rounded-2xl shadow-sm p-6 sm:p-8 w-full">
          <form onSubmit={handleSubmit} className="space-y-4" aria-live="polite">
            <div>
              <label className="block text-sm font-medium mb-1">PrÃ©nom (optionnel)</label>
              <input
                type="text"
                value={prenom}
                onChange={e => setPrenom(e.target.value)}
                className="w-full border rounded-md px-3 py-2 focus:outline-none focus:ring"
              />
            </div>

            <div>
              <label className="block text-sm font-medium mb-1">Adresse e-mail *</label>
              <div className="relative">
                <Mail className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400" />
                <input
                  type="email"
                  required
                  value={email}
                  onChange={e => setEmail(e.target.value)}
                  className="w-full border rounded-md pl-10 pr-3 py-2 focus:outline-none focus:ring"
                />
              </div>
            </div>

            <fieldset className="space-y-2">
              <legend className="text-sm font-medium">Je souhaite recevoir :</legend>
              <label className="flex items-center gap-2 text-sm">
                <input type="checkbox" checked={alerts.ventes} onChange={e => setAlerts(a => ({ ...a, ventes: e.target.checked }))} />
                <Bell className="w-4 h-4" /> Ventes solidaires
              </label>
              <label className="flex items-center gap-2 text-sm">
                <input type="checkbox" checked={alerts.evenements} onChange={e => setAlerts(a => ({ ...a, evenements: e.target.checked }))} />
                <Bell className="w-4 h-4" /> Collectes & Ã©vÃ©nements
              </label>
              <label className="flex items-center gap-2 text-sm">
                <input type="checkbox" checked={alerts.actus} onChange={e => setAlerts(a => ({ ...a, actus: e.target.checked }))} />
                <Bell className="w-4 h-4" /> ActualitÃ©s de lâ€™association
              </label>
            </fieldset>

            <label className="flex items-start gap-2 text-xs text-gray-600">
              <input type="checkbox" required />
              Jâ€™accepte de recevoir les actualitÃ©s de lâ€™association RÃªve dâ€™Ivoire.
            </label>

            <button type="submit" className="w-full mt-2 px-4 py-3 rounded-md bg-[#7B2D2D] text-white font-semibold">
              Sâ€™inscrire
            </button>

            <p className="text-xs text-gray-500 text-center">
              ZÃ©ro spam â€¢ 1 Ã  2 messages par mois â€¢ DÃ©sinscription possible Ã  tout moment
            </p>

            {status === 'success' && (
              <div className="flex items-center gap-2 text-green-700 text-sm mt-3">
                <CheckCircle2 className="w-4 h-4" />
                Merci, votre inscription est bien prise en compte.
              </div>
            )}
            {status === 'error' && (
              <div className="flex items-center gap-2 text-red-700 text-sm mt-3">
                <AlertCircle className="w-4 h-4" />
                Adresse e-mail invalide ou dÃ©jÃ  inscrite.
              </div>
            )}
          </form>

          <div className="mt-6 text-center">
            <p className="text-sm mb-2">Vous prÃ©fÃ©rez WhatsApp ?</p>
            <a
              href="https://wa.me/33123456789?text=Bonjour%20je%20souhaite%20recevoir%20les%20alertes%20RÃªve%20dâ€™Ivoire"
              target="_blank"
              rel="noopener noreferrer"
              className="inline-flex items-center gap-2 px-4 py-2 border rounded-md text-sm"
              aria-label="Recevoir les alertes WhatsApp RÃªve dâ€™Ivoire"
            >
              <Bell className="w-4 h-4" /> Recevoir les alertes WhatsApp
            </a>
          </div>
        </div>
      </div>
    </section>
  );
}
// SocialFeed_RÃªve_dIvoire.jsx
import React, { useState, useEffect, useRef } from 'react';
import PropTypes from 'prop-types';

function IconInstagram({ className = 'w-5 h-5' }) {
  return (
    <svg className={className} viewBox="0 0 24 24" fill="none" aria-hidden>
      <rect x="2" y="2" width="20" height="20" rx="5" stroke="currentColor" strokeWidth="1.2" />
      <circle cx="12" cy="12" r="3.2" stroke="currentColor" strokeWidth="1.2" />
      <circle cx="17.2" cy="6.8" r="0.6" fill="currentColor" />
    </svg>
  );
}

function formatDateISO(dateStr) {
  try {
    const d = new Date(dateStr);
    return d.toLocaleDateString(undefined, { day: '2-digit', month: 'short', year: 'numeric' });
  } catch (e) {
    return dateStr || '';
  }
}

export default function SocialFeed({
  mode = 'server',
  initialPosts = [],
  limit = 6,
  fetchMore = null,
  elfsightAppId = ''
}) {
  // client-only guard to avoid SSR mismatches (pages/ router)
  const [isClient, setIsClient] = useState(false);
  useEffect(() => { setIsClient(true); }, []);

  const [posts, setPosts] = useState(initialPosts.length ? initialPosts.slice(0, limit) : []);
  const [hasMore, setHasMore] = useState(false);
  const [loading, setLoading] = useState(false);
  const [preview, setPreview] = useState(null);
  const embedLoadedRef = useRef({ instagram: false });

  // Server-mode fetch
  useEffect(() => {
    if (mode !== 'server') return;
    if (initialPosts.length) {
      setPosts(initialPosts.slice(0, limit));
      return;
    }
    setLoading(true);
    fetch(`/api/instagram?limit=${limit}`)
      .then(r => r.json())
      .then(data => {
        if (Array.isArray(data.posts)) {
          setPosts(data.posts);
          setHasMore(Boolean(data.hasMore));
        }
      })
      .catch((e) => {
        // log quietly for debugging
        // console.warn('Instagram API fetch failed', e);
      })
      .finally(() => setLoading(false));
  }, [mode, initialPosts.length, limit]);

  // Elfsight init: robust handling whether script exists, loaded or not.
  useEffect(() => {
    if (!isClient) return;
    if (mode !== 'elfsight' || !elfsightAppId) return;

    const initElfsight = () => {
      if (typeof window !== 'undefined' && window.elfsight && typeof window.elfsight.init === 'function') {
        try { window.elfsight.init(); } catch (e) { /* silence */ }
      }
    };

    const selector = 'script[src="https://elfsightcdn.com/platform.js"]';
    const existingScript = document.querySelector(selector);

    if (!existingScript) {
      const script = document.createElement('script');
      script.src = 'https://elfsightcdn.com/platform.js';
      script.async = true;
      script.onload = initElfsight;
      document.body.appendChild(script);
      // nothing to cleanup here (script stays)
      return;
    }

    // If script exists but elfsight isn't ready yet, wait for load
    if (typeof window === 'undefined' || !window.elfsight) {
      const onLoad = () => initElfsight();
      existingScript.addEventListener('load', onLoad);
      return () => existingScript.removeEventListener('load', onLoad);
    }

    // If script already loaded, just init
    initElfsight();
  }, [mode, elfsightAppId, isClient]);

  function openPreview(post) {
    setPreview(post);
    if (mode === 'embed') loadEmbedScriptFor(post.network);
  }

  function closePreview() { setPreview(null); }

  function loadEmbedScriptFor(network) {
    if (embedLoadedRef.current[network]) return;
    embedLoadedRef.current[network] = true;
    if (network === 'instagram') {
      const s = document.createElement('script');
      s.src = 'https://www.instagram.com/embed.js';
      s.async = true; s.defer = true; document.body.appendChild(s);
    }
  }

  // Render nothing on server; avoids SSR mismatch and Elfsight silent failure
  if (!isClient) return null;

  return (
    <section aria-labelledby="social-feed-title" className="my-8">
      <div className="flex items-start justify-between mb-4">
        <div>
          <h2 id="social-feed-title" className="text-xl font-bold">RÃ©seaux â€” RÃªve dâ€™Ivoire</h2>
          <p className="text-sm text-gray-600">Derniers posts Instagram â€” suivez nos actions en direct.</p>
        </div>
        <div className="hidden sm:flex gap-2 items-center">
          <a href="https://www.instagram.com/reve_divoire/" target="_blank" rel="noreferrer" className="text-sm underline">Suivre Instagram</a>
        </div>
      </div>

      {/* Elfsight widget mode */}
      {mode === 'elfsight' && elfsightAppId ? (
        <div className="w-full">
          <div className={`elfsight-app-${elfsightAppId}`} data-elfsight-app-lazy />
        </div>
      ) : (
        <>
          <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
            {posts.map(post => (
              <article key={post.id} className="relative group bg-gray-50 rounded-md overflow-hidden">
                <button type="button" onClick={() => openPreview(post)} className="w-full h-full block text-left focus:outline-none" aria-label={`Ouvrir le post ${post.id}`}>
                  <div className="w-full aspect-square bg-gray-200 overflow-hidden">
                    {post.thumbnail ? (
                      <img src={post.thumbnail} alt={post.alt || `Post RÃªve d'Ivoire â€” Instagram`} className="w-full h-full object-cover" loading="lazy" />
                    ) : (
                      <div className="w-full h-full flex items-center justify-center text-sm text-gray-500">Image non disponible</div>
                    )}
                  </div>
                  <div className="absolute top-2 left-2 inline-flex items-center gap-2 bg-white/90 px-2 py-1 rounded-full text-xs font-medium">
                    <IconInstagram />
                    <span>Instagram</span>
                  </div>
                  <div className="p-2">
                    <div className="text-sm line-clamp-1 text-gray-800">{post.caption || 'â€”'}</div>
                    <div className="text-xs text-gray-500 mt-1">{formatDateISO(post.date)}</div>
                  </div>
                </button>
                <a href={post.url} target="_blank" rel="noreferrer" className="absolute bottom-2 right-2 bg-white/95 px-2 py-1 rounded-md text-xs font-semibold opacity-0 group-hover:opacity-100 transition">Voir</a>
              </article>
            ))}
          </div>

          <div className="mt-6 flex justify-center">
            {!posts.length && !loading ? <div className="text-sm text-gray-500">Aucun post disponible.</div> : null}
            {/* use hasMore legitimately so ESLint stops whining */}
            {hasMore ? (
              fetchMore ? (
                <button onClick={fetchMore} className="px-3 py-2 border rounded">Charger plus</button>
              ) : (
                <a href="https://www.instagram.com/reve_divoire/" target="_blank" rel="noreferrer" className="px-3 py-2 border rounded">Voir plus sur Instagram</a>
              )
            ) : null}
          </div>
        </>
      )}

      {preview && (
        <div className="fixed inset-0 z-50 flex items-center justify-center p-4" role="dialog" aria-modal="true">
          <div className="absolute inset-0 bg-black/50" onClick={closePreview} />
          <div className="relative w-full max-w-3xl bg-white rounded-md overflow-hidden shadow-lg">
            <div className="flex items-center justify-between p-3 border-b">
              <div className="flex items-center gap-3"><IconInstagram /><div className="text-sm font-medium">Instagram</div></div>
              <div><a href={preview.url} target="_blank" rel="noreferrer" className="text-sm underline mr-3">Voir sur Instagram</a><button onClick={closePreview} className="px-2">Fermer</button></div>
            </div>
            <div className="p-4 grid grid-cols-1 lg:grid-cols-2 gap-4">
              <div>{preview.thumbnail ? <img src={preview.thumbnail} alt={preview.alt || 'AperÃ§u du post'} className="w-full h-auto object-cover rounded" /> : <div className="w-full h-64 bg-gray-100 flex items-center justify-center">Image non disponible</div>}</div>
              <div>
                <div className="text-sm text-gray-700 mb-3">{preview.caption || 'Aucune lÃ©gende fournie.'}</div>
                <div className="text-xs text-gray-500">{formatDateISO(preview.date)}</div>
                <div className="mt-4 flex gap-2"><a href={preview.url} target="_blank" rel="noreferrer" className="px-3 py-2 border rounded-sm text-sm">Voir sur Instagram</a></div>
              </div>
            </div>
          </div>
        </div>
      )}
    </section>
  );
}

SocialFeed.propTypes = {
  mode: PropTypes.oneOf(['server', 'embed', 'elfsight']),
  initialPosts: PropTypes.array,
  limit: PropTypes.number,
  fetchMore: PropTypes.func,
  elfsightAppId: PropTypes.string
};
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages\actualites> cd..
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages> ls


    Répertoire : C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages


Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
d-----        12/12/2025     18:22                About
d-----        12/12/2025     22:44                Actions
d-----        13/12/2025     03:45                actualites
d-----        13/12/2025     21:18                contact
d-----        13/12/2025     14:57                don
d-----        13/12/2025     00:52                Frip2Reve
-a----        12/12/2025     20:18            552 Home.js


PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages> cat .\contact\ContactPage.js
import React from 'react';

import Hero from './sections/Hero';
import Infos from './sections/Infos';
import Formulaire from './sections/Formulaire';
import Plan from './sections/Plan';
import Horaires from './sections/Horaires';
import Securite from './sections/Securite';


export default function AboutPage() {
  return (
    <>
      <main id="main">
        <Hero />
        <Infos/>
        <Formulaire/>
        <Plan/>
        <Horaires/>
        <Securite/>
      </main>
    </>
  );
}
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages> cat .\contact\sections\*.js
import React, { useEffect, useRef, useState } from 'react';
import PropTypes from 'prop-types';
import { Mail, Paperclip, CheckCircle2, AlertCircle } from 'lucide-react';

/**
 * SectionFormulaireContact.js
 * - Mobile-first, accessible, full-width
 * - Vouvoyement dans tous les textes
 * - Champs: Nom, Email (requis), Sujet (select), Message, PiÃ¨ce jointe
 * - Honeypot anti-spam + simple captcha arithmÃ©tique
 * - Upload : PDF, JPG, PNG, max 5 Mo
 * - Envoi vers apiEndpoint (POST FormData), ou simulation si non fourni
 */

const MAX_FILE_BYTES = 5 * 1024 * 1024; // 5MB
const ACCEPTED_TYPES = ['application/pdf', 'image/jpeg', 'image/png'];

export default function SectionFormulaireContact({ apiEndpoint = '/api/contact' }) {
  const [nom, setNom] = useState('');
  const [email, setEmail] = useState('');
  const [sujet, setSujet] = useState('General');
  const [message, setMessage] = useState('');
  const [file, setFile] = useState(null);
  const [captchaA, setCaptchaA] = useState(1);
  const [captchaB, setCaptchaB] = useState(2);
  const [captchaAnswer, setCaptchaAnswer] = useState('');
  const [loading, setLoading] = useState(false);
  const [status, setStatus] = useState(null); // {type:'success'|'error', text}
  const liveRef = useRef(null);

  // Honeypot field (should remain empty)
  const [hp, setHp] = useState('');

  useEffect(() => {
    // generate a tiny arithmetic captcha (simple but effective against bots)
    const a = Math.floor(Math.random() * 4) + 1; // 1..4
    const b = Math.floor(Math.random() * 5) + 1; // 1..5
    setCaptchaA(a);
    setCaptchaB(b);
  }, []);

  function validateEmail(e) {
    if (!e) return false;
    return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(e);
  }

  function validateFile(f) {
    if (!f) return { ok: true };
    if (!ACCEPTED_TYPES.includes(f.type)) return { ok: false, error: 'Format de fichier non autorisÃ© (PDF, JPG, PNG).' };
    if (f.size > MAX_FILE_BYTES) return { ok: false, error: 'Fichier trop volumineux (max 5 Mo).' };
    return { ok: true };
  }

  async function handleSubmit(e) {
    e.preventDefault();
    setStatus(null);

    // Honeypot check
    if (hp) {
      setStatus({ type: 'error', text: "Spam dÃ©tectÃ©." });
      liveRef.current?.focus?.();
      return;
    }

    if (!validateEmail(email)) {
      setStatus({ type: 'error', text: "Adresse eâ€‘mail invalide." });
      liveRef.current?.focus?.();
      return;
    }

    if (!message.trim()) {
      setStatus({ type: 'error', text: "Veuillez saisir votre message." });
      liveRef.current?.focus?.();
      return;
    }

    const capCorrect = Number(captchaAnswer) === (captchaA + captchaB);
    if (!capCorrect) {
      setStatus({ type: 'error', text: "RÃ©ponse antiâ€‘spam incorrecte." });
      liveRef.current?.focus?.();
      return;
    }

    const fCheck = validateFile(file);
    if (!fCheck.ok) {
      setStatus({ type: 'error', text: fCheck.error });
      liveRef.current?.focus?.();
      return;
    }

    setLoading(true);

    try {
      if (!apiEndpoint) {
        // simulate network
        await new Promise(res => setTimeout(res, 800));
        setStatus({ type: 'success', text: 'Message envoyÃ©. Nous vous rÃ©pondrons rapidement.' });
        setNom(''); setEmail(''); setSujet('General'); setMessage(''); setFile(null); setCaptchaAnswer('');
        liveRef.current?.focus?.();
        return;
      }

      const fd = new FormData();
      fd.append('nom', nom);
      fd.append('email', email);
      fd.append('sujet', sujet);
      fd.append('message', message);
      if (file) fd.append('file', file, file.name);

      const res = await fetch(apiEndpoint, { method: 'POST', body: fd });
      if (!res.ok) {
        const txt = await res.text().catch(() => 'Erreur serveur');
        throw new Error(txt || 'Erreur serveur');
      }

      setStatus({ type: 'success', text: 'Message envoyÃ©. Nous vous rÃ©pondrons rapidement.' });
      setNom(''); setEmail(''); setSujet('General'); setMessage(''); setFile(null); setCaptchaAnswer('');
      liveRef.current?.focus?.();
    } catch (err) {
      setStatus({ type: 'error', text: `Impossible d'envoyer le message : ${err.message}` });
      liveRef.current?.focus?.();
    } finally {
      setLoading(false);
    }
  }

  return (
    <section className="w-full bg-white py-16 px-4 sm:px-6 lg:px-12" aria-labelledby="contact-form-title">
      <div className="max-w-7xl mx-auto">
        <h2 id="contact-form-title" className="text-2xl sm:text-3xl font-extrabold text-gray-900 mb-3">Ã‰crivezâ€‘nous</h2>
        <p className="text-gray-700 mb-8">Expliquez-nous votre demande, nous vous rÃ©pondons dÃ¨s que possible.</p>

        <form onSubmit={handleSubmit} className="grid grid-cols-1 lg:grid-cols-2 gap-8" noValidate>
          <div>
            <label htmlFor="c-nom" className="block text-sm font-medium text-gray-700">Nom</label>
            <input id="c-nom" type="text" placeholder="Ton nom" value={nom} onChange={e => setNom(e.target.value)} className="mt-1 block w-full border rounded-md px-3 py-2 focus:ring-2 focus:ring-[#7B2D2D]" />

            <label htmlFor="c-email" className="block text-sm font-medium text-gray-700 mt-4">Email <span className="text-red-600">*</span></label>
            <input id="c-email" type="email" inputMode="email" placeholder="Ton email" value={email} onChange={e => setEmail(e.target.value)} required className="mt-1 block w-full border rounded-md px-3 py-2 focus:ring-2 focus:ring-[#7B2D2D]" />

            <label htmlFor="c-sujet" className="block text-sm font-medium text-gray-700 mt-4">Sujet</label>
            <select id="c-sujet" value={sujet} onChange={e => setSujet(e.target.value)} className="mt-1 block w-full border rounded-md px-3 py-2 focus:ring-2 focus:ring-[#7B2D2D]">
              <option value="General">GÃ©nÃ©ral</option>
              <option value="Don">Don</option>
              <option value="Benevolat">BÃ©nÃ©volat</option>
              <option value="Partenariat">Partenariat</option>
            </select>

            <label htmlFor="c-file" className="block text-sm font-medium text-gray-700 mt-4">PiÃ¨ce jointe (PDF, JPG, PNG â€” max 5 Mo)</label>
            <div className="flex items-center gap-3">
              <label htmlFor="c-file" className="inline-flex items-center gap-2 px-4 py-2 rounded-md border bg-white cursor-pointer">
                <Paperclip className="w-4 h-4" /> Joindre un fichier
              </label>
              <input id="c-file" type="file" accept=".pdf,image/jpeg,image/png" onChange={e => setFile(e.target.files?.[0] || null)} className="sr-only" />
              <div className="text-sm text-gray-600">{file ? file.name : 'Aucun fichier'}</div>
            </div>

          </div>

          <div>
            <label htmlFor="c-message" className="block text-sm font-medium text-gray-700">Message <span className="text-red-600">*</span></label>
            <textarea id="c-message" rows={8} placeholder="Votre message" value={message} onChange={e => setMessage(e.target.value)} required className="mt-1 block w-full border rounded-md px-3 py-2 focus:ring-2 focus:ring-[#7B2D2D]"></textarea>

            {/* Captcha simple */}
            <label htmlFor="c-captcha" className="block text-sm font-medium text-gray-700 mt-4">Antiâ€‘spam : combien font {captchaA} + {captchaB} ? <span className="text-red-600">*</span></label>
            <input id="c-captcha" type="number" placeholder="RÃ©ponse" value={captchaAnswer} onChange={e => setCaptchaAnswer(e.target.value)} className="mt-1 block w-40 border rounded-md px-3 py-2 focus:ring-2 focus:ring-[#7B2D2D]" />

            {/* Honeypot (visually hidden) */}
            <label className="sr-only" htmlFor="c-newsletter">Ne pas remplir (champ anti-spam)</label>
            <input id="c-newsletter" name="c-newsletter" type="text" value={hp} onChange={e => setHp(e.target.value)} autoComplete="off" className="sr-only" />

            {/* Submit (desktop placed here visually) */}
            <div className="hidden lg:block mt-6">
              <button type="submit" className="px-6 py-3 bg-[#7B2D2D] text-white rounded-md font-medium" aria-label="Envoyer le message" disabled={loading}>{loading ? 'Envoiâ€¦' : 'Envoyer le message'}</button>
            </div>

          </div>

          {/* Mobile submit (full-width) */}
          <div className="lg:hidden">
            <button type="submit" className="w-full px-6 py-3 bg-[#7B2D2D] text-white rounded-md font-medium" aria-label="Envoyer le message" disabled={loading}>{loading ? 'Envoiâ€¦' : 'Envoyer le message'}</button>
            <p className="text-xs text-gray-500 mt-2">Vos donnÃ©es sont protÃ©gÃ©es et ne sont jamais revendues.</p>
          </div>

        </form>

        {/* Live feedback */}
        <div tabIndex={-1} ref={liveRef} aria-live="polite" className="mt-6">
          {status?.type === 'success' && (
            <div className="flex items-center gap-2 text-green-700"><CheckCircle2 className="w-4 h-4" />{status.text}</div>
          )}
          {status?.type === 'error' && (
            <div className="flex items-center gap-2 text-red-700"><AlertCircle className="w-4 h-4" />{status.text}</div>
          )}
        </div>

      </div>
    </section>
  );
}
import React from 'react';
import PropTypes from 'prop-types';
import { Mail, Users } from 'lucide-react';

/**
 * HeroContact.js â€” Hero pour la page Contact (RÃªve d'Ivoire)
 * - Desktop : 60â€“70vh
 * - Mobile  : 50â€“60vh
 * - Full-bleed image avec overlay lÃ©ger
 * - Vouvoyement dans les textes
 *
 * Props:
 *  - title, subtitle
 *  - primaryHref (mailto or contact form url)
 *  - imageSrc, imageAlt
 *  - variant: 'default' | 'short'
 */

export default function HeroContact({
  title = 'Contacte RÃªve dâ€™Ivoire',
  subtitle = 'Une question, un projet ou envie de vous engager ? Ã‰crivezâ€‘nous, nous rÃ©pondons simplement.',
  primaryHref = '/contact/form',
  imageSrc = 'https://picsum.photos/seed/contact/1600/900',
  imageAlt = "RÃªve d'Ivoire â€” Ã©quipe et actions solidaires",
  variant = 'default',
}) {
  const isShort = variant === 'short';

  return (
    <header className="relative w-full" role="banner" aria-label="Hero Contact RÃªve d'Ivoire">
      <div className="relative h-[55vh] lg:h-[65vh] w-screen left-1/2 right-1/2 -ml-[50vw] -mr-[50vw] overflow-hidden">
        <img src={imageSrc} alt={imageAlt} className="absolute inset-0 w-full h-full object-cover" loading="eager" />
        <div className="absolute inset-0 bg-black/35" />

        <div className="absolute inset-0 flex items-center">
          <div className="max-w-7xl mx-auto w-full px-6 sm:px-8 lg:px-12">
            <div className="max-w-3xl text-white">
              <h1 id="contact-hero-title" className="text-2xl sm:text-3xl lg:text-4xl font-extrabold leading-tight">
                {isShort ? 'Besoin de nous contacter ?' : title}
              </h1>

              <p className="mt-3 text-sm sm:text-base lg:text-lg text-white/90">
                {isShort ? 'Lâ€™Ã©quipe RÃªve dâ€™Ivoire est lÃ  pour vous rÃ©pondre.' : subtitle}
              </p>

              <div className="mt-6 flex flex-col sm:flex-row sm:items-center gap-3">
                <a
                  href={primaryHref}
                  aria-label="Contacter lâ€™association RÃªve dâ€™Ivoire"
                  className="inline-flex items-center justify-center gap-3 px-5 py-3 rounded-md bg-[#7B2D2D] text-white font-semibold w-full sm:w-auto focus:outline-none focus:ring-2 focus:ring-white/40"
                >
                  <Mail className="w-5 h-5" />
                  Nous contacter
                </a>

                <a
                  href="/don"
                  aria-label="Faire un don Ã  RÃªve dâ€™Ivoire"
                  className="inline-flex items-center justify-center gap-3 px-5 py-3 rounded-md border border-white/25 bg-white/8 text-white font-medium w-full sm:w-auto hover:bg-white/12"
                >
                  <Users className="w-5 h-5" />
                  Faire un don
                </a>
              </div>

              <p className="mt-3 text-xs sm:text-sm text-white/85">RÃ©ponse humaine â€¢ Sans engagement â€¢ DonnÃ©es protÃ©gÃ©es</p>
            </div>
          </div>
        </div>
      </div>
    </header>
  );
}

HeroContact.propTypes = {
  title: PropTypes.string,
  subtitle: PropTypes.string,
  primaryHref: PropTypes.string,
  imageSrc: PropTypes.string,
  imageAlt: PropTypes.string,
  variant: PropTypes.oneOf(['default','short']),
};
import React from 'react';
import PropTypes from 'prop-types';
import { Clock, Instagram, Video } from 'lucide-react';

/**
 * Section â€” Heures dâ€™ouverture & rÃ©seaux sociaux
 * - Mobile-first, accessible, full-width
 * - Vouvoyement dans tous les textes
 */

export default function SectionHorairesReseaux({
  instagram = 'https://www.instagram.com/revedivoire',
  tiktok = 'https://www.tiktok.com/@revedivoire',
}) {
  return (
    <section className="w-full bg-white py-12 px-4 sm:px-6 lg:px-12" aria-labelledby="horaires-reseaux-title">
      <div className="max-w-7xl mx-auto">
        <header className="max-w-3xl mb-6">
          <h2 id="horaires-reseaux-title" className="text-2xl font-extrabold text-gray-900">Horaires & rÃ©seaux sociaux</h2>
          <p className="mt-2 text-gray-700">Dites quand nous joindre et oÃ¹ suivre nos actions.</p>
        </header>

        <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
          {/* Heures d'ouverture */}
          <div className="bg-gray-50 rounded-lg p-6">
            <div className="flex items-start gap-3">
              <Clock className="w-6 h-6 text-[#7B2D2D]" aria-hidden />
              <div>
                <h3 className="text-lg font-semibold">Heures dâ€™ouverture</h3>
                <div className="mt-3 text-base text-gray-800">
                  <div>Lundi â€“ Vendredi : 9h â€“ 18h</div>
                  <div>Samedi : sur rendezâ€‘vous</div>
                  <div>Dimanche : fermÃ©</div>
                </div>
                <p className="mt-3 text-sm text-gray-500">RÃ©ponse sous 24 Ã  48h ouvrÃ©es.</p>
              </div>
            </div>
          </div>

          {/* RÃ©seaux sociaux */}
          <div className="bg-gray-50 rounded-lg p-6">
            <div className="flex items-start gap-3">
              <Instagram className="w-6 h-6 text-[#7B2D2D]" aria-hidden />
              <div>
                <h3 className="text-lg font-semibold">Suis RÃªve dâ€™Ivoire</h3>
                <p className="mt-3 text-gray-700">Suivez-nous pour nos actualitÃ©s, actions et Ã©vÃ©nements.</p>

                <div className="mt-4 grid grid-cols-1 sm:grid-cols-2 gap-3">
                  <a href={instagram} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-3 px-4 py-3 rounded-md bg-white border text-gray-900 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-offset-1 focus:ring-[#7B2D2D]" aria-label="Ouvrir Instagram de RÃªve d'Ivoire">
                    <Instagram className="w-5 h-5" />
                    <span>@revedivoire</span>
                  </a>

                  <a href={tiktok} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-3 px-4 py-3 rounded-md bg-white border text-gray-900 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-offset-1 focus:ring-[#7B2D2D]" aria-label="Ouvrir TikTok de RÃªve d'Ivoire">
                    <Video className="w-5 h-5" />
                    <span>@revedivoire</span>
                  </a>
                </div>

              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}

SectionHorairesReseaux.propTypes = {
  instagram: PropTypes.string,
  tiktok: PropTypes.string,
};
import React from 'react';
import { Phone, Mail, MapPin, Clock, ExternalLink } from 'lucide-react';

/**
 * SectionCoordonnees.js
 * Section â€” Nos coordonnÃ©es
 * - Full-width, mobile-first, accessible
 * - Vouvoyement dans tous les textes
 *
 * Usage: <SectionCoordonnees />
 * Vous pouvez passer props pour remplacer les valeurs par dÃ©faut si besoin.
 */

export default function SectionCoordonnees({
  organisation = 'Association RÃªve dâ€™Ivoire',
  addressLine1 = '35 Avenue Hector Berlioz',
  addressLine2 = '95820 BruyÃ¨res-sur-Oise, France',
  phoneDisplay = '+33 6 62 03 47 50',
  phoneHref = 'tel:+33662034750',
  whatsappNumber = '33662034750', // used to build wa.me link (no +)
  email = 'contact@revedivoire.fr',
  mapQuery = '35 Avenue Hector Berlioz 95820 Bruy%C3%A8res-sur-Oise France',
}) {
  const waLink = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent("Bonjour RÃªve d'Ivoire â€” J'aimerais avoir des informations.")}`;
  const mapsHref = `https://www.google.com/maps/search/?api=1&query=${mapQuery}`;

  return (
    <section className="w-full bg-gray-50 py-12 px-4 sm:px-6 lg:px-12" aria-labelledby="coordonnees-title">
      <div className="max-w-7xl mx-auto">
        <h2 id="coordonnees-title" className="text-2xl font-extrabold text-gray-900 mb-4">Nos coordonnÃ©es</h2>
        <p className="text-base text-gray-700 mb-8">Vous prÃ©fÃ©rez nous joindre directement ? Choisissez lâ€™option qui vous convient : appel, WhatsApp, email ou retrouvez-nous en personne.</p>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* CoordonnÃ©es principales */}
          <div className="bg-white rounded-lg shadow-sm p-6">
            <div className="flex items-start gap-4">
              <MapPin className="w-6 h-6 text-[#7B2D2D] flex-shrink-0" aria-hidden />
              <div>
                <div className="text-sm text-gray-600">{organisation}</div>
                <address className="not-italic mt-2 text-base text-gray-800">
                  <div>{addressLine1}</div>
                  <div>{addressLine2}</div>
                </address>
                <div className="mt-3 flex flex-wrap gap-2">
                  <a href={mapsHref} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 px-3 py-2 rounded-md bg-gray-100 text-sm text-gray-800 hover:bg-gray-200 focus:outline-none focus:ring-2 focus:ring-offset-1 focus:ring-[#7B2D2D]" aria-label="Ouvrir l'adresse dans Google Maps"> <ExternalLink className="w-4 h-4" /> ItinÃ©raire</a>
                </div>
              </div>
            </div>

            <hr className="my-6" />

            <div className="space-y-3 text-base">
              <div className="flex items-center gap-3">
                <Phone className="w-5 h-5 text-gray-700" aria-hidden />
                <div>
                  <div className="text-sm text-gray-600">TÃ©lÃ©phone</div>
                  <a href={phoneHref} className="text-base text-gray-900 block" aria-label={`Appeler ${phoneDisplay}`}>{phoneDisplay}</a>
                  <div className="text-xs text-gray-500">Appel ou WhatsApp</div>
                </div>
              </div>

              <div className="flex items-center gap-3">
                <Mail className="w-5 h-5 text-gray-700" aria-hidden />
                <div>
                  <div className="text-sm text-gray-600">Email professionnel</div>
                  <a href={`mailto:${email}`} className="text-base text-gray-900 block" aria-label={`Envoyer un email Ã  ${email}`}>{email}</a>
                </div>
              </div>

            </div>
          </div>

          {/* Horaires */}
          <div className="bg-white rounded-lg shadow-sm p-6">
            <div className="flex items-start gap-3">
              <Clock className="w-6 h-6 text-[#7B2D2D]" aria-hidden />
              <div>
                <h3 className="text-lg font-medium">Horaires</h3>
                <div className="mt-3 text-base text-gray-800">
                  <div>Lundi â€“ Vendredi : 9h â€“ 18h</div>
                  <div>Samedi : sur rendezâ€‘vous</div>
                </div>
                <p className="mt-3 text-sm text-gray-500">RÃ©ponse sous 24 Ã  48h ouvrÃ©es.</p>
              </div>
            </div>
          </div>

          {/* Actions rapides */}
          <div className="bg-white rounded-lg shadow-sm p-6 flex flex-col justify-between">
            <div>
              <h3 className="text-lg font-medium mb-4">Actions rapides</h3>

              <div className="grid grid-cols-1 gap-3">
                <a href={phoneHref} className="inline-flex items-center gap-3 px-4 py-3 rounded-md bg-[#7B2D2D] text-white text-center" aria-label="Appeler RÃªve d'Ivoire"> <Phone className="w-4 h-4" /> Appeler</a>
                <a href={waLink} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-3 px-4 py-3 rounded-md bg-green-600 text-white text-center" aria-label="Contacter RÃªve d'Ivoire via WhatsApp"> <Phone className="w-4 h-4" /> WhatsApp</a>
                <a href={`mailto:${email}`} className="inline-flex items-center gap-3 px-4 py-3 rounded-md border text-gray-900 text-center" aria-label="Envoyer un email Ã  RÃªve d'Ivoire"> <Mail className="w-4 h-4" /> Ã‰crire</a>
                <a href={mapsHref} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-3 px-4 py-3 rounded-md bg-gray-100 text-gray-900 text-center" aria-label="Ouvrir l'adresse sur Google Maps"> <MapPin className="w-4 h-4" /> ItinÃ©raire</a>
              </div>
            </div>

            <div className="mt-6 text-sm text-gray-500">
              <div>Annuaire des Entreprises +1</div>
              <div className="mt-2">Vous pouvez Ã©galement consulter nos coordonnÃ©es sur les annuaires publics.</div>
            </div>
          </div>

        </div>
      </div>
    </section>
  );
}
import React from 'react';
import PropTypes from 'prop-types';
import { MapPin, ExternalLink } from 'lucide-react';

/**
 * SectionPlanAcces.js
 * Section â€” Plan dâ€™accÃ¨s / Carte
 * - Mobile-first, full-width map embed (Google Maps)
 * - Vouvoyement dans tous les textes
 *
 * Props:
 *  - name, addressLine1, addressLine2, mapQuery
 */

export default function SectionPlanAcces({
  name = 'RÃªve dâ€™Ivoire',
  addressLine1 = '35 Avenue Hector Berlioz',
  addressLine2 = '95820 BruyÃ¨res-sur-Oise, France',
  mapQuery = '35 Avenue Hector Berlioz 95820 Bruy%C3%A8res-sur-Oise France',
}) {
  const mapsSearch = `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(mapQuery)}`;
  const mapsDirections = `https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(mapQuery)}`;
  const embedSrc = `https://www.google.com/maps?q=${encodeURIComponent(mapQuery)}&output=embed`;

  return (
    <section className="w-full bg-white py-12 px-4 sm:px-6 lg:px-12" aria-labelledby="plan-title">
      <div className="max-w-7xl mx-auto">
        <header className="max-w-3xl mb-6">
          <h2 id="plan-title" className="text-2xl font-extrabold text-gray-900">Nous trouver</h2>
          <p className="mt-2 text-gray-700">Cliquez sur la carte pour ouvrir lâ€™itinÃ©raire.</p>
        </header>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Address block */}
          <div className="bg-gray-50 rounded-lg p-6">
            <div className="flex items-start gap-3">
              <MapPin className="w-6 h-6 text-[#7B2D2D]" aria-hidden />
              <div>
                <div className="text-sm text-gray-600">{name}</div>
                <address className="not-italic mt-2 text-base text-gray-800">
                  <div>{addressLine1}</div>
                  <div>{addressLine2}</div>
                </address>

                <div className="mt-4 flex flex-col gap-2">
                  <a href={mapsDirections} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 px-4 py-2 rounded-md bg-[#7B2D2D] text-white" aria-label="Ouvrir l'itinÃ©raire vers RÃªve d'Ivoire"> <ExternalLink className="w-4 h-4" /> ItinÃ©raire Google Maps</a>
                  <a href={mapsSearch} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 px-4 py-2 rounded-md bg-white border text-gray-900" aria-label="Ouvrir l'adresse dans Google Maps">Voir l'adresse sur Google Maps</a>
                </div>
              </div>
            </div>

            <p className="mt-4 text-sm text-gray-500">Horaires : Lundiâ€“Vendredi 9hâ€“18h. Cliquez sur la carte pour ouvrir lâ€™itinÃ©raire.</p>
          </div>

          {/* Map full width on desktop by spanning columns */}
          <div className="lg:col-span-2">
            <div className="w-full rounded-lg overflow-hidden shadow-sm">
              <a href={mapsDirections} target="_blank" rel="noopener noreferrer" aria-label="Ouvrir l'itinÃ©raire vers RÃªve d'Ivoire" className="block">
                <div className="w-full aspect-video bg-gray-100">
                  <iframe
                    title={`Carte â€” ${name}`}
                    src={embedSrc}
                    width="100%"
                    height="100%"
                    loading="lazy"
                    style={{ border: 0 }}
                    aria-hidden="false"
                  />
                </div>
              </a>

              {/* Fallback link visible if JS disabled or iframe blocked */}
              <div className="p-4 bg-white">
                <p className="text-sm text-gray-700">Si la carte ne sâ€™affiche pas, <a href={mapsSearch} target="_blank" rel="noopener noreferrer" className="underline">ouvrez lâ€™itinÃ©raire sur Google Maps</a>.</p>
              </div>
            </div>
          </div>

        </div>
      </div>
    </section>
  );
}

SectionPlanAcces.propTypes = {
  name: PropTypes.string,
  addressLine1: PropTypes.string,
  addressLine2: PropTypes.string,
  mapQuery: PropTypes.string,
};
import React from 'react';
import PropTypes from 'prop-types';
import { ShieldCheck, Lock, FileText } from 'lucide-react';

/**
 * Section â€” SÃ©curitÃ© & confidentialitÃ© (RGPD)
 * - Mobile-first, concise, rassurant
 * - Vouvoyement dans tous les textes
 */

export default function SectionSecuriteConfidentialite({ privacyHref = '/politique-confidentialite' }) {
  return (
    <section className="w-full bg-gray-50 py-12 px-4 sm:px-6 lg:px-12" aria-labelledby="sec-conf-title">
      <div className="max-w-4xl mx-auto">
        <h2 id="sec-conf-title" className="text-2xl font-extrabold text-gray-900">SÃ©curitÃ© & confidentialitÃ©</h2>

        <div className="mt-4 grid grid-cols-1 sm:grid-cols-3 gap-6">
          <div className="flex items-start gap-3">
            <ShieldCheck className="w-6 h-6 text-[#7B2D2D] flex-shrink-0" aria-hidden />
            <div>
              <h3 className="font-semibold">Usage des donnÃ©es</h3>
              <p className="mt-2 text-sm text-gray-700">Les informations transmises via ce formulaire sont utilisÃ©es uniquement pour rÃ©pondre Ã  votre demande. Aucune donnÃ©e nâ€™est vendue ou cÃ©dÃ©e Ã  des tiers Ã  des fins commerciales.</p>
            </div>
          </div>

          <div className="flex items-start gap-3">
            <Lock className="w-6 h-6 text-[#7B2D2D] flex-shrink-0" aria-hidden />
            <div>
              <h3 className="font-semibold">Stockage sÃ©curisÃ©</h3>
              <p className="mt-2 text-sm text-gray-700">Les donnÃ©es sont stockÃ©es de maniÃ¨re sÃ©curisÃ©e et traitÃ©es conformÃ©ment au RGPD. Vous pouvez demander lâ€™accÃ¨s, la modification ou la suppression de vos donnÃ©es Ã  tout moment.</p>
            </div>
          </div>

          <div className="flex items-start gap-3">
            <FileText className="w-6 h-6 text-[#7B2D2D] flex-shrink-0" aria-hidden />
            <div>
              <h3 className="font-semibold">Vos droits</h3>
              <p className="mt-2 text-sm text-gray-700">En envoyant ce formulaire, vous acceptez notre politique de confidentialitÃ©. <a href={privacyHref} className="underline text-indigo-600" aria-label="Lire la politique de confidentialitÃ©">Lire la politique de confidentialitÃ©</a></p>
            </div>
          </div>
        </div>

        <p className="mt-6 text-sm text-gray-600">Si vous souhaitez exercer vos droits ou poser une question relative Ã  la protection des donnÃ©es, contactez-nous via <a href="mailto:contact@revedivoire.fr" className="underline">contact@revedivoire.fr</a>.</p>
      </div>
    </section>
  );
}

SectionSecuriteConfidentialite.propTypes = {
  privacyHref: PropTypes.string,
};
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages> cat .\don\DonPage.js
import React from 'react';

import Hero from './sections/Hero';
import Pourquoi from './sections/Pourquoi';
import FaireDon from './sections/FaireDon';
import Benevole from './sections/Benevole';
import Securite from './sections/Securite';
import FAQ from './sections/FAQ';


export default function AboutPage() {
  return (
    <>
      <main id="main">
        <Hero />
        <Pourquoi/>
        <FaireDon/>
        <Benevole/>
        <Securite/>
        <FAQ/>
      </main>
    </>
  );
}
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages> cat .\don\sections\*.js
import React, { useState, useRef } from 'react';
import PropTypes from 'prop-types';
import { Calendar, Users, Mail as MailIcon, CheckCircle2, AlertCircle } from 'lucide-react';

/**
 * SectionDevenirBenevole.js
 * Section â€” Devenir bÃ©nÃ©vole
 * - Mobile-first, accessible, full-width for desktop
 * - Vouvoyement dans tous les textes
 * - Champs : PrÃ©nom, Email (requis), DisponibilitÃ©s, CompÃ©tences, Autre (libre), Message
 * - Micro-copy, confirmation accessible (aria-live)
 */

export default function SectionDevenirBenevole({ apiEndpoint = '/api/benevole' }) {
  const [prenom, setPrenom] = useState('');
  const [email, setEmail] = useState('');
  const [dispo, setDispo] = useState('ponctuel');
  const [skills, setSkills] = useState({ vente: false, logistique: false, comm: false, autre: false });
  const [autreText, setAutreText] = useState('');
  const [message, setMessage] = useState('');
  const [loading, setLoading] = useState(false);
  const [status, setStatus] = useState(null); // { type: 'success'|'error', text }
  const liveRef = useRef(null);

  const emailValid = (e) => !!e && /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(e);

  const toggleSkill = (k) => setSkills(s => ({ ...s, [k]: !s[k] }));

  async function handleSubmit(e) {
    e.preventDefault();
    setStatus(null);

    if (!emailValid(email)) {
      setStatus({ type: 'error', text: 'Adresse eâ€‘mail invalide.' });
      liveRef.current?.focus?.();
      return;
    }

    // Build payload
    const payload = {
      prenom: prenom || null,
      email,
      disponibilite: dispo,
      competences: Object.keys(skills).filter(k => skills[k]).map(k => (k === 'comm' ? 'communication' : k === 'vente' ? 'vente' : k === 'logistique' ? 'logistique' : 'autre')),
      autre: skills.autre ? (autreText || null) : null,
      message: message || null,
      submittedAt: new Date().toISOString(),
    };

    setLoading(true);
    try {
      // If an API endpoint is provided, POST payload (else simulate success)
      if (apiEndpoint) {
        const res = await fetch(apiEndpoint, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
        if (!res.ok) {
          const txt = await res.text().catch(() => 'Erreur serveur');
          throw new Error(txt || 'Erreur serveur');
        }
      }

      setStatus({ type: 'success', text: "Merci ! Nous avons bien reÃ§u votre proposition. Nous vous contactons bientÃ´t." });
      // reset form
      setPrenom(''); setEmail(''); setDispo('ponctuel'); setSkills({ vente: false, logistique: false, comm: false, autre: false }); setAutreText(''); setMessage('');
    } catch (err) {
      setStatus({ type: 'error', text: `Impossible d'envoyer votre proposition : ${err.message}` });
    } finally {
      setLoading(false);
      liveRef.current?.focus?.();
    }
  }

  return (
    <section className="w-full bg-white py-16 px-4 sm:px-6 lg:px-12">
      <div className="max-w-7xl mx-auto">
        <div className="max-w-3xl mb-8">
          <h2 className="text-2xl sm:text-3xl font-extrabold text-gray-900">Devenir bÃ©nÃ©vole</h2>
          <p className="mt-3 text-gray-700">Vous pouvez donner un peu de temps pour aider, ponctuellement ou rÃ©guliÃ¨rement. Chaque coup de main permet de sauver des vÃªtements et de financer des actions solidaires.</p>
        </div>

        <form onSubmit={handleSubmit} className="grid grid-cols-1 lg:grid-cols-[1fr_360px] gap-8" noValidate>
          {/* Left: champs */}
          <div>
            <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
              <div>
                <label htmlFor="bv-prenom" className="block text-sm font-medium text-gray-700">PrÃ©nom</label>
                <input id="bv-prenom" type="text" placeholder="Ton prÃ©nom" value={prenom} onChange={e => setPrenom(e.target.value)} className="mt-1 block w-full border rounded-md px-3 py-2 focus:ring-2 focus:ring-[#7B2D2D]" />
              </div>

              <div>
                <label htmlFor="bv-email" className="block text-sm font-medium text-gray-700">Email <span className="text-red-600">*</span></label>
                <input id="bv-email" type="email" placeholder="Votre email pour vous contacter" value={email} onChange={e => setEmail(e.target.value)} required className="mt-1 block w-full border rounded-md px-3 py-2 focus:ring-2 focus:ring-[#7B2D2D]" />
              </div>
            </div>

            {/* DisponibilitÃ©s */}
            <fieldset className="mt-6">
              <legend className="text-sm font-medium text-gray-700">DisponibilitÃ©s</legend>
              <div className="mt-2 flex gap-3 items-center">
                <label className="flex items-center gap-2 text-sm">
                  <input type="radio" name="dispo" checked={dispo === 'ponctuel'} onChange={() => setDispo('ponctuel')} />
                  Ponctuel
                </label>
                <label className="flex items-center gap-2 text-sm">
                  <input type="radio" name="dispo" checked={dispo === 'regulier'} onChange={() => setDispo('regulier')} />
                  RÃ©gulier
                </label>
              </div>
            </fieldset>

            {/* CompÃ©tences */}
            <fieldset className="mt-6">
              <legend className="text-sm font-medium text-gray-700">CompÃ©tences / domaines d'aide</legend>
              <div className="mt-3 grid grid-cols-1 sm:grid-cols-2 gap-3">
                <label className="flex items-center gap-2">
                  <input type="checkbox" checked={skills.vente} onChange={() => toggleSkill('vente')} />
                  Vente
                </label>
                <label className="flex items-center gap-2">
                  <input type="checkbox" checked={skills.logistique} onChange={() => toggleSkill('logistique')} />
                  Logistique
                </label>
                <label className="flex items-center gap-2">
                  <input type="checkbox" checked={skills.comm} onChange={() => toggleSkill('comm')} />
                  Communication
                </label>
                <div className="flex items-start gap-2">
                  <input type="checkbox" checked={skills.autre} onChange={() => toggleSkill('autre')} />
                  <div className="flex-1">
                    <div className="text-sm">Autre</div>
                    <input type="text" placeholder="PrÃ©cisez (ex. couture, photo...)" value={autreText} onChange={e => setAutreText(e.target.value)} disabled={!skills.autre} className="mt-1 block w-full border rounded-md px-3 py-2" />
                  </div>
                </div>
              </div>
            </fieldset>

            {/* Message libre */}
            <div className="mt-6">
              <label htmlFor="bv-message" className="block text-sm font-medium text-gray-700">Message (optionnel)</label>
              <textarea id="bv-message" placeholder="Parlez-nous de vous et de ce que vous pouvez apporter" value={message} onChange={e => setMessage(e.target.value)} rows={5} className="mt-1 block w-full border rounded-md px-3 py-2" />
            </div>

            {/* CTA mobile visible */}
            <div className="sm:hidden mt-6">
              <button type="submit" className="w-full px-4 py-3 bg-[#7B2D2D] text-white rounded-md font-medium" aria-label="Je propose mon aide" disabled={loading}>Je propose mon aide</button>
              <p className="text-xs text-gray-500 mt-2">On vous recontacte rapidement â€¢ Engagement libre â€¢ Participation selon votre temps</p>
            </div>

          </div>

          {/* Right: rÃ©sumÃ© + CTA desktop sticky */}
          <aside className="lg:sticky top-28">
            <div className="bg-gray-50 border rounded-lg p-6">
              <div className="flex items-center gap-3 text-sm text-gray-700">
                <Users className="w-5 h-5" />
                <div>
                  <div className="font-medium">Pourquoi s'engager ?</div>
                  <div className="text-xs text-gray-600">Chaque coup de main a un impact immÃ©diat.</div>
                </div>
              </div>

              <dl className="mt-4 text-sm text-gray-700">
                <div className="flex justify-between py-1"><dt>DisponibilitÃ©</dt><dd className="text-gray-900">{dispo === 'ponctuel' ? 'Ponctuel' : 'RÃ©gulier'}</dd></div>
                <div className="flex justify-between py-1"><dt>CompÃ©tences</dt><dd className="text-gray-900">{Object.keys(skills).filter(k => skills[k]).map(k => (k === 'comm' ? 'Communication' : k === 'vente' ? 'Vente' : k === 'logistique' ? 'Logistique' : 'Autre')).join(', ') || 'Ã€ prÃ©ciser'}</dd></div>
              </dl>

              <div className="mt-6 hidden sm:block">
                <button type="submit" onClick={(e) => { document.querySelector('form')?.dispatchEvent(new Event('submit', { cancelable: true, bubbles: true })); }} className="w-full px-4 py-3 bg-[#7B2D2D] text-white rounded-md font-medium" aria-label="Je propose mon aide" disabled={loading}>Je propose mon aide</button>
                <p className="text-xs text-gray-500 mt-2">On vous recontacte rapidement â€¢ Engagement libre â€¢ Participation selon votre temps</p>
              </div>

              {/* Live status */}
              <div tabIndex={-1} ref={liveRef} aria-live="polite" className="mt-4">
                {status?.type === 'success' && (
                  <div className="flex items-center gap-2 text-green-700"><CheckCircle2 className="w-4 h-4" />{status.text}</div>
                )}
                {status?.type === 'error' && (
                  <div className="flex items-center gap-2 text-red-700"><AlertCircle className="w-4 h-4" />{status.text}</div>
                )}
              </div>

            </div>
          </aside>

        </form>

      </div>
    </section>
  );
}

SectionDevenirBenevole.propTypes = {
  apiEndpoint: PropTypes.string,
};
import React, { useState } from 'react';
import { CreditCard, Building2, Mail, Package, CheckCircle2, AlertCircle } from 'lucide-react';

export default function SectionFaireUnDon() {
  const [amount, setAmount] = useState(20);
  const [customAmount, setCustomAmount] = useState('');
  const [method, setMethod] = useState('card');
  const [status, setStatus] = useState(null); // success | error | null

  const suggested = [
    { value: 10, label: 'Sauver une piÃ¨ce et lui donner une seconde vie' },
    { value: 20, label: 'PrÃ©parer plusieurs vÃªtements pour une action solidaire' },
    { value: 50, label: 'Soutenir une action concrÃ¨te, ici ou sur le terrain' },
    { value: 100, label: 'Avoir un impact fort et durable' },
  ];

  const finalAmount = customAmount ? Number(customAmount) : amount;

  const handleSubmit = e => {
    e.preventDefault();
    if (!finalAmount || finalAmount <= 0) {
      setStatus('error');
      return;
    }
    // TODO: intÃ©grer Stripe / logique backend
    setStatus('success');
  };

  return (
    <section className="w-full bg-gray-50 py-16 px-4 sm:px-6 lg:px-12">
      <div className="max-w-4xl mx-auto">
        {/* Titre */}
        <h2 className="text-2xl sm:text-3xl font-extrabold text-gray-900 mb-4">
          Faire un don
        </h2>
        <p className="text-gray-700 mb-10">
          Choisissez le montant et la mÃ©thode qui vous conviennent.<br />
          Chaque don finance directement des actions solidaires concrÃ¨tes.
        </p>

        <form onSubmit={handleSubmit} className="space-y-10">
          {/* Montants suggÃ©rÃ©s */}
          <div className="space-y-4">
            <h3 className="font-semibold">Montant du don</h3>
            <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
              {suggested.map(item => (
                <button
                  key={item.value}
                  type="button"
                  onClick={() => { setAmount(item.value); setCustomAmount(''); }}
                  className={`w-full text-left border rounded-lg p-4 ${amount === item.value && !customAmount ? 'border-[#7B2D2D] bg-white' : 'bg-white'}`}
                >
                  <div className="text-lg font-bold">{item.value} â‚¬</div>
                  <div className="text-sm text-gray-600">{item.label}</div>
                </button>
              ))}
            </div>
          </div>

          {/* Montant libre */}
          <div>
            <label className="block font-semibold mb-1">Montant libre (â‚¬)</label>
            <input
              type="number"
              min="1"
              placeholder="Je donne ce que je peux"
              value={customAmount}
              onChange={e => setCustomAmount(e.target.value)}
              className="w-full border rounded-md px-3 py-2"
            />
            <p className="text-xs text-gray-500 mt-1">Chaque euro compte vraiment.</p>
          </div>

          {/* MÃ©thodes de don */}
          <div className="space-y-3">
            <h3 className="font-semibold">MÃ©thode de don</h3>
            <div className="space-y-2">
              <label className="flex items-center gap-3 border rounded-lg p-3 cursor-pointer">
                <input type="radio" checked={method === 'card'} onChange={() => setMethod('card')} />
                <CreditCard className="w-4 h-4" /> Carte bancaire (paiement sÃ©curisÃ© via Stripe)
              </label>
              <label className="flex items-center gap-3 border rounded-lg p-3 cursor-pointer">
                <input type="radio" checked={method === 'transfer'} onChange={() => setMethod('transfer')} />
                <Building2 className="w-4 h-4" /> Virement bancaire
              </label>
              <label className="flex items-center gap-3 border rounded-lg p-3 cursor-pointer">
                <input type="radio" checked={method === 'check'} onChange={() => setMethod('check')} />
                <Mail className="w-4 h-4" /> ChÃ¨que
              </label>
              <label className="flex items-center gap-3 border rounded-lg p-3 cursor-pointer">
                <input type="radio" checked={method === 'nature'} onChange={() => setMethod('nature')} />
                <Package className="w-4 h-4" /> Don en nature (vÃªtements)
              </label>
            </div>
            <p className="text-xs text-gray-500">Les instructions sont envoyÃ©es aprÃ¨s validation.</p>
          </div>

          {/* CTA */}
          <div className="space-y-3">
            <button
              type="submit"
              aria-label="Valider mon don"
              className="w-full px-6 py-4 rounded-md bg-[#7B2D2D] text-white font-semibold"
            >
              Valider mon don {finalAmount ? `â€” ${finalAmount} â‚¬` : ''}
            </button>
            <p className="text-xs text-gray-500 text-center">
              Paiement sÃ©curisÃ© â€¢ Don sans engagement â€¢ ReÃ§u fiscal si applicable
            </p>
          </div>

          {/* Ã‰tats */}
          {status === 'success' && (
            <div className="flex items-center gap-2 text-green-700 text-sm">
              <CheckCircle2 className="w-4 h-4" />
              Merci pour votre don. Un email de confirmation vous a Ã©tÃ© envoyÃ©.
            </div>
          )}
          {status === 'error' && (
            <div className="flex items-center gap-2 text-red-700 text-sm">
              <AlertCircle className="w-4 h-4" />
              Le paiement nâ€™a pas abouti. Aucun montant nâ€™a Ã©tÃ© dÃ©bitÃ©.
            </div>
          )}
        </form>
      </div>
    </section>
  );
}
import React, { useState, useEffect, useRef } from 'react';
import PropTypes from 'prop-types';
import { ChevronDown, Phone } from 'lucide-react';

/**
 * SectionFAQDonBenevole.js
 * Section â€” FAQ Don & BÃ©nÃ©volat
 * - AccordÃ©on accessible (1 question ouverte Ã  la fois)
 * - Vouvoyement dans tous les textes
 * - Mobile-first, full-width
 */

const FAQ_ITEMS = [
  {
    group: 'Don',
    items: [
      {
        q: 'Le paiement estâ€‘il sÃ©curisÃ© ?',
        a: (
          <>
            <p>Oui. Les paiements par carte bancaire sont traitÃ©s via Stripe, une plateforme sÃ©curisÃ©e et reconnue. Aucune donnÃ©e bancaire nâ€™est stockÃ©e par Frip2RÃªve.</p>
          </>
        ),
      },
      {
        q: 'Puisâ€‘je donner sans carte bancaire ?',
        a: (
          <>
            <p>Oui. Vous pouvez faire un don par virement bancaire, par chÃ¨que ou en don en nature (vÃªtements).</p>
          </>
        ),
      },
      {
        q: 'Puisâ€‘je donner un petit montant ?',
        a: (
          <>
            <p>Oui. Chaque don, mÃªme modeste, a un impact rÃ©el sur nos actions solidaires.</p>
          </>
        ),
      },
      {
        q: 'Vaisâ€‘je recevoir un reÃ§u ?',
        a: (
          <>
            <p>Un reÃ§u ou justificatif est envoyÃ© par email aprÃ¨s validation du don. Un reÃ§u fiscal est fourni si applicable.</p>
          </>
        ),
      },
    ],
  },
  {
    group: 'BÃ©nÃ©volat',
    items: [
      {
        q: "Fautâ€‘il sâ€™engager sur la durÃ©e ?",
        a: (
          <>
            <p>Non. Vous pouvez aider ponctuellement ou rÃ©guliÃ¨rement, selon votre temps et vos disponibilitÃ©s.</p>
          </>
        ),
      },
      {
        q: 'Quelles compÃ©tences sont utiles ?',
        a: (
          <>
            <p>Vente, logistique, communication ou aide gÃ©nÃ©rale. Toutes les bonnes volontÃ©s sont les bienvenues.</p>
          </>
        ),
      },
      {
        q: 'OÃ¹ se dÃ©roulent les actions ?',
        a: (
          <>
            <p>Les actions ont lieu principalement en France (collectes, ventes, logistique) et sur des projets terrain en CÃ´te dâ€™Ivoire.</p>
          </>
        ),
      },
      {
        q: 'Comment se passe le premier contact ?',
        a: (
          <>
            <p>AprÃ¨s votre inscription, nous vous recontactons par email pour vous expliquer la suite, simplement.</p>
          </>
        ),
      },
    ],
  },
];

export default function SectionFAQDonBenevole({ whatsappNumber = '33123456789' }) {
  const [open, setOpen] = useState({ group: 0, index: 0 }); // group idx, question idx (one open at a time)
  const controlRefs = useRef([]);
  const containerRef = useRef(null);

  // keyboard navigation within the accordion: up/down to move between questions, enter/space to toggle
  useEffect(() => {
    function onKey(e) {
      if (!containerRef.current) return;
      if (!containerRef.current.contains(document.activeElement)) return;

      const flat = controlRefs.current.filter(Boolean);
      const idx = flat.indexOf(document.activeElement);
      if (idx === -1) return;

      if (e.key === 'ArrowDown') {
        e.preventDefault();
        const next = (idx + 1) % flat.length;
        flat[next].focus();
      }
      if (e.key === 'ArrowUp') {
        e.preventDefault();
        const prev = (idx - 1 + flat.length) % flat.length;
        flat[prev].focus();
      }
    }
    window.addEventListener('keydown', onKey);
    return () => window.removeEventListener('keydown', onKey);
  }, []);

  const handleToggle = (gIdx, qIdx) => {
    setOpen(prev => (prev.group === gIdx && prev.index === qIdx ? { group: -1, index: -1 } : { group: gIdx, index: qIdx }));
  };

  const whatsappHref = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent('Bonjour Frip2RÃªve â€” Jâ€™ai une question au sujet des dons / du bÃ©nÃ©volat.')}`;

  return (
    <section className="w-full bg-white py-16 px-4 sm:px-6 lg:px-12" aria-labelledby="faq-don-title" ref={containerRef}>
      <div className="max-w-7xl mx-auto">
        <header className="max-w-3xl mb-8">
          <h2 id="faq-don-title" className="text-2xl sm:text-3xl font-extrabold text-gray-900">Questions frÃ©quentes</h2>
          <p className="mt-2 text-gray-600">RÃ©ponses claires pour lever les doutes avant de donner ou de proposer votre aide.</p>
        </header>

        <div className="space-y-8">
          {FAQ_ITEMS.map((group, gIdx) => (
            <div key={group.group}>
              <h3 className="text-lg font-semibold mb-3">{group.group}</h3>

              <div className="border rounded-md overflow-hidden">
                {group.items.map((it, qIdx) => {
                  const isOpen = open.group === gIdx && open.index === qIdx;
                  const controlId = `faq-${gIdx}-${qIdx}-control`;
                  const panelId = `faq-${gIdx}-${qIdx}-panel`;
                  const refIndex = controlRefs.current.length;

                  return (
                    <div key={it.q} className="">
                      <button
                        id={controlId}
                        ref={el => { controlRefs.current[refIndex] = el; }}
                        aria-expanded={isOpen}
                        aria-controls={panelId}
                        onClick={() => handleToggle(gIdx, qIdx)}
                        className="w-full flex items-center justify-between px-4 py-4 text-left bg-white hover:bg-gray-50 focus:outline-none focus-visible:ring"
                      >
                        <span className="font-medium text-gray-800">{it.q}</span>
                        <ChevronDown className={`w-5 h-5 transform transition-transform ${isOpen ? 'rotate-180' : 'rotate-0'}`} aria-hidden />
                      </button>

                      <div id={panelId} role="region" aria-labelledby={controlId} className={`${isOpen ? 'block' : 'hidden'} px-4 pb-4 text-sm text-gray-700 bg-white border-t`}>
                        {it.a}
                      </div>
                    </div>
                  );
                })}
              </div>
            </div>
          ))}
        </div>

        <div className="mt-8 text-center">
          <p className="font-medium mb-3">Une question supplÃ©mentaire ?</p>
          <a href={whatsappHref} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 px-5 py-3 bg-[#7B2D2D] text-white rounded-md" aria-label="Contacter Frip2RÃªve via WhatsApp">
            <Phone className="w-4 h-4" /> Contacter via WhatsApp
          </a>
          <p className="text-xs text-gray-500 mt-2">RÃ©ponse humaine â€¢ Rapide â€¢ Sans engagement</p>
        </div>
      </div>
    </section>
  );
}

SectionFAQDonBenevole.propTypes = {
  whatsappNumber: PropTypes.string,
};
import React from 'react';
import PropTypes from 'prop-types';
import { CreditCard, Users, ShieldCheck, HeartHandshake } from 'lucide-react';

/**
 * HeroDon.js â€” HERO Faire un don / Devenir bÃ©nÃ©vole
 * Desktop : 100vh
 * Mobile  : 70â€“75vh
 * CTA visibles sans scroller
 */

export default function HeroDon({
  title = 'Aidez-nous Ã  agir, dÃ¨s aujourdâ€™hui.',
  subtitle = 'Un don ou un peu de temps suffit pour soutenir des actions solidaires concrÃ¨tes en CÃ´te dâ€™Ivoire et en France.',
  primaryCta = { label: 'Faire un don', href: '/don/faire' },
  secondaryCta = { label: 'Devenir bÃ©nÃ©vole', href: '/don/benevole' },
  imageSrc = 'https://picsum.photos/seed/frip2reve-don/1600/900',
  imageAlt = 'Actions solidaires Frip2RÃªve â€” bÃ©nÃ©voles et terrain',
}) {
  return (
    <header className="relative w-full" aria-label="Hero Faire un don et bÃ©nÃ©volat">
      {/* Full width / full height image */}
      <div className="relative h-[72vh] lg:h-screen w-screen left-1/2 right-1/2 -ml-[50vw] -mr-[50vw] overflow-hidden">
        <img
          src={imageSrc}
          alt={imageAlt}
          className="absolute inset-0 w-full h-full object-cover"
          loading="eager"
        />

        {/* Overlay sombre lÃ©ger */}
        <div className="absolute inset-0 bg-black/45" />

        {/* Contenu */}
        <div className="absolute inset-0 flex items-center">
          <div className="max-w-7xl mx-auto px-6 sm:px-8 lg:px-12 w-full">
            <div className="max-w-2xl text-white">
              <h1 className="text-2xl sm:text-3xl lg:text-4xl font-extrabold leading-tight">
                {title}
              </h1>

              <p className="mt-4 text-sm sm:text-base lg:text-lg text-white/90">
                {subtitle}
              </p>

              {/* CTAs */}
              <div className="mt-6 flex flex-col sm:flex-row gap-3">
                <a
                  href={primaryCta.href}
                  aria-label="Faire un don Ã  Frip2RÃªve"
                  className="inline-flex justify-center items-center gap-3 px-5 py-3 rounded-md bg-[#7B2D2D] text-white font-semibold focus:outline-none focus:ring-2 focus:ring-white/40"
                >
                  <CreditCard className="w-5 h-5" />
                  {primaryCta.label}
                </a>

                <a
                  href={secondaryCta.href}
                  aria-label="Devenir bÃ©nÃ©vole chez Frip2RÃªve"
                  className="inline-flex justify-center items-center gap-3 px-5 py-3 rounded-md border border-white/40 bg-white/10 text-white font-medium hover:bg-white/15"
                >
                  <Users className="w-5 h-5" />
                  {secondaryCta.label}
                </a>
              </div>

              {/* Micro-rÃ©assurance */}
              <p className="mt-3 text-xs sm:text-sm text-white/85">
                Paiement sÃ©curisÃ© â€¢ Engagement libre â€¢ Impact rÃ©el
              </p>

              {/* Preuves rapides */}
              <div className="mt-6 flex flex-wrap gap-x-6 gap-y-3 text-xs sm:text-sm text-white/90">
                <div className="flex items-center gap-2">
                  <HeartHandshake className="w-4 h-4" />
                  Actions solidaires concrÃ¨tes
                </div>
                <div className="flex items-center gap-2">
                  <Users className="w-4 h-4" />
                  Association Ã  but non lucratif
                </div>
                <div className="flex items-center gap-2">
                  <ShieldCheck className="w-4 h-4" />
                  ReÃ§u fiscal si applicable
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </header>
  );
}

HeroDon.propTypes = {
  title: PropTypes.string,
  subtitle: PropTypes.string,
  primaryCta: PropTypes.shape({ label: PropTypes.string, href: PropTypes.string }),
  secondaryCta: PropTypes.shape({ label: PropTypes.string, href: PropTypes.string }),
  imageSrc: PropTypes.string,
  imageAlt: PropTypes.string,
};
import React from 'react';
import { CreditCard, Users, Sparkles, HeartHandshake } from 'lucide-react';

export default function SectionPourquoiDon() {
  return (
    <section className="w-full bg-white py-16 px-4 sm:px-6 lg:px-12">
      <div className="max-w-7xl mx-auto">
        {/* Titre */}
        <div className="max-w-3xl mb-10">
          <h2 className="text-2xl sm:text-3xl font-extrabold text-gray-900">
            Votre aide change des choses. Vraiment.
          </h2>
          <p className="mt-4 text-gray-700">
            Ce ne sont pas des promesses.<br />
            Ce sont des actions concrÃ¨tes rendues possibles par des personnes comme vous.
          </p>
        </div>

        {/* Impact immÃ©diat */}
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-12">
          <div className="rounded-xl border p-5">
            <div className="text-3xl font-extrabold text-[#7B2D2D]">10 â‚¬</div>
            <p className="mt-2 text-sm text-gray-700">
              Un vÃªtement africain rÃ©cupÃ©rÃ©, nettoyÃ© et remis Ã  neuf.
            </p>
          </div>
          <div className="rounded-xl border p-5">
            <div className="text-3xl font-extrabold text-[#7B2D2D]">30 â‚¬</div>
            <p className="mt-2 text-sm text-gray-700">
              Plusieurs piÃ¨ces prÃªtes Ã  Ãªtre vendues pour financer une action solidaire.
            </p>
          </div>
          <div className="rounded-xl border p-5">
            <div className="text-3xl font-extrabold text-[#7B2D2D]">60 â‚¬</div>
            <p className="mt-2 text-sm text-gray-700">
              Une action locale soutenue directement.
            </p>
          </div>
          <div className="rounded-xl border p-5">
            <div className="text-3xl font-extrabold text-[#7B2D2D]">1 bÃ©nÃ©vole</div>
            <p className="mt-2 text-sm text-gray-700">
              Des dizaines de vÃªtements sauvÃ©s et redistribuÃ©s.
            </p>
          </div>
        </div>

        <p className="text-sm text-gray-500 mb-10">
          Vous voyez exactement oÃ¹ va votre aide.
        </p>

        {/* Urgence douce */}
        <div className="max-w-3xl mb-12">
          <p className="text-gray-800">
            Chaque jour sans aide, ce sont des vÃªtements perdus et des actions repoussÃ©es.<br />
            Chaque don ou coup de main permet dâ€™agir tout de suite.
          </p>
        </div>

        {/* Donner ou sâ€™engager */}
        <div className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-12">
          <div className="rounded-2xl bg-gray-50 p-6">
            <h3 className="text-lg font-bold mb-3">Faire un don</h3>
            <ul className="space-y-2 text-sm text-gray-700">
              <li className="flex items-center gap-2"><Sparkles className="w-4 h-4" /> Impact immÃ©diat</li>
              <li className="flex items-center gap-2"><CreditCard className="w-4 h-4" /> En 30 secondes</li>
              <li className="flex items-center gap-2"><HeartHandshake className="w-4 h-4" /> 100 % utile</li>
            </ul>
          </div>
          <div className="rounded-2xl bg-gray-50 p-6">
            <h3 className="text-lg font-bold mb-3">Devenir bÃ©nÃ©vole</h3>
            <ul className="space-y-2 text-sm text-gray-700">
              <li className="flex items-center gap-2"><Users className="w-4 h-4" /> ZÃ©ro obligation</li>
              <li className="flex items-center gap-2"><Users className="w-4 h-4" /> Selon votre temps</li>
              <li className="flex items-center gap-2"><HeartHandshake className="w-4 h-4" /> Impact humain direct</li>
            </ul>
          </div>
        </div>

        {/* Texte fort */}
        <p className="text-center font-semibold text-gray-900 mb-8">
          Pas besoin de faire beaucoup. Il faut juste commencer.
        </p>

        {/* Preuve sociale */}
        <p className="text-center text-sm text-gray-600 mb-10">
          DÃ©jÃ  soutenu par des donateurs et bÃ©nÃ©voles engagÃ©s.
        </p>
      </div>
    </section>
  );
}
import React from 'react';
import PropTypes from 'prop-types';
import { ShieldCheck, CreditCard, FileText, Eye, CheckCircle2 } from 'lucide-react';

/**
 * Section â€” SÃ©curitÃ© & transparence
 * - Mobile-first, full-width
 * - Vouvoyement partout
 */

export default function SectionSecuriteTransparence({ donateHref = '/don#form', privacyHref = '/politique-confidentialite' }) {
  return (
    <section className="w-full bg-white py-16 px-4 sm:px-6 lg:px-12" aria-labelledby="securite-title">
      <div className="max-w-7xl mx-auto">
        <header className="max-w-3xl mb-8">
          <h2 id="securite-title" className="text-2xl sm:text-3xl font-extrabold text-gray-900">SÃ©curitÃ© et transparence</h2>
          <p className="mt-3 text-gray-700 text-base">Chez Frip2RÃªve, chaque don et chaque engagement bÃ©nÃ©vole sont gÃ©rÃ©s avec sÃ©rieux. Voici comment nous protÃ©geons vos donnÃ©es et garantissons lâ€™usage correct de votre soutien.</p>
        </header>

        <div className="grid grid-cols-1 lg:grid-cols-4 gap-6">
          {/* Bloc 1 â€” DonnÃ©es personnelles */}
          <div className="rounded-lg border p-5 bg-gray-50">
            <div className="flex items-start gap-3">
              <ShieldCheck className="w-6 h-6 text-[#7B2D2D] flex-shrink-0" aria-hidden />
              <div>
                <h3 className="font-semibold">DonnÃ©es personnelles</h3>
                <p className="mt-2 text-sm text-gray-700">ConformitÃ© RGPD â€” vos informations sont sÃ©curisÃ©es et utilisÃ©es uniquement pour le traitement de votre don ou de votre proposition de bÃ©nÃ©volat.</p>
                <p className="mt-2 text-sm text-gray-600">Vous pouvez demander la suppression de vos donnÃ©es Ã  tout moment.</p>
                <a href={privacyHref} className="mt-3 inline-block text-sm text-indigo-600 underline">Voir la politique de confidentialitÃ©</a>
              </div>
            </div>
          </div>

          {/* Bloc 2 â€” Paiement sÃ©curisÃ© */}
          <div className="rounded-lg border p-5 bg-gray-50">
            <div className="flex items-start gap-3">
              <CreditCard className="w-6 h-6 text-[#7B2D2D] flex-shrink-0" aria-hidden />
              <div>
                <h3 className="font-semibold">Paiement sÃ©curisÃ©</h3>
                <p className="mt-2 text-sm text-gray-700">Carte bancaire via Stripe â€” sÃ©curisÃ© et conforme PCI DSS.</p>
                <p className="mt-2 text-sm text-gray-700">Virement et chÃ¨que â€” instructions claires et suivi manuel. Don en nature â€” modalitÃ©s validÃ©es par Frip2RÃªve.</p>
                <p className="mt-2 text-sm text-gray-600">Aucun don nâ€™est prÃ©levÃ© avant confirmation.</p>
              </div>
            </div>
          </div>

          {/* Bloc 3 â€” Utilisation des fonds */}
          <div className="rounded-lg border p-5 bg-gray-50">
            <div className="flex items-start gap-3">
              <FileText className="w-6 h-6 text-[#7B2D2D] flex-shrink-0" aria-hidden />
              <div>
                <h3 className="font-semibold">Utilisation transparente des fonds</h3>
                <p className="mt-2 text-sm text-gray-700">Chaque don est affectÃ© directement aux actions solidaires. Des rapports simplifiÃ©s sont disponibles sur demande pour les donateurs.</p>
                <p className="mt-2 text-sm text-gray-700">Un reÃ§u fiscal vous sera fourni si votre don le permet.</p>
                <p className="mt-2 text-sm text-gray-600">Vous savez exactement oÃ¹ va votre aide.</p>
              </div>
            </div>
          </div>

          {/* Bloc 4 â€” Rassurance visuelle */}
          <div className="rounded-lg border p-5 bg-gray-50">
            <div className="flex items-start gap-3">
              <Eye className="w-6 h-6 text-[#7B2D2D] flex-shrink-0" aria-hidden />
              <div>
                <h3 className="font-semibold">Rassurances</h3>
                <ul className="mt-2 text-sm text-gray-700 space-y-2">
                  <li className="flex items-center gap-2"><CheckCircle2 className="w-4 h-4 text-green-600" aria-hidden /> Paiement sÃ©curisÃ©</li>
                  <li className="flex items-center gap-2"><CheckCircle2 className="w-4 h-4 text-green-600" aria-hidden /> Don transparent</li>
                  <li className="flex items-center gap-2"><CheckCircle2 className="w-4 h-4 text-green-600" aria-hidden /> RGPD respectÃ©</li>
                  <li className="flex items-center gap-2"><CheckCircle2 className="w-4 h-4 text-green-600" aria-hidden /> ReÃ§u fiscal possible</li>
                </ul>
              </div>
            </div>
          </div>
        </div>

        {/* CTA */}
        <div className="mt-10 text-center">
          <a href={donateHref} className="inline-block px-6 py-3 bg-[#7B2D2D] text-white rounded-md font-semibold" aria-label="Retour au formulaire de don">Je fais un don maintenant</a>
        </div>
      </div>
    </section>
  );
}

SectionSecuriteTransparence.propTypes = {
  donateHref: PropTypes.string,
  privacyHref: PropTypes.string,
};
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages> cat .\Frip2Reve\Frip2RevePage.js
import React from 'react';

import Hero from './sections/Hero';
import Collections from './sections/Collections';
import Produits from './sections/Produits';
import FAQ from './sections/FAQ';

export default function AboutPage() {
  return (
    <>
      <main id="main">
        <Hero />
        <Collections/>
        <Produits/>
        <FAQ/>
      </main>
    </>
  );
}
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages> cat .\Frip2Reve\sections\*.js
// src/pages/Frip2Reve/sections/Collections.js
import React, { useMemo, useState, useEffect } from "react";
import PropTypes from "prop-types";

/**
 * Collections â€” Section Collections & CatÃ©gories (mobile-first)
 * Objectif: aider rapidement un visiteur Ã  trouver une piÃ¨ce disponible.
 * - Filtres visibles (chips) : Taille, Style, Retrait
 * - Mobile: bouton "Filtrer" -> bottom-sheet
 * - Grille responsive: 2 cols mobile / 3-4 desktop
 * - Cartes produits avec badges (Disponible / RÃ©servÃ© / Vendu)
 * - CTA principal sur mobile plein largeur, sticky "Filtrer"
 * - AccessibilitÃ©: labels, focus-visible, aria-pressed pour chips
 * - SEO: H2 unique, contenu textuel
 *
 * Note d'intÃ©gration:
 *  - Ajuster `whatsappNumber` pour la redirection Commander.
 *  - Passer la prop `items` pour connecter Ã  un backend ou CMS.
 */

const SIZES = ["XS", "S", "M", "L", "XL", "XXL", "Taille unique"];
const STYLES = ["Traditionnel", "Moderne", "Mixte", "Enfant", "Accessoires"];
const LOCATIONS = ["Beauvais", "Autre point de retrait", "Livraison possible"];

function toggleSet(set, value) {
  const n = new Set(set);
  if (n.has(value)) n.delete(value);
  else n.add(value);
  return n;
}

export default function Collections({ items = defaultItems, whatsappNumber = "33123456789" }) {
  // filtres temporaires (non appliquÃ©s tant que l'utilisateur n'a pas cliquÃ© sur Appliquer sur mobile)
  const [sizeFilter, setSizeFilter] = useState(new Set());
  const [styleFilter, setStyleFilter] = useState(new Set());
  const [locFilter, setLocFilter] = useState(new Set());

  // version temp pendant Ã©dition mobile
  const [tempSize, setTempSize] = useState(new Set());
  const [tempStyle, setTempStyle] = useState(new Set());
  const [tempLoc, setTempLoc] = useState(new Set());

  const [mobileOpen, setMobileOpen] = useState(false);

  // compteur et filtered list (temps rÃ©el pour desktop; pour mobile on applique Ã  la validation)
  const filtered = useMemo(() => {
    return items.filter((it) => {
      // must be available to show
      if (!it.available) return false;
      if (sizeFilter.size && sizeFilter.size > 0 && !sizeFilter.has(it.size)) return false;
      if (styleFilter.size && styleFilter.size > 0 && !styleFilter.has(it.style)) return false;
      if (locFilter.size && locFilter.size > 0 && !locFilter.has(it.location)) return false;
      return true;
    });
  }, [items, sizeFilter, styleFilter, locFilter]);

  // Mobile: copy current filters into temp when opening
  const openMobile = () => {
    setTempSize(new Set(sizeFilter));
    setTempStyle(new Set(styleFilter));
    setTempLoc(new Set(locFilter));
    setMobileOpen(true);
    document.body.style.overflow = "hidden"; // lock scroll
  };
  const closeMobile = () => {
    setMobileOpen(false);
    document.body.style.overflow = "";
  };

  const applyMobileFilters = () => {
    setSizeFilter(new Set(tempSize));
    setStyleFilter(new Set(tempStyle));
    setLocFilter(new Set(tempLoc));
    closeMobile();
  };

  const resetAll = () => {
    setSizeFilter(new Set());
    setStyleFilter(new Set());
    setLocFilter(new Set());
    setTempSize(new Set());
    setTempStyle(new Set());
    setTempLoc(new Set());
  };

  const removeChip = (kind, value) => {
    if (kind === "size") setSizeFilter((s) => { const n = new Set(s); n.delete(value); return n; });
    if (kind === "style") setStyleFilter((s) => { const n = new Set(s); n.delete(value); return n; });
    if (kind === "loc") setLocFilter((s) => { const n = new Set(s); n.delete(value); return n; });
  };

  // sticky bottom filter button on mobile: only show when not open
  useEffect(() => {
    return () => { document.body.style.overflow = ""; };
  }, []);

  return (
    <section aria-labelledby="collections-h2" className="bg-white text-gray-900">
      <div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header + info */}
        <div className="flex items-start justify-between gap-4">
          <div>
            <h2 id="collections-h2" className="text-2xl sm:text-3xl font-extrabold">Trouve la piÃ¨ce qui te correspond</h2>
            <p className="mt-2 text-sm sm:text-base text-gray-600">Filtre par taille, style ou lieu de retrait pour voir uniquement ce qui est dispo maintenant.</p>
            <p className="mt-1 text-xs text-gray-500">Seules les piÃ¨ces disponibles sâ€™affichent.</p>
          </div>

          <div className="hidden sm:flex sm:items-center sm:gap-3">
            <button
              type="button"
              className="px-4 py-2 rounded-md border text-sm focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500"
              onClick={resetAll}
            >
              RÃ©initialiser
            </button>
            <div className="text-sm text-gray-700">{filtered.length} piÃ¨ces disponibles</div>
          </div>
        </div>

        {/* Filters (desktop visible) */}
        <div className="mt-6 hidden sm:block">
          <div className="flex items-center gap-4 flex-wrap">
            {/* Taille */}
            <div aria-label="Filtrer par taille" className="flex items-center gap-2 flex-wrap">
              {SIZES.map((s) => (
                <button
                  key={s}
                  type="button"
                  onClick={() => setSizeFilter((cur) => toggleSet(cur, s))}
                  className={`px-3 py-1 rounded-full text-sm border focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500 ${sizeFilter.has(s) ? 'bg-indigo-600 text-white border-indigo-600' : 'bg-white text-gray-700'}`}
                  aria-pressed={sizeFilter.has(s)}
                >
                  {s}
                </button>
              ))}
            </div>

            {/* Style */}
            <div aria-label="Filtrer par style" className="flex items-center gap-2 flex-wrap">
              {STYLES.map((st) => (
                <button
                  key={st}
                  type="button"
                  onClick={() => setStyleFilter((cur) => toggleSet(cur, st))}
                  className={`px-3 py-1 rounded-full text-sm border focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500 ${styleFilter.has(st) ? 'bg-indigo-600 text-white border-indigo-600' : 'bg-white text-gray-700'}`}
                  aria-pressed={styleFilter.has(st)}
                >
                  {st}
                </button>
              ))}
            </div>

            {/* Location */}
            <div aria-label="Filtrer par retrait" className="flex items-center gap-2 flex-wrap">
              {LOCATIONS.map((loc) => (
                <button
                  key={loc}
                  type="button"
                  onClick={() => setLocFilter((cur) => toggleSet(cur, loc))}
                  className={`px-3 py-1 rounded-full text-sm border focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500 ${locFilter.has(loc) ? 'bg-indigo-600 text-white border-indigo-600' : 'bg-white text-gray-700'}`}
                  aria-pressed={locFilter.has(loc)}
                >
                  {loc}
                </button>
              ))}
            </div>

            {/* Active chips */}
            <div className="ml-auto flex items-center gap-2">
              {Array.from(sizeFilter).map((v) => (
                <button key={`chip-size-${v}`} onClick={() => removeChip('size', v)} className="inline-flex items-center gap-2 px-3 py-1 bg-gray-100 rounded-full text-sm">
                  {v} <span className="sr-only">â€” retirer filtre taille</span>
                </button>
              ))}
              {Array.from(styleFilter).map((v) => (
                <button key={`chip-style-${v}`} onClick={() => removeChip('style', v)} className="inline-flex items-center gap-2 px-3 py-1 bg-gray-100 rounded-full text-sm">
                  {v} <span className="sr-only">â€” retirer filtre style</span>
                </button>
              ))}
              {Array.from(locFilter).map((v) => (
                <button key={`chip-loc-${v}`} onClick={() => removeChip('loc', v)} className="inline-flex items-center gap-2 px-3 py-1 bg-gray-100 rounded-full text-sm">
                  {v} <span className="sr-only">â€” retirer filtre retrait</span>
                </button>
              ))}
            </div>
          </div>
        </div>

        {/* Mobile sticky Filter button */}
        <div className="sm:hidden">
          <div className="mt-4">
            <div className="flex items-center gap-3">
              <button onClick={openMobile} className="flex-1 px-4 py-3 rounded-md bg-indigo-600 text-white text-base font-medium focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500">Filtrer</button>
              <button onClick={resetAll} className="px-4 py-3 rounded-md border text-base">RÃ©initialiser</button>
            </div>
            <div className="mt-3 text-sm text-gray-700">{filtered.length} piÃ¨ces disponibles</div>
          </div>
        </div>

        {/* Grid */}
        <div className="mt-6">
          {filtered.length === 0 ? (
            <div className="text-center py-12">
              <p className="text-base text-gray-600">Aucune piÃ¨ce ne correspond Ã  ces filtres pour le moment.</p>
              <a href="#" onClick={(e) => { e.preventDefault(); resetAll(); }} className="mt-4 inline-block text-indigo-600 underline">Voir toutes les piÃ¨ces</a>
            </div>
          ) : (
            <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4">
              {filtered.map((it) => (
                <ProductCard key={it.id} item={it} whatsappNumber={whatsappNumber} />
              ))}
            </div>
          )}
        </div>
      </div>

      {/* Mobile bottom-sheet */}
      {mobileOpen && (
        <div className="fixed inset-0 z-50 sm:hidden" role="dialog" aria-modal="true">
          <div className="absolute inset-0 bg-black opacity-40" onClick={closeMobile} />
          <div className="absolute left-0 right-0 bottom-0 bg-white rounded-t-lg p-4 max-h-[85vh] overflow-auto">
            <div className="flex items-center justify-between">
              <h3 className="text-lg font-medium">Filtrer</h3>
              <button onClick={closeMobile} className="px-3 py-2 rounded focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500">Fermer</button>
            </div>

            <div className="mt-4 space-y-4">
              <div>
                <div className="text-sm font-semibold">Taille</div>
                <div className="mt-2 grid grid-cols-3 gap-2">
                  {SIZES.map((s) => (
                    <button
                      key={s}
                      onClick={() => setTempSize((cur) => toggleSet(cur, s))}
                      className={`px-3 py-2 rounded-md text-base w-full ${tempSize.has(s) ? 'bg-indigo-600 text-white' : 'bg-white border'}`}
                      aria-pressed={tempSize.has(s)}
                    >{s}</button>
                  ))}
                </div>
              </div>

              <div>
                <div className="text-sm font-semibold">Style</div>
                <div className="mt-2 grid grid-cols-2 gap-2">
                  {STYLES.map((st) => (
                    <button
                      key={st}
                      onClick={() => setTempStyle((cur) => toggleSet(cur, st))}
                      className={`px-3 py-2 rounded-md text-base w-full ${tempStyle.has(st) ? 'bg-indigo-600 text-white' : 'bg-white border'}`}
                      aria-pressed={tempStyle.has(st)}
                    >{st}</button>
                  ))}
                </div>
              </div>

              <div>
                <div className="text-sm font-semibold">Retrait</div>
                <div className="mt-2 grid grid-cols-1 gap-2">
                  {LOCATIONS.map((loc) => (
                    <button
                      key={loc}
                      onClick={() => setTempLoc((cur) => toggleSet(cur, loc))}
                      className={`px-3 py-2 rounded-md text-base w-full ${tempLoc.has(loc) ? 'bg-indigo-600 text-white' : 'bg-white border'}`}
                      aria-pressed={tempLoc.has(loc)}
                    >{loc}</button>
                  ))}
                </div>
              </div>
            </div>

            <div className="fixed bottom-4 left-4 right-4">
              <div className="flex gap-3">
                <button onClick={() => { setTempSize(new Set()); setTempStyle(new Set()); setTempLoc(new Set()); }} className="flex-1 px-4 py-3 rounded-md border">RÃ©initialiser</button>
                <button onClick={applyMobileFilters} className="flex-1 px-4 py-3 rounded-md bg-indigo-600 text-white">Appliquer les filtres</button>
              </div>
            </div>
          </div>
        </div>
      )}

      {/* Sticky Filter CTA on mobile when not open */}
      <div className="sm:hidden fixed left-4 right-4 bottom-4 z-40 pointer-events-auto">
        <div className="flex gap-3">
          <button onClick={openMobile} className="flex-1 px-4 py-3 rounded-md bg-white border">Filtres</button>
          <a href="#grid" className="flex-1 px-4 py-3 rounded-md bg-indigo-600 text-white text-center">Voir ({filtered.length})</a>
        </div>
      </div>

    </section>
  );
}

Collections.propTypes = {
  items: PropTypes.array,
  whatsappNumber: PropTypes.string,
};

/** ProductCard component */
function ProductCard({ item, whatsappNumber }) {
  const [idx, setIdx] = useState(0);
  const images = item.images && item.images.length ? item.images : [null];

  const next = () => setIdx((i) => (i + 1) % images.length);
  const prev = () => setIdx((i) => (i - 1 + images.length) % images.length);

  const whatsappHref = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent("Bonjour%20Frip2R%C3%AAve%20-%20Je%20souhaite%20commander%20:"+item.title)}`;

  return (
    <article className={`border rounded-md overflow-hidden bg-white`} aria-labelledby={`p-${item.id}-title`}>
      <div className="relative bg-gray-100 h-44 flex items-center justify-center">
        {images[0] ? (
          <img src={images[idx]} alt={item.title} className="object-cover w-full h-44" />
        ) : (
          <div className="text-gray-400">Image</div>
        )}

        {images.length > 1 && (
          <div className="absolute top-2 right-2 flex items-center gap-2">
            <button aria-label="Image prÃ©cÃ©dente" onClick={prev} className="px-2 py-1 bg-white rounded focus:outline-none">â€¹</button>
            <button aria-label="Image suivante" onClick={next} className="px-2 py-1 bg-white rounded focus:outline-none">â€º</button>
          </div>
        )}

        {/* Badge */}
        <span className={`absolute left-2 top-2 px-2 py-1 text-xs font-semibold rounded ${item.status === 'sold' ? 'bg-gray-300 text-gray-700' : item.status === 'reserved' ? 'bg-yellow-400 text-black' : 'bg-green-600 text-white'}`}>
          {item.status === 'sold' ? 'Vendu' : item.status === 'reserved' ? 'RÃ©servÃ©' : 'Disponible'}
        </span>
      </div>

      <div className="p-3">
        <h3 id={`p-${item.id}-title`} className="text-sm font-semibold text-gray-900">{item.title}</h3>
        <div className="mt-1 text-xs text-gray-600">{item.size} â€¢ {item.style}</div>
        <div className="mt-2 text-sm font-medium text-gray-900">{item.price ? item.price + ' â‚¬' : ''}</div>

        {item.quantity === 1 && item.status === 'available' && (
          <div className="mt-2 text-xs text-red-600">DerniÃ¨re piÃ¨ce disponible</div>
        )}

        <div className="mt-3">
          {item.status === 'sold' ? (
            <button disabled className="w-full px-3 py-2 rounded-md bg-gray-200 text-gray-600">Vendu</button>
          ) : (
            <div className="flex gap-2">
              <a href={whatsappHref} target="_blank" rel="noopener noreferrer" className="flex-1 px-3 py-2 rounded-md bg-[#7B2D2D] text-white text-center">Commander</a>
              <button className="px-3 py-2 rounded-md border">Voir dÃ©tails</button>
            </div>
          )}
        </div>
      </div>
    </article>
  );
}

ProductCard.propTypes = {
  item: PropTypes.object.isRequired,
  whatsappNumber: PropTypes.string,
};

// Exemple de donnÃ©es par dÃ©faut (12 piÃ¨ces) â€” visibles sans JS uniquement si intÃ©grÃ©es cÃ´tÃ© serveur
const defaultItems = [
  { id: 1, title: "Robe Wax rouge", size: "M", style: "Traditionnel", location: "Beauvais", available: true, status: 'available', quantity: 2, price: 35, images: [] },
  { id: 2, title: "Chemise Ankara homme", size: "L", style: "Moderne", location: "Amiens", available: true, status: 'available', quantity: 1, price: 45, images: [] },
  { id: 3, title: "Jupe vintage", size: "S", style: "Mixte", location: "Paris (point relais)", available: true, status: 'reserved', quantity: 1, price: 28, images: [] },
  { id: 4, title: "T-shirt casual", size: "M", style: "Moderne", location: "Beauvais", available: true, status: 'available', quantity: 3, price: 12, images: [] },
  { id: 5, title: "Veste chic", size: "XL", style: "Mixte", location: "Livraison possible", available: true, status: 'available', quantity: 1, price: 60, images: [] },
  { id: 6, title: "Ensemble enfant", size: "XS", style: "Enfant", location: "Beauvais", available: true, status: 'available', quantity: 4, price: 20, images: [] },
  { id: 7, title: "Sac accessoires", size: "Taille unique", style: "Accessoires", location: "Paris (point relais)", available: true, status: 'available', quantity: 2, price: 15, images: [] },
  { id: 8, title: "Robe Wax bleue", size: "L", style: "Traditionnel", location: "Amiens", available: true, status: 'available', quantity: 1, price: 40, images: [] },
  { id: 9, title: "Chemisette casual", size: "M", style: "Moderne", location: "Livraison possible", available: true, status: 'available', quantity: 2, price: 18, images: [] },
  { id: 10, title: "Gilet vintage", size: "XL", style: "Vintage", location: "Beauvais", available: true, status: 'sold', quantity: 0, price: 30, images: [] },
  { id: 11, title: "Pantalon Ankara", size: "S", style: "Traditionnel", location: "Amiens", available: true, status: 'available', quantity: 1, price: 25, images: [] },
  { id: 12, title: "Blouse chic", size: "M", style: "Mixte", location: "Paris (point relais)", available: true, status: 'available', quantity: 2, price: 32, images: [] },
];
import React, { useState } from 'react';
import PropTypes from 'prop-types';

/**
 * Section â€” FAQ Boutique
 * Objectif : lever les doutes avant commande (mobile-first, accessible)
 */

const FAQ_ITEMS = [
  {
    q: "Puis-je retourner un article ?",
    a: (
      <>
        <p>Oui, sous conditions. Les retours sont acceptÃ©s sous <strong>7 jours aprÃ¨s rÃ©ception</strong>, uniquement si lâ€™article nâ€™a pas Ã©tÃ© portÃ© et est dans son Ã©tat dâ€™origine.</p>
        <p className="text-xs text-gray-500 mt-2">Les frais de retour sont Ã  la charge de lâ€™acheteur.</p>
      </>
    ),
  },
  {
    q: "Puis-je Ã©changer un article ?",
    a: (
      <>
        <p>Oui, si une autre piÃ¨ce est disponible. Lâ€™Ã©change se fait lors dâ€™un retrait ou dâ€™une prochaine vente, selon les stocks.</p>
        <p className="text-xs text-gray-500 mt-2">Les piÃ¨ces sont uniques â€” lâ€™Ã©change dÃ©pend des disponibilitÃ©s.</p>
      </>
    ),
  },
  {
    q: "Comment se passe la livraison ?",
    a: (
      <>
        <ul className="list-disc list-inside">
          <li>Retrait local (Beauvais) â€” gratuit</li>
          <li>Livraison â€” frais calculÃ©s lors de la confirmation</li>
        </ul>
        <p className="text-xs text-gray-500 mt-2">Livraison possible en France mÃ©tropolitaine.</p>
      </>
    ),
  },
  {
    q: "Quels sont les dÃ©lais ?",
    a: (
      <ul className="list-disc list-inside">
        <li>Retrait local : sous 2 Ã  5 jours aprÃ¨s confirmation</li>
        <li>Livraison : 5 Ã  10 jours ouvrÃ©s</li>
      </ul>
    ),
  },
  {
    q: "Comment payer ?",
    a: (
      <>
        <ul className="list-disc list-inside">
          <li>Virement bancaire</li>
          <li>Lien de paiement sÃ©curisÃ©</li>
        </ul>
        <p className="text-xs text-gray-500 mt-2">Les instructions sont envoyÃ©es aprÃ¨s la commande.</p>
      </>
    ),
  },
  {
    q: "Les vÃªtements sont-ils neufs ?",
    a: (
      <>
        <p>Non. Ce sont des vÃªtements africains de seconde main, triÃ©s, rÃ©parÃ©s et remis Ã  neuf avec soin.</p>
        <p className="text-xs text-gray-500 mt-2">Chaque piÃ¨ce est unique.</p>
      </>
    ),
  },
  {
    q: "Et si lâ€™article nâ€™est plus disponible ?",
    a: (
      <p>La disponibilitÃ© est confirmÃ©e manuellement. Si lâ€™article est dÃ©jÃ  rÃ©servÃ© ou vendu, nous te proposons une alternative ou un remboursement.</p>
    ),
  },
];

export default function FAQBoutique({ whatsappNumber = '33123456789' }) {
  const [openIndex, setOpenIndex] = useState(0); // une question ouverte par dÃ©faut

  const toggle = (i) => setOpenIndex(prev => (prev === i ? -1 : i));

  const whatsappHref = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(
    'Bonjour Frip2RÃªve â€” Jâ€™ai une question avant de commander.'
  )}`;

  return (
    <section className="max-w-3xl mx-auto px-4 py-12" aria-labelledby="faq-title">
      <header className="mb-6">
        <h2 id="faq-title" className="text-2xl font-extrabold">Questions frÃ©quentes</h2>
        <p className="text-sm text-gray-600 mt-1">Tout ce quâ€™il faut savoir avant de commander.</p>
      </header>

      <div className="divide-y border rounded-md">
        {FAQ_ITEMS.map((item, i) => {
          const isOpen = openIndex === i;
          return (
            <div key={i}>
              <button
                type="button"
                className="w-full flex justify-between items-center text-left px-4 py-4 focus:outline-none focus-visible:ring"
                aria-expanded={isOpen}
                aria-controls={`faq-panel-${i}`}
                onClick={() => toggle(i)}
              >
                <span className="font-medium">{item.q}</span>
                <span aria-hidden className="ml-4">{isOpen ? 'âˆ’' : '+'}</span>
              </button>
              <div
                id={`faq-panel-${i}`}
                role="region"
                className={`px-4 pb-4 text-sm text-gray-700 ${isOpen ? 'block' : 'hidden'}`}
              >
                {item.a}
              </div>
            </div>
          );
        })}
      </div>

      {/* CTA */}
      <div className="mt-8 text-center">
        <p className="font-medium mb-3">Une autre question ?</p>
        <a
          href={whatsappHref}
          target="_blank"
          rel="noopener noreferrer"
          aria-label="Contacter Frip2RÃªve via WhatsApp"
          className="inline-block px-5 py-3 bg-[#7B2D2D] text-white rounded-md"
        >
          Contacter via WhatsApp
        </a>
        <p className="text-xs text-gray-500 mt-2">RÃ©ponse rapide â€¢ humaine â€¢ sans robot</p>
      </div>
    </section>
  );
}

FAQBoutique.propTypes = {
  whatsappNumber: PropTypes.string,
};
// src/pages/Frip2Reve/sections/Hero.js
import React from 'react';
import PropTypes from 'prop-types';
import { ExternalLink } from 'lucide-react';

// Import direct du logo prÃ©sent dans src/assets/frip2reve/logo.png
// Chemin relatif depuis src/pages/Frip2Reve/sections -> remonter jusqu'Ã  src (../../..)
import defaultLogo from '../../../assets/frip2reve/logo.png';
// Logo par dÃ©faut de l'association RÃªve d'Ivoire (ajuster le chemin si nÃ©cessaire)
import defaultAssocLogo from '../../../assets/revedivoire/logo.png';
import defaultHeroBg from '../../../assets/frip2reve/hero.png';

/**
 * HeroFrip2Reve
 * - Section principale pour Frip2RÃªve
 * - Plein Ã©cran desktop / 60-75vh mobile
 * - Background image via <picture> + lazy-loading + srcset
 * - Overlay gradient, SVG lion dÃ©coratif, logo EDISYNCHRO en bas-right
 * - Respecte prefers-reduced-motion
 *
 * NouveautÃ©s pour association:
 *  - associationName, associationLink, associationLogo, showAssociation
 *  - micro-copy et footer de la section mettent en valeur le partenariat
 *
 */

export default function HeroFrip2Reve({
  images = {
    desktop: defaultHeroBg,
    mobile: defaultHeroBg,
  },
  alt = 'Stand Frip2RÃªve â€” vÃªtements africains remis en valeur',
  logoPath, // possibilitÃ© d'override externe pour logo EDISYNCHRO
  lionSvg = defaultLogo,
  collectionsLink = '/collections',
  whatsappLink = 'https://wa.me/33123456789?text=Bonjour%20Frip2R%C3%AAve%20--',
  liveLink = '#',
  preferWhatsAppOnMobile = false,
  className = '',
  // Association props
  showAssociation = true,
  associationName = 'Association RÃªve d\'Ivoire',
  associationLink = 'https://revedivoire.org',
  associationLogo = defaultAssocLogo,
  associationTagline = 'Financement des actions solidaires en CÃ´te d\'Ivoire',
}) {
  // Order classes for CTA on mobile if preferWhatsAppOnMobile
  const whatsappOrder = preferWhatsAppOnMobile ? 'order-1 sm:order-2' : 'order-2 sm:order-2';
  const primaryOrder = preferWhatsAppOnMobile ? 'order-2 sm:order-1' : 'order-1 sm:order-1';

  // Utilise le logo importÃ© comme fallback si pas de logoPath passÃ© en prop
  const logoSrc = logoPath || defaultLogo;

  return (
    <header className={`relative w-full overflow-hidden ${className}`} role="banner">
      {/* Background image using picture for srcset + lazy-loading */}
      <picture className="absolute inset-0 w-full h-full pointer-events-none">
        <source media="(min-width:1024px)" srcSet={images.desktop} />
        <img
          src={images.mobile}
          alt={alt}
          className="w-full h-full object-cover object-center"
          loading="lazy"
        />
      </picture>

      {/* Overlay gradient */}
      <div
        className="absolute inset-0"
        aria-hidden="true"
        style={{
          background: 'linear-gradient(180deg, rgba(0,0,0,0) 10%, rgba(0,0,0,0.45) 100%)',
        }}
      />

      {/* Decorative lion SVG (top-left) */}
      <img
        src={lionSvg}
        alt=""
        aria-hidden="true"
        className="absolute left-6 top-6 w-30 h-12 opacity-90"
        style={{ pointerEvents: 'none' }}
      />

      {/* Content container */}
      <div className="relative z-10 max-w-6xl mx-auto px-4 sm:px-6 lg:px-8" style={{ minHeight: '60vh' }}>
        <div className="flex flex-col-reverse lg:flex-row items-center lg:items-end justify-between h-full py-12 lg:py-20">
          {/* Left column: text */}
          <div className="w-full lg:w-2/3 text-left">
            <div className="max-w-2xl">
              <h1
                className="text-3xl sm:text-4xl lg:text-5xl font-extrabold leading-tight text-white drop-shadow-md"
                style={{ fontSize: 'clamp(28px, 4vw, 40px)' }}
              >
                Frip2RÃªve â€” VÃªtements africains remis Ã  neuf
              </h1>

              <p className="mt-4 text-sm sm:text-base text-gray-100 max-w-prose" style={{ fontSize: '16px' }}>
                PiÃ¨ces uniques triÃ©es, rÃ©parÃ©es et vendues pour financer nos actions solidaires en CÃ´te dâ€™Ivoire.
                Commandez facilement via Instagram ou WhatsApp â€” retrait local possible.
              </p>

              {/* CTAs */}
              <div className="mt-6 flex flex-col sm:flex-row items-stretch sm:items-center gap-3">
                <a
                  href={collectionsLink}
                  className={`${primaryOrder} inline-flex items-center justify-center px-5 py-3 rounded-lg shadow focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-white bg-[#7B2D2D] text-white font-semibold text-sm sm:text-base w-full sm:w-auto transition-transform transform will-change-transform`}
                  aria-label="Voir les collections Frip2RÃªve â€” boutique solidaire"
                >
                  Voir les collections
                </a>

                <a
                  href={whatsappLink}
                  target="_blank"
                  rel="noopener noreferrer"
                  className={`${whatsappOrder} inline-flex items-center justify-center px-5 py-3 rounded-lg border border-white bg-transparent text-white font-semibold text-sm sm:text-base w-full sm:w-auto transition-shadow focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-white`}
                  aria-label="Commander via WhatsApp â€” Frip2RÃªve"
                >
                  Commander via WhatsApp
                </a>

                <a
                  href={liveLink}
                  className="order-3 inline-flex items-center gap-2 text-sm text-gray-100 hover:underline w-full sm:w-auto"
                  aria-label="S'inscrire au live-vente Frip2RÃªve"
                >
                  Sâ€™inscrire au live-vente
                </a>
              </div>

              {/* Micro-copy */}
              <div className="mt-4 text-xs text-gray-200">
                Retrait local â€¢ Paiement par virement / lien sÃ©curisÃ© â€¢ Â« MÃªme 1 piÃ¨ce compte Â»
                {showAssociation && (
                  <> â€” En association avec <a href={associationLink} target="_blank" rel="noopener noreferrer" className="underline">{associationName}</a></>
                )}
              </div>
            </div>
          </div>

          {/* Right column: optional visual / logos */}
          <div className="w-full lg:w-1/3 flex justify-end lg:justify-end mb-6 lg:mb-0">
            <div className="flex items-end gap-4">
              {/* Association logo (if activÃ©) */}
              {showAssociation && (
                <a
                  href={associationLink}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="flex items-center gap-2 text-white text-xs opacity-90 hover:opacity-100"
                  aria-label={`En association avec ${associationName}`}
                >
                  <img src={associationLogo} alt={associationName} className="w-16 h-auto rounded-sm shadow-sm" />
                  <span className="hidden sm:inline text-xs">{associationName}</span>
                  <ExternalLink className="w-3 h-3 ml-1" />
                </a>
              )}

              {/* EDISYNCHRO logo bottom-right */}
              <a
                href="https://edisynchro.com"
                target="_blank"
                rel="noopener noreferrer"
                className="flex items-center gap-2 text-white text-xs opacity-90 hover:opacity-100"
                aria-label="PropulsÃ© par EDISYNCHRO"
              >
                <img src={logoSrc} alt="EDISYNCHRO" className="w-20 h-auto rounded-sm shadow-sm" />
                <span className="hidden sm:inline">PropulsÃ© par EDISYNCHRO</span>
                <ExternalLink className="w-3 h-3 ml-1" />
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Accessibility / animation styles (respect prefers-reduced-motion) */}
      <style>{`
        @media (prefers-reduced-motion: reduce) {
          .hero-animate { animation: none !important; transition: none !important; }
        }
      `}</style>

      {/* Meta recommendations (not rendered) */}
      {false && (
        <div data-meta>
          {/*
            Meta title: Frip2RÃªve â€” Friperie solidaire de vÃªtements africains
            Meta description: PiÃ¨ces africaines remis Ã  neuf. Ventes solidaires via Frip2RÃªve : commande via Instagram/WhatsApp, retraits locaux et live-ventes. Tous les profits financent nos actions en CÃ´te dâ€™Ivoire en association avec l'Association RÃªve d'Ivoire.
          */}
        </div>
      )}
    </header>
  );
}

HeroFrip2Reve.propTypes = {
  images: PropTypes.shape({ desktop: PropTypes.string, mobile: PropTypes.string }),
  alt: PropTypes.string,
  logoPath: PropTypes.string,
  lionSvg: PropTypes.string,
  collectionsLink: PropTypes.string,
  whatsappLink: PropTypes.string,
  liveLink: PropTypes.string,
  preferWhatsAppOnMobile: PropTypes.bool,
  className: PropTypes.string,
  // Association
  showAssociation: PropTypes.bool,
  associationName: PropTypes.string,
  associationLink: PropTypes.string,
  associationLogo: PropTypes.string,
  associationTagline: PropTypes.string,
};
import React, { useEffect, useState } from 'react';
import PropTypes from 'prop-types';

export default function ProductDetail({
  product,
  loading = false,
  whatsappNumber = '33123456789',
  instagramLink,
}) {
  // Hooks ALWAYS at the top
  const [currentImg, setCurrentImg] = useState(0);

  useEffect(() => {
    console.log('ProductDetail: product prop ->', product);
  }, [product]);

  // Reset currentImg if images length changes (avoid index out of bounds)
  useEffect(() => {
    const len = Array.isArray(product?.images) ? product.images.length : 0;
    setCurrentImg(prev => (prev >= len ? 0 : prev));
  }, [product?.images]);

  // Si on est en loading ou pas de product -> skeleton
  if (loading || !product) {
    return (
      <section className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div className="w-full h-64 bg-gray-100 flex items-center justify-center rounded-md">
          Chargement du produitâ€¦
        </div>
      </section>
    );
  }

  // DÃ©fensive : product.images peut Ãªtre undefined
  const images = Array.isArray(product.images) && product.images.length ? product.images : [null];

  const nextImg = () => setCurrentImg(i => (i + 1) % images.length);
  const prevImg = () => setCurrentImg(i => (i - 1 + images.length) % images.length);

  const isAvailable = product?.status === 'available';
  const isReserved = product?.status === 'reserved';
  const isSold = product?.status === 'sold';

  const productNameForText = product?.name || 'produit';
  const whatsappHref = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(
    `Bonjour Frip2RÃªve - Je souhaite commander : ${productNameForText}`
  )}`;

  return (
    <section className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <div className="relative w-full mb-6">
        {images[0] ? (
          <img
            src={images[currentImg]}
            alt={Array.isArray(product.imagesAlt) ? product.imagesAlt?.[currentImg] || productNameForText : productNameForText}
            className="w-full h-auto object-cover rounded-md"
          />
        ) : (
          <div className="w-full h-64 bg-gray-200 flex items-center justify-center">Image produit</div>
        )}

        {images.length > 1 && (
          <div className="absolute top-1/2 left-2 right-2 flex justify-between">
            <button onClick={prevImg} aria-label="Image prÃ©cÃ©dente" className="bg-white px-2 py-1 rounded-md">â€¹</button>
            <button onClick={nextImg} aria-label="Image suivante" className="bg-white px-2 py-1 rounded-md">â€º</button>
          </div>
        )}

        {isReserved && <span className="absolute top-2 left-2 px-2 py-1 bg-yellow-400 text-black text-xs font-semibold rounded">RÃ©servÃ©</span>}
        {isSold && <span className="absolute top-2 left-2 px-2 py-1 bg-gray-400 text-white text-xs font-semibold rounded">Vendu</span>}
      </div>

      <div className="flex flex-col lg:flex-row gap-6">
        <div className="lg:w-2/3">
          <h1 className="text-2xl sm:text-3xl font-extrabold mb-2">{product?.name}</h1>
          <p className="text-gray-700 mb-2">{product?.subtitle}</p>
          <p className="text-gray-600 mb-4">{product?.description}</p>

          <ul className="text-sm text-gray-700 mb-4 space-y-1">
            <li>Taille : {product?.size}</li>
            <li>Style : {product?.style}</li>
            <li>Ã‰tat : {product?.condition}</li>
            <li>DisponibilitÃ© : {isAvailable ? 'En stock' : isReserved ? 'RÃ©servÃ©' : 'Vendu'}</li>
            <li>Retrait : {product?.location}</li>
          </ul>

          <p className="text-xs text-gray-500 mb-4">Remis Ã  neuf manuellement par lâ€™Ã©quipe Frip2RÃªve.</p>
          <div className="text-2xl font-bold text-gray-900 mb-1">{product?.price ?? 'â€”'} â‚¬</div>
          <p className="text-xs text-gray-500 mb-4">Tous les bÃ©nÃ©fices financent nos actions solidaires.</p>

          {isAvailable && (
            <div className="flex flex-col sm:flex-row gap-3 mb-4">
              <a href={whatsappHref} target="_blank" rel="noopener noreferrer" aria-label="Commander via WhatsApp" className="flex-1 px-4 py-3 bg-[#7B2D2D] text-white rounded-md text-center">Commander via WhatsApp</a>
              {instagramLink && <a href={instagramLink} target="_blank" rel="noopener noreferrer" aria-label="Commander via Instagram" className="flex-1 px-4 py-3 border rounded-md text-center">Commander via Instagram</a>}
            </div>
          )}

          {!isAvailable && isReserved && <div className="mb-4 text-yellow-600 font-semibold">Cette piÃ¨ce est en cours de rÃ©servation.</div>}
          {!isAvailable && isSold && <div className="mb-4 text-gray-600 font-semibold">PiÃ¨ce vendue. <a href="#collections" className="text-indigo-600 underline">Voir des piÃ¨ces similaires</a></div>}

          <ul className="flex flex-wrap gap-3 text-xs text-gray-700">
            <li>PiÃ¨ce unique</li>
            <li>Retrait local possible</li>
            <li>Paiement par virement ou lien sÃ©curisÃ©</li>
            <li>Association solidaire</li>
          </ul>
        </div>
      </div>

      {isAvailable && (
        <div className="sm:hidden fixed bottom-0 left-0 right-0 bg-white p-3 border-t z-40">
          <a href={whatsappHref} target="_blank" rel="noopener noreferrer" aria-label="Commander via WhatsApp" className="w-full px-4 py-3 bg-[#7B2D2D] text-white rounded-md text-center">Commander via WhatsApp</a>
        </div>
      )}
    </section>
  );
}

ProductDetail.propTypes = {
  product: PropTypes.shape({
    id: PropTypes.oneOfType([PropTypes.string, PropTypes.number]),
    name: PropTypes.string,
    subtitle: PropTypes.string,
    description: PropTypes.string,
    size: PropTypes.string,
    style: PropTypes.string,
    condition: PropTypes.string,
    location: PropTypes.string,
    price: PropTypes.oneOfType([PropTypes.number, PropTypes.string]),
    status: PropTypes.oneOf(['available','reserved','sold']),
    images: PropTypes.arrayOf(PropTypes.string),
    imagesAlt: PropTypes.arrayOf(PropTypes.string),
  }),
  loading: PropTypes.bool,
  whatsappNumber: PropTypes.string,
  instagramLink: PropTypes.string,
};
import React from 'react';

export default function ProductDetailSkeleton() {
  return (
    <section className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8 animate-pulse">
      <div className="w-full h-64 bg-gray-200 rounded-md mb-6" />
      <div className="space-y-3">
        <div className="h-6 bg-gray-200 rounded w-1/2" />
        <div className="h-4 bg-gray-200 rounded w-3/4" />
        <div className="h-4 bg-gray-200 rounded w-full" />
        <div className="h-4 bg-gray-200 rounded w-2/3" />
      </div>
    </section>
  );
}
import React, { useEffect, useState } from 'react';
import PropTypes from 'prop-types';

/**
 * Produits.js
 * Section "Produits" pour Frip2RÃªve
 *
 * - Fetch list / fallback
 * - Recherche simple + filtres de disponibilitÃ©
 * - Grid responsive
 * - AperÃ§u/Modal simple via le composant Produit (dÃ©tail)
 *
 * IMPORTANT: Aucun hook n'est appelÃ© conditionnellement â€” respecte ESLint react-hooks/rules-of-hooks
 */

/* ---------------------- Small skeleton card ---------------------- */
function CardSkeleton() {
  return (
    <article className="animate-pulse bg-white rounded-md p-3 shadow-sm">
      <div className="w-full h-40 bg-gray-200 rounded mb-3" />
      <div className="h-5 bg-gray-200 rounded w-3/4 mb-2" />
      <div className="h-4 bg-gray-200 rounded w-1/2" />
    </article>
  );
}

/* ---------------------- Product compact card ---------------------- */
function ProductCard({ product, onOpen }) {
  const isAvailable = product?.status === 'available';
  const thumb = Array.isArray(product.images) && product.images[0] ? product.images[0] : null;

  return (
    <article className="bg-white rounded-md overflow-hidden shadow-sm group">
      <button
        type="button"
        onClick={() => onOpen(product)}
        className="w-full text-left"
        aria-label={`Ouvrir la fiche de ${product.name}`}
      >
        <div className="w-full h-40 bg-gray-50 flex items-center justify-center overflow-hidden">
          {thumb ? (
            <img src={thumb} alt={product.imagesAlt?.[0] || product.name} className="w-full h-full object-cover" />
          ) : (
            <div className="text-gray-400">Pas d'image</div>
          )}
        </div>

        <div className="p-3">
          <h3 className="text-sm font-semibold truncate">{product.name}</h3>
          <div className="mt-2 flex items-center justify-between">
            <div>
              <div className="text-base font-bold">{product.price ?? 'â€”'} â‚¬</div>
              <div className="text-xs text-gray-500">Taille: {product.size || 'â€”'}</div>
            </div>
            <div className="text-xs">
              {isAvailable ? (
                <span className="inline-block px-2 py-1 bg-green-50 text-green-700 rounded-full">En stock</span>
              ) : product.status === 'reserved' ? (
                <span className="inline-block px-2 py-1 bg-yellow-50 text-yellow-800 rounded-full">RÃ©servÃ©</span>
              ) : (
                <span className="inline-block px-2 py-1 bg-gray-100 text-gray-700 rounded-full">Vendu</span>
              )}
            </div>
          </div>
        </div>
      </button>
    </article>
  );
}

ProductCard.propTypes = {
  product: PropTypes.object.isRequired,
  onOpen: PropTypes.func.isRequired,
};

/* ---------------------- Main section ---------------------- */
export default function Produits({ apiEndpoint = '/api/frip2reve/products', initial = null }) {
  // Hooks â€” toujours appelÃ©s, jamais conditionnels
  const [products, setProducts] = useState(initial);
  const [loading, setLoading] = useState(!initial);
  const [error, setError] = useState(null);

  const [query, setQuery] = useState('');
  const [filter, setFilter] = useState('all'); // all | available | reserved | sold

  const [selected, setSelected] = useState(null); // produit ouvert en dÃ©tail

  // Fetch products si pas fournis en prop initial
  useEffect(() => {
    let mounted = true;
    if (initial) {
      setLoading(false);
      return () => { mounted = false; };
    }

    async function load() {
      setLoading(true);
      setError(null);
      try {
        const res = await fetch(apiEndpoint);
        if (!res.ok) throw new Error(`HTTP ${res.status}`);
        const data = await res.json();
        if (mounted) setProducts(Array.isArray(data) ? data : []);
      } catch (err) {
  if (mounted) {
    // pas d'erreur visible si fallback volontaire
    setProducts([
      {
        id: 'demo-1',
        name: 'Veste africaine en wax â€” piÃ¨ce unique',
        price: 45,
        size: 'M',
        status: 'available',
        images: ['https://picsum.photos/seed/frip1/800/600'],
        imagesAlt: ['Veste wax â€” vue face'],
      },
      {
        id: 'demo-2',
        name: 'Robe wax â€” imprimÃ© floral',
        price: 38,
        size: 'L',
        status: 'reserved',
        images: ['https://picsum.photos/seed/frip2/800/600'],
        imagesAlt: ['Robe wax â€” vue face'],
      },
      {
        id: 'demo-3',
        name: 'Chemise traditionnelle rÃ©-Ã©pinglÃ©e',
        price: 30,
        size: 'S',
        status: 'sold',
        images: ['https://picsum.photos/seed/frip3/800/600'],
        imagesAlt: ['Chemise â€” dÃ©tail'],
      },
    ]);
  }
}

finally {
        if (mounted) setLoading(false);
      }
    }

    load();
    return () => { mounted = false; };
  }, [apiEndpoint, initial]);

  // Filtrage + recherche
  const normalizedQuery = query.trim().toLowerCase();
  const visible = (products || []).filter((p) => {
    if (filter !== 'all' && p.status !== filter) return false;
    if (!normalizedQuery) return true;
    return (
      (p.name && p.name.toLowerCase().includes(normalizedQuery)) ||
      (p.shortDescription && p.shortDescription.toLowerCase().includes(normalizedQuery))
    );
  });

  return (
    <section id="produits" className="max-w-6xl mx-auto px-4 py-8">
      <header className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 mb-6">
        <div>
          <h2 className="text-xl font-extrabold">Nos piÃ¨ces â€” Frip2RÃªve</h2>
          <p className="text-sm text-gray-600 mt-1">PiÃ¨ces uniques triÃ©es et remises Ã  neuf. Chaque achat finance nos actions solidaires.</p>
        </div>

        <div className="flex gap-2 items-center">
          <label htmlFor="search" className="sr-only">Rechercher</label>
          <input
            id="search"
            type="search"
            placeholder="Rechercher (nom, description)..."
            value={query}
            onChange={(e) => setQuery(e.target.value)}
            className="px-3 py-2 border rounded-md shadow-sm w-48 sm:w-64"
          />

          <select
            aria-label="Filtrer disponibilitÃ©"
            value={filter}
            onChange={(e) => setFilter(e.target.value)}
            className="px-2 py-2 border rounded-md"
          >
            <option value="all">Tous</option>
            <option value="available">En stock</option>
            <option value="reserved">RÃ©servÃ©</option>
            <option value="sold">Vendu</option>
          </select>
        </div>
      </header>

      {/* Error */}
      {error && (
        <div className="mb-4 p-3 rounded-md bg-yellow-50 border border-yellow-100 text-sm text-yellow-800">
          {error}
        </div>
      )}

      {/* Loading skeleton */}
      {loading ? (
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
          <CardSkeleton />
          <CardSkeleton />
          <CardSkeleton />
        </div>
      ) : (
        <>
          {visible.length === 0 ? (
            <div className="text-center py-8 text-gray-600">Aucune piÃ¨ce trouvÃ©e pour ces critÃ¨res.</div>
          ) : (
            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
              {visible.map((p) => (
                <ProductCard key={p.id || p.name} product={p} onOpen={setSelected} />
              ))}
            </div>
          )}
        </>
      )}

      {/* DÃ©tail produit (modale simple ou section detail) */}
      {selected && (
        <div
          role="dialog"
          aria-modal="true"
          aria-label={`DÃ©tail de ${selected.name}`}
          className="fixed inset-0 z-50 flex items-start justify-center p-4 sm:p-8"
        >
          <div
            className="absolute inset-0 bg-black/40"
            onClick={() => setSelected(null)}
            aria-hidden="true"
          />
          <div className="relative w-full max-w-3xl bg-white rounded-md shadow-lg overflow-auto max-h-[90vh]">
            <div className="sticky top-0 bg-white p-3 border-b flex justify-between items-center z-10">
              <h3 className="font-semibold">{selected.name}</h3>
              <button
                type="button"
                aria-label="Fermer"
                onClick={() => setSelected(null)}
                className="px-3 py-1 rounded-md bg-gray-100"
              >
                Fermer
              </button>
            </div>

            {/* On rÃ©utilise ton composant Produit pour le rendu dÃ©taillÃ© */}

          </div>
        </div>
      )}
    </section>
  );
}

Produits.propTypes = {
  apiEndpoint: PropTypes.string,
  initial: PropTypes.array,
};
PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages> ls


    Répertoire : C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages


Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
d-----        12/12/2025     18:22                About
d-----        12/12/2025     22:44                Actions
d-----        13/12/2025     03:45                actualites
d-----        13/12/2025     21:18                contact
d-----        13/12/2025     14:57                don
d-----        13/12/2025     00:52                Frip2Reve
-a----        12/12/2025     20:18            552 Home.js


PS C:\Users\Dell Latitude 5480\Desktop\Projet Sibly\site_vitrine\src\pages>


voici tout ce que j'ai fais pour elle en deux jours, fais une presentation a natacha 